# ERP 시스템 기능화 체크리스트 및 실행 계획

작성일: 2026-07-04

목표: 현재 구현된 ERP 화면 디자인을 변경하지 않고, 동일한 화면 구성 위에 실제 작동 기능을 단계적으로 연결한다.

핵심 원칙:

- 현재 화면의 레이아웃, 폰트 비율, 색상, 간격, 테이블 구성은 기능 작업 중 변경하지 않는다.
- 먼저 프론트엔드에서 실제 시스템처럼 작동하게 만들고, 이후 백엔드와 데이터베이스를 연결한다.
- 결제 요청, 승인, 지급 흐름을 최우선으로 구현한다.
- 모든 승인, 반려, 지급, 수정 이력은 감사 로그로 남긴다.

## 0. 진행 단계 요약

- [x] 1단계: 전체 주요 화면 시안 구성
- [x] 2단계: 1920 x 1080 기준 화면 검증
- [x] 3단계: UI 기준 고정 및 기준 스크린샷 보관
- [x] 4단계: 데이터 모델 및 업무 상태 정의
- [x] 5단계: 프론트엔드 mock 기능 연결
- [x] 6단계: 결제 요청, 승인, 지급 핵심 플로우 구현
- [x] 7단계: 예산, 거래처, 보고서, 설정, 즐겨찾기 기능 구현
- [x] 8단계: API 계층 분리
- [x] 9단계: 백엔드 및 데이터베이스 연결
- [x] 10단계: 권한, 감사 로그, 파일 업로드, 알림 구현
- [x] 11단계: 테스트, 성능 검증, 배포 준비
- [x] 12단계: 화면 버튼별 실제 사용 기능 보강

## 1. 현재 화면 기준 고정

- [x] 대시보드 화면 구성
- [x] 결제 요청 화면 구성
- [x] 승인 관리 화면 구성
- [x] 지급 관리 화면 구성
- [x] 예산 관리 화면 구성
- [x] 거래처 관리 화면 구성
- [x] 보고서 화면 구성
- [x] 시스템 설정 화면 구성
- [x] 즐겨찾기 화면 구성
- [x] 1920 x 1080 기준 화면 검증
- [x] 현재 화면별 기준 스크린샷 보관
- [x] 화면별 라우트 목록 정리
- [x] 화면별 주요 컴포넌트 목록 정리
- [x] 기능 작업 중 UI 변경 금지 기준 문서화
- [x] 1920 x 1080 기준 시각 검증 체크 방식 정리
- [x] 한글 문서 및 소스 파일 UTF-8 인코딩 기준 고정

## 2. 데이터 모델 정의

- [x] 사용자 모델 정의
- [x] 부서 모델 정의
- [x] 거래처 모델 정의
- [x] 결제 요청 모델 정의
- [x] 승인 모델 정의
- [x] 지급 모델 정의
- [x] 예산 모델 정의
- [x] 보고서 모델 정의
- [x] 즐겨찾기 모델 정의
- [x] 첨부파일 모델 정의
- [x] 결재선 모델 정의
- [x] 알림 모델 정의
- [x] 변경 이력 및 감사 로그 모델 정의
- [x] 상태값 표준화 (결제 요청/승인/지급 1차 범위)
- [x] 금액, 날짜, 상태, 권한 규칙 정리 (핵심 업무 규칙 1차 문서)
- [x] 화면별 필요한 데이터 필드 매핑
- [x] 목록 데이터와 상세 패널 데이터 관계 정의

## 3. 업무 상태 및 액션 규칙

- [x] 결제 요청 상태 전이표 정의
- [x] 승인 상태 전이표 정의
- [x] 지급 상태 전이표 정의
- [x] 예산 상태 전이표 정의
- [x] 거래처 상태 전이표 정의
- [x] 상태별 허용 액션 정의
- [x] 상태별 수정 가능 필드 정의
- [x] 승인 완료 건 수정 제한 규칙
- [x] 지급 완료 건 수정 및 취소 제한 규칙
- [x] 반려 건 재상신 규칙
- [x] 보류 건 재처리 규칙
- [x] 삭제 가능 조건 정의
- [x] 업무 마감 이후 처리 제한 규칙

## 4. 프론트엔드 구조 정리

- [x] 정적 배열 데이터를 별도 mock data 파일로 분리
- [x] 타입 정의 파일 분리
- [x] 페이지 컴포넌트와 데이터 계층 분리
- [x] 공통 테이블 컴포넌트 적용 범위 검토 (결제 요청/승인/지급 1차 범위)
- [x] 공통 필터 컴포넌트 적용 범위 검토 (결제 요청/승인/지급 1차 범위)
- [x] 공통 우측 상세 패널 구조 검토 (결제 요청/승인/지급 1차 범위)
- [x] 상태 배지 표시 규칙 정리
- [x] 금액 표시 포맷 공통화
- [x] 날짜 표시 포맷 공통화
- [x] 긴 텍스트와 큰 금액 표시 시 레이아웃 깨짐 검증 (자동 스모크 1차 범위)
- [x] 빈 데이터 상태 UI 정의
- [x] 로딩 상태 UI 정의
- [x] 오류 상태 UI 정의

## 5. 프론트엔드 공통 기능 연결

- [x] 검색 기능 연결 (결제 요청/승인/지급 1차 범위)
- [x] 필터 기능 연결 (결제 요청/승인/지급 1차 범위)
- [x] 페이지네이션 기능 연결 (결제 요청/승인/지급 1차 범위)
- [x] 테이블 행 선택 기능 연결 (결제 요청/승인/지급 1차 범위)
- [x] 체크박스 다중 선택 기능 연결 (결제 요청/승인/지급 1차 범위)
- [x] 우측 상세 패널 데이터 연동 (결제 요청/승인/지급 1차 범위)
- [x] 탭 상태 연동 (지급 상태 탭 1차 범위)
- [x] 정렬 기능 연결 (결제 요청/승인/지급 1차 범위)
- [x] 목록 새로고침 기능 연결 (결제 요청/승인/지급 1차 범위)
- [x] 선택 행 하이라이트 처리 (결제 요청/승인/지급 1차 범위)
- [x] 버튼 비활성화 조건 연결 (결제 요청/승인/지급 1차 범위)
- [x] 버튼 중복 클릭 방지 (결제 요청/승인/지급 1차 범위)
- [x] 작업 성공 메시지 처리 (결제 요청/승인/지급 1차 범위)
- [x] 작업 실패 메시지 처리 (결제 요청/승인/지급 1차 범위)

## 6. 결제 요청 기능

- [x] 신규 요청 버튼 동작 (mock 1차 범위)
- [x] 임시 저장 기능 (mock 1차 범위)
- [x] 제출 기능 (mock 1차 범위)
- [x] 요청 정보 입력값 관리 (우측 패널 1차 범위)
- [x] 필수값 검증 (제출 시 1차 범위)
- [x] 금액 입력 검증 (제출 시 1원 이상)
- [x] 거래처 선택 기능 (mock 거래처 1차 범위)
- [x] 부서 선택 기능 (mock 부서 1차 범위)
- [x] 요청 사유 입력 기능
- [x] 증빙 파일 표시
- [x] 증빙 파일 업로드 준비
- [x] 예산 확인 패널 연동 (mock 예산 잔액 1차 범위)
- [x] 결재선 표시 (금액 기준 예상 결재선 1차 범위)
- [x] 결제 요청 목록 갱신 (mock 1차 범위)
- [x] 임시 저장 건 재수정 (우측 패널 1차 범위)
- [x] 제출 후 수정 제한 (입력/첨부 비활성화 1차 범위)

## 7. 승인 관리 기능

- [x] 승인 대기 목록 필터링
- [x] 내 요청 보기 기능 (결재선 기준 1차 필터)
- [x] 일괄 승인 기능 (선택 행 1차 범위)
- [x] 승인 처리 기능 (mock 1차 범위)
- [x] 반려 처리 기능 (mock 1차 범위)
- [x] 보류 처리 기능 (mock 1차 범위)
- [x] 반려 사유 입력
- [x] 보류 사유 입력
- [x] 상세 정보 패널 연동 (mock 1차 범위)
- [x] 첨부 파일 목록 연동 (요청번호 기준 mock 첨부 1차 범위)
- [x] 결재선 진행 상태 연동 (상태 기반 1차 범위)
- [x] 승인/반려 이력 저장 (mock 행 이력 1차 범위)
- [x] 결재 순서 검증 (현재 결재자 기준 1차 범위)
- [x] 이미 처리된 건 중복 처리 방지

## 8. 지급 관리 기능

- [x] 지급 예정 목록 필터링
- [x] 지급 상태 탭 연동
- [x] 일괄 지급 기능 (선택 행 1차 범위)
- [x] 지급 실행 기능 (mock 1차 범위)
- [x] 보류 기능 (mock 1차 범위)
- [x] 지급 보류 사유 입력
- [x] 계좌 확인 상태 연동
- [x] 지급 일정 패널 연동
- [x] 지급 오류 상태 처리
- [x] 승인번호와 지급건 연결
- [x] 지급 완료 후 상태 잠금
- [x] 지급 실패 재처리 기준 정의

## 9. 예산 관리 기능

- [x] 예산 기간 필터 연동 (프론트 상태 1차 범위)
- [x] 부서별 예산 사용 현황 연동
- [x] 예산 항목별 사용 현황 연동
- [x] 초과 위험 항목 계산
- [x] 예산 조정 기능 (mock 증액 1차 범위)
- [x] 예산 연계 요청 목록 연동
- [x] 예산 조정 이력 조회
- [x] 사용률 기준 상태 판정
- [x] 예산 초과 시 결제 요청 제한 규칙
- [x] 예산 초과 시 추가 승인 규칙
- [x] 예산 금액 계산 기준 정의
- [x] 공급가액, 부가세, 총액 처리 기준 정의

## 10. 거래처 관리 기능

- [x] 거래처 검색 기능
- [x] 거래처 상태 필터
- [x] 계좌 확인 필터
- [x] 거래처 추가 기능
- [x] 거래처 수정 기능
- [x] 거래처 비활성화 기능
- [x] 계좌 재확인 기능
- [x] 사업자번호 중복 검증
- [x] 계좌번호 중복 검증
- [x] 세금계산서 정보 표시
- [x] 최근 지급 요청 연동
- [x] 비활성 거래처 결제 요청 제한 (활성 거래처 선택 목록 1차 범위)

## 11. 보고서 기능

- [x] 보고서 유형 필터
- [x] 보고서 생성 기능
- [x] 월별 지급 추이 데이터 연동
- [x] 부서별 지출 데이터 연동
- [x] 승인 상태 차트 연동
- [x] 보고서 목록 연동
- [x] 엑셀 다운로드 기능 (CSV 호환 1차 범위)
- [x] PDF 다운로드 기능 (단순 PDF 1차 범위)
- [x] 예약 발송 설정 기능
- [x] 저장된 보고서 관리
- [x] 보고서 권한별 조회 범위 정의
- [x] 보고서 생성 기준일 정의

## 12. 시스템 설정 기능

- [x] 결재 정책 저장
- [x] 승인 한도 구간 추가
- [x] 승인 한도 구간 수정
- [x] 승인 한도 구간 삭제
- [x] 결재선 규칙 저장
- [x] 권한 그룹 관리
  - 진행 메모(2026-07-08): 사용자 권한 탭에 세부 권한 코드 토글을 추가하고 `Role.permissions` 배열 저장 경로에 연결했다. 결제 요청, 승인, 지급, 예산, 보고서, 거래처, 감사, 시스템 설정 권한 코드를 개별로 켜고 끄며, 권한 변경 시 backend 역할 API와 세션 무효화 meta를 사용한다.
- [x] 사용자 권한 추가
- [x] 변경 이력 기록
- [x] 알림 설정 연동
- [x] 외부 연동 설정 구조 준비
- [x] 설정 변경 시 즉시 적용 범위 정의
- [x] 설정 변경 시 기존 결재 건 영향 범위 정의

## 13. 즐겨찾기 기능

- [x] 자주 쓰는 메뉴 목록 연동
- [x] 저장된 필터 목록 연동
- [x] 최근 사용 목록 연동
- [x] 바로가기 추가
- [x] 순서 편집
- [x] 즐겨찾기 삭제
- [x] 연결된 필터 표시
- [x] 사용자별 즐겨찾기 저장
- [x] 즐겨찾기 중복 추가 방지
- [x] 비활성 메뉴 즐겨찾기 처리 기준 정의

## 14. 첨부파일 및 문서 처리

- [x] 허용 파일 확장자 정의
- [x] 최대 업로드 용량 정의
- [x] 파일 업로드 실패 처리
- [x] 파일 다운로드 권한 검증
- [x] 파일 삭제 가능 조건 정의
- [x] 승인 완료 이후 첨부파일 변경 제한
- [x] 파일명 중복 처리
- [x] 파일 바이러스 검사 필요 여부 검토
- [x] PDF 미리보기 필요 여부 검토
- [x] 세금계산서 파일 관리 기준 정의

## 15. 알림 기능

- [x] 승인 요청 알림 (알림 센터 1차 범위)
- [x] 반려 알림 (알림 센터 1차 범위)
- [x] 보류 알림 (알림 센터 1차 범위)
- [x] 승인 완료 알림 (알림 센터 1차 범위)
- [x] 지급 예정 알림 (알림 센터 1차 범위)
- [x] 지급 완료 알림 (알림 센터 1차 범위)
- [x] 예산 초과 알림 (알림 센터 1차 범위)
- [x] 결재 지연 알림 (알림 센터 1차 범위)
- [x] 시스템 설정 변경 알림 (알림 센터 1차 범위)
- [x] 알림 읽음 처리
- [x] 알림 보관 기간 정의

## 16. API 계층 준비

- [x] 프론트엔드 서비스 계층 생성
- [x] mock API 함수 정의
- [x] 요청/응답 타입 정의
- [x] API 응답 표준 포맷 정의
- [x] 에러 코드 체계 정의
- [x] 로딩 상태 정의 (목록 API 1차 범위)
- [x] 오류 상태 정의 (목록 API 1차 범위)
- [x] 빈 데이터 상태 정의 (목록 API 1차 범위)
- [x] 실제 백엔드 교체 가능한 구조 설계
- [x] 목록 조회 API 패턴 정의
- [x] 상세 조회 API 패턴 정의
- [x] 생성/수정/삭제 API 패턴 정의
- [x] 승인/반려/지급 같은 액션 API 패턴 정의

## 17. 백엔드 및 데이터베이스

- [x] 백엔드 스택 확정
- [x] 데이터베이스 스택 확정
- [x] API 명세 작성
- [x] DB ERD 작성
- [x] DB 스키마 설계
- [x] DB 마이그레이션 전략 수립
- [x] 초기 샘플 데이터 seed 작성
- [x] 인증 구조 설계
- [x] 파일 업로드 저장소 설계
- [x] 감사 로그 저장 구조 설계
- [x] 승인 워크플로우 엔진 설계
- [x] 백업 및 복원 정책 정의
- [x] 개발/테스트/운영 환경 분리

## 18. 권한 및 보안

- [x] 요청자 권한 정의
- [x] 승인자 권한 정의
- [x] 재무팀 권한 정의
- [x] 관리자 권한 정의
- [x] 메뉴 접근 권한 정의
- [x] 데이터 조회 범위 정의 (결제 요청 API 1차 범위)
- [x] 승인/반려/지급 권한 검증 (프론트 액션 게이트 1차 범위)
- [x] 첨부파일 접근 권한 검증
- [x] 변경 이력 추적
- [x] 권한 매트릭스 작성
- [x] 세션 만료 처리
- [x] 비밀번호 정책 검토
- [x] 개인정보 노출 범위 검토
- [x] 감사 로그 삭제 불가 원칙 정의
- [x] 보안 이벤트 저장 구조 정의 및 파일 접근 실패 1차 기록

## 19. 동시성 및 예외 처리

- [x] 같은 결재 건 동시 승인 처리 기준
- [x] 같은 결재 건 동시 수정 처리 기준
- [x] 이미 처리된 건 재처리 방지
- [x] 네트워크 실패 시 재시도 기준
- [x] 저장 중 화면 이탈 처리
- [x] 중복 제출 방지
- [x] 일괄 처리 중 일부 실패 처리 기준
- [x] 지급 실행 중 오류 복구 기준 (계좌 재확인 후 재처리 1차 기준)
- [x] 관리자 수동 복구 기준
- [x] 데이터 정합성 검증 기준

## 20. 문서화

- [x] 화면별 기능 명세서
- [x] API 명세서
- [x] DB ERD
- [x] 권한 매트릭스
- [x] 상태 전이표
- [x] 사용자 매뉴얼
- [x] 관리자 매뉴얼
- [x] 테스트 시나리오 문서 (자동화 테스트 1차 범위)
- [x] 배포 절차 문서
- [x] 장애 대응 문서

## 21. 검증 및 배포 준비

검토 메모: 이 섹션의 완료 표시는 운영/배포 문서와 1차 검증 항목을 정리한 범위다. 실제 운영 투입 가능 여부는 환경 분리, 보안, 재무 통제, 백업 복구, 모니터링, 장애 대응 리허설까지 확인하는 아래 24장 기준으로 별도 판정한다.

- [x] 화면별 기능 테스트
- [x] 1920 x 1080 시각 회귀 테스트
- [x] 1440 해상도 확인 (자동 스모크 1차 범위)
- [x] 1280 해상도 확인
- [x] 모바일 최소 대응 확인
- [x] 긴 데이터 입력 테스트
- [x] 대량 테이블 데이터 테스트
- [x] 테이블 1,000건 성능 테스트
- [x] 테이블 10,000건 성능 테스트
- [x] 키보드 이동 및 포커스 검증
- [x] 색 대비 기본 검증
- [x] 빌드 검증
- [x] 배포 방식 결정
- [x] 운영 환경 설정 정리
- [x] 모니터링 방식 결정

## 최우선 실행 계획

### 1차 목표: 현재 UI를 유지한 채 프론트 기능화

- [x] 기준 스크린샷 저장
- [x] 타입 정의 분리
- [x] mock data 분리
- [x] mock API 계층 생성
- [x] 검색, 필터, 페이지네이션, 행 선택 기능 연결 (결제 요청/승인/지급 1차 범위)
- [x] 우측 상세 패널 데이터 연동 (결제 요청/승인/지급 1차 범위)
- [x] 결제 요청 제출 흐름 mock 구현
- [x] 승인/반려/보류 흐름 mock 구현
- [x] 지급 실행/보류 흐름 mock 구현

### 2차 목표: 핵심 업무 규칙 확정

- [x] 결제 요청 상태 전이표 작성
- [x] 승인 상태 전이표 작성
- [x] 지급 상태 전이표 작성
- [x] 권한 매트릭스 작성
- [x] 결재선 자동 생성 규칙 정의
- [x] 예산 초과 처리 규칙 정의
- [x] 중복 처리 및 동시성 기준 정의

### 3차 목표: 실제 백엔드 연결 준비

- [x] API 명세 작성
- [x] DB ERD 작성
- [x] 백엔드 스택 결정
- [x] 데이터베이스 스택 결정
- [x] 인증 방식 결정
- [x] 파일 업로드 저장 방식 결정
- [x] 감사 로그 저장 방식 결정

### 4차 목표: 운영 가능한 시스템 완성

- [x] 백엔드 API 연결 (remote service adapter 1차 범위)
- [x] DB 연결 (Prisma schema/backend DB client/seed 1차 범위)
- [x] 로그인 및 권한 적용 (session/RBAC 1차 범위)
- [x] 첨부파일 업로드 적용 (프론트 검증/목록 1차 범위)
- [x] 알림 적용 (알림 센터/읽음 처리 1차 범위)
- [x] 보고서 다운로드 적용 (CSV/PDF 1차 범위)
- [x] 테스트 자동화 (unit/e2e 1차 범위)
- [x] 배포 및 운영 문서 정리

## 22. 버튼 기능 보강 리스트

기준: 화면에 버튼으로 표시된 항목 중 버튼 이름과 성격에 비해 실제 업무 동작이 없거나 부족한 기능을 추가한다.

검토 메모: 이 섹션의 완료 표시는 프론트 화면에서 버튼이 반응하고 mock/local 상태 기준 흐름이 연결된 범위다. 실제 DB 저장, 파일 저장소 업로드, 서버 권한 적용, 새로고침 후 데이터 유지 여부는 아래 23장 데이터 연동성 기준으로 별도 판정한다.

우선순위:

- P0: 실제 업무 처리에 바로 필요한 핵심 기능
- P1: 반복 사용성과 관리 품질을 높이는 기능
- P2: 편의, 탐색, 고급 보기 기능

### 22.1 공통 버튼 및 내비게이션

- [x] P0: 모든 `닫기` 버튼이 우측 패널, 드롭다운, 상세 영역을 실제로 닫도록 연결
- [x] P0: 모든 `다운로드` 아이콘 버튼에 화면별 CSV/PDF/첨부 다운로드 동작 연결
- [x] P0: 모든 `필터` 버튼에 상세 필터 패널 열기, 적용, 초기화, 닫기 기능 연결
- [x] P0: 모든 `전체 보기` 버튼이 해당 목록 전체 페이지 또는 전체 모달로 이동하도록 연결
- [x] P1: 모든 `상세 보기` 버튼이 선택 항목 상세 패널 또는 상세 라우트로 이동하도록 연결
- [x] P1: 모든 `더보기` 버튼에 추가 목록 로딩 또는 확장/접기 기능 연결
- [x] P1: 모든 페이지네이션 숫자, 처음/이전/다음/끝 버튼을 실제 페이지 상태와 연결
- [x] P1: 모든 `rows-select`, `10개씩`, `10건씩` 버튼에 페이지 크기 선택 메뉴 연결
- [x] P1: 모바일/좁은 화면 `메뉴` 버튼에 사이드바 열기/닫기 기능 연결
- [x] P1: 버튼 클릭 결과를 toast 또는 패널 메시지로 일관되게 표시
- [x] P1: destructive 버튼에는 확인 모달과 취소 경로 추가
- [x] P2: 주요 아이콘 버튼에 tooltip과 접근성 라벨 보강

### 22.2 랜딩 및 데모 영역

- [x] P1: `제품 데모 보기` 버튼이 ERP 앱 또는 데모 라우트로 진입하도록 연결
- [x] P2: 랜딩 미리보기의 메뉴 번호 버튼이 실제 미리보기 슬라이드 전환을 수행하도록 연결
- [x] P2: 랜딩 카드의 `전체 보기` 버튼이 해당 기능 소개 또는 ERP 화면으로 이동하도록 연결
- [x] P2: 상단 랜딩 내비게이션 버튼/링크에 섹션 스크롤 또는 라우트 이동 연결

### 22.3 대시보드

- [x] P0: KPI 카드 클릭 시 관련 목록 필터 적용 후 해당 화면으로 이동
- [x] P1: `긴급 결재 상세 보기` 버튼에 긴급 결재 전체 목록/필터 이동 연결
- [x] P1: `최근 활동 상세 보기` 버튼에 감사 로그 또는 활동 이력 화면 연결
- [x] P1: `최근 결제 요청 상세 보기` 버튼에 결제 요청 목록 필터 이동 연결
- [x] P1: `이번 달` 기간 버튼에 기간 선택 메뉴와 대시보드 지표 재계산 연결
- [x] P2: 대시보드 차트 막대/도넛 클릭 시 세부 데이터 드릴다운 연결

### 22.4 결제 요청 화면

- [x] P0: 상단 날짜 필터 버튼에 기간 선택 캘린더 연결
- [x] P0: 상단 거래처/상태/상세 필터 버튼에 필터 적용, 초기화, 저장 기능 연결
- [x] P0: 결제 요청 목록 `다운로드` 버튼에 현재 필터 기준 CSV/PDF 다운로드 연결
- [x] P0: 우측 패널 `거래처 추가` 버튼이 거래처 관리 신규 등록 플로우로 이동하거나 모달을 열도록 연결
- [x] P0: 첨부파일 행 클릭 또는 파일 버튼에 미리보기/다운로드 기능 연결
- [x] P0: 예산 확인 카드 `상세 보기` 버튼에 예산 상세 패널 또는 예산 화면 이동 연결
- [x] P0: 예상 결재선 `변경` 버튼에 결재선 수동 편집 모달 연결
- [x] P1: 결제 요청 `임시 저장` 버튼에 저장 중 이탈 방지, autosave, 복구 목록 연결
- [x] P1: 결제 요청 `제출` 버튼에 제출 전 최종 확인 모달과 idempotencyKey 적용
- [x] P1: 패널 `닫기` 버튼에 변경 사항 확인 후 닫기 기능 연결

### 22.5 승인 관리 화면

- [x] P0: 승인 목록 날짜/부서/상세 필터 버튼에 실제 필터 패널 연결
- [x] P0: 승인 목록 `다운로드` 버튼에 승인 목록/결재 이력 다운로드 연결
- [x] P0: 첨부파일 항목에 미리보기, 다운로드, 권한 오류 메시지 연결
- [x] P0: `일괄 승인` 버튼에 대상 미리보기, 일부 실패 결과, 감사 로그 상세 표시 연결
- [x] P0: `승인`, `반려`, `보류` 버튼에 확인 모달, 사유 검증, 처리 후 다음 건 자동 선택 연결
- [x] P1: `내 요청` 버튼에 현재 사용자 역할별 범위 표시와 필터 해제 기능 보강
- [x] P1: 승인 상세 `닫기` 버튼에 선택 해제 또는 목록 확장 레이아웃 복귀 연결
- [x] P2: 결재선 단계 클릭 시 단계별 처리 이력 상세 표시

### 22.6 지급 관리 화면

- [x] P0: 지급 목록 날짜/은행/계좌/상세 필터 버튼에 실제 필터 패널 연결
- [x] P0: 지급 목록 `다운로드` 버튼에 지급 목록/은행 이체 파일 다운로드 연결
- [x] P0: `일괄 지급` 버튼에 지급 대상 확인 모달, 일부 실패 결과, 재시도 목록 연결
- [x] P0: 지급 상세 `지급 실행` 버튼에 최종 확인, 지급번호 생성, 감사 로그 연결
- [x] P0: 지급 상세 `보류` 버튼에 보류 사유 필수 검증과 알림 발송 연결
- [x] P0: 지급 상세 `재처리` 버튼에 오류 원인별 재처리 가능/불가 분기 연결
- [x] P0: 지급 상세 `계좌 재확인` 버튼에 실제 계좌 검증 API 또는 mock 검증 상태 흐름 연결
- [x] P1: 지급 예정일 이전/다음 버튼에 날짜 이동과 업무일 계산 연결
- [x] P1: 지급 상세 `닫기` 버튼에 패널 닫기/선택 해제 기능 연결

### 22.7 예산 관리 화면

- [x] P0: 예산 상세 필터 버튼에 예산 항목, 부서, 상태, 기간 복합 필터 패널 연결
- [x] P0: 예산 `다운로드` 버튼에 현재 필터 기준 예산 현황 다운로드 연결
- [x] P0: `예산 조정` 버튼에 조정 금액/사유 입력 모달과 승인 필요 여부 연결
- [x] P1: `목록 보기` 버튼에 카드/표 보기 전환 또는 컬럼 설정 연결
- [x] P1: 예산 테이블 페이지네이션과 행 수 선택 버튼 실제 동작 연결
- [x] P1: `예산 항목별 상세 보기` 버튼에 항목별 드릴다운 테이블 연결
- [x] P1: 연결 요청 `더보기` 버튼에 관련 결제 요청 전체 표시 연결
- [x] P1: `예산 조정 이력 보기` 버튼에 조정 이력 전체 모달 연결
- [x] P2: 예산 경고 항목 클릭 시 관련 부서/항목 필터 자동 적용

### 22.8 거래처 관리 화면

- [x] P0: 거래처 상세 필터 버튼에 상태, 계좌, 세금계산서, 최근 지급 조건 패널 연결
- [x] P0: 거래처 `다운로드` 버튼에 거래처 목록/계좌 검증 결과 다운로드 연결
- [x] P0: 거래처 구분/유형 필터 버튼에 실제 거래처 유형 필터 연결
- [x] P0: 사업자등록증/통장사본/세금계산서 파일 업로드, 다운로드, 삭제 연결
- [x] P0: 거래처 `수정` 버튼에 저장 전 변경 요약 확인과 감사 로그 연결
- [x] P0: 거래처 `비활성화` 버튼에 비활성 사유 입력, 진행 중 요청 영향 확인 연결
- [x] P1: 거래처 상세 `지급 이력` 탭에 실제 지급 이력 목록 렌더링
- [x] P1: 최근 지급 요청 `더보기` 버튼에 전체 지급 이력 모달 연결
- [x] P1: 거래처 테이블 페이지네이션과 행 수 선택 버튼 실제 동작 연결
- [x] P1: 거래처 상세 `닫기` 버튼에 패널 닫기/선택 해제 기능 연결

### 22.9 보고서 화면

- [x] P0: 보고서 상단 `다운로드` 아이콘 버튼에 현재 보고서 즉시 다운로드 메뉴 연결
- [x] P0: 보고서 목록 `상세 보기` 버튼에 보고서 상세/미리보기 패널 연결
- [x] P0: 보고서명 검색 버튼에 실제 검색 입력과 필터 적용 연결
- [x] P1: `최신순` 버튼에 정렬 메뉴 연결
- [x] P1: 보고서 목록 페이지네이션과 행 수 선택 버튼 실제 동작 연결
- [x] P1: 저장된 보고서 `전체 보기` 버튼에 저장 보고서 전체 목록 연결
- [x] P1: 저장된 보고서 별표/더보기 메뉴에 즐겨찾기, 이름 변경, 삭제, 권한 변경 연결
- [x] P1: 예약 발송 `추가` 버튼에 수신자, 주기, 시간, 형식 입력 모달 연결
- [x] P2: 차트 클릭 시 원천 데이터 테이블 드릴다운 연결
  - 진행 메모(2026-07-06): 보고서 차트의 월별 지급, 부서별 지출, 승인 상태 클릭이 메시지 표시에서 원천 행 테이블 패널 표시로 확장되었다. 저장된 보고서 `더보기` 메뉴는 이름 변경, 공유 권한 저장, 삭제를 `reports` row update/delete API로 호출한다. 저장된 보고서 별표 버튼도 `FavoriteItem` 조회, 생성, 재활성화, 비활성화 API로 연결했다.

### 22.10 시스템 설정 화면

- [x] P0: 설정 상단 탭 버튼이 실제 섹션 전환 또는 스크롤 이동을 수행하도록 연결
- [x] P0: 설정 `취소` 버튼에 변경 전 상태 비교와 확인 모달 연결
- [x] P0: `사용자 검색` 입력에 실제 사용자 검색/선택 목록 연결
- [x] P0: 권한 그룹 추가 버튼에 그룹명, 유형, 기본 권한 템플릿 입력 폼 연결
- [x] P0: 사용자별 권한 상세 행에서 부서, 권한 그룹, 역할, 활성 상태 수정 및 저장 연결
- [x] P0: 부서 관리 탭의 부서 추가, 기본 권한 그룹, 승인 라우팅, 예산 담당자 저장 연결
- [x] P0: 승인 한도 수정 버튼에 금액 범위, 단계, 승인자 수 직접 편집 연결
- [x] P0: 승인 한도 삭제 버튼에 영향 범위 확인 모달 연결
- [x] P1: 변경 이력 `전체` 필터 버튼에 이력 유형/작업자/기간 필터 연결
- [x] P1: 변경 이력 `더보기` 버튼에 추가 이력 로딩 연결
- [x] P1: 외부 연동 상태 버튼에 연동 테스트, 인증 정보 입력, 실패 사유 표시 연결
- [x] P1: 알림 설정 저장 버튼에 테스트 알림 발송 기능 연결

### 22.11 즐겨찾기 화면

- [x] P0: `바로가기 추가` 버튼에 대상 화면, 필터 조건, 이름, 공유 범위 입력 모달 연결
- [x] P0: `순서 편집` 버튼에 드래그 앤 드롭 또는 순서 저장 모드 연결
- [x] P0: 즐겨찾기 `열기` 버튼이 실제 라우트 이동과 연결 필터 적용을 수행하도록 연결
- [x] P0: 즐겨찾기 `삭제` 버튼에 삭제 확인 모달과 undo 기능 연결
- [x] P0: 즐겨찾기 `사용자 저장` 버튼에 백엔드 저장 API 또는 저장 실패 복구 흐름 연결
- [x] P1: 저장된 필터 `전체 보기` 버튼에 필터 전체 목록 연결
- [x] P1: 최근 사용 `전체 보기` 버튼에 최근 사용 전체 이력 연결
- [x] P1: 즐겨찾기 테이블 `선택` 버튼에 행 선택과 상세 패널 포커스 이동 연결
- [x] P1: 연결된 필터 `더보기` 버튼에 전체 필터 조건 표시 연결
- [x] P1: 즐겨찾기 페이지네이션과 행 수 선택 버튼 실제 동작 연결
- [x] P2: 비활성 메뉴 즐겨찾기 클릭 시 대체 경로 또는 관리자 문의 안내 연결

### 22.12 실제 사용성 보강 공통 기준

- [x] P0: 버튼별 권한 부족 상태, 비활성 사유, 처리 불가 사유를 UI에 표시
- [x] P0: 모든 상태 변경 버튼에 중복 클릭 방지와 요청 중 로딩 상태 표시
- [x] P0: 모든 서버 액션 버튼에 성공/실패/부분 실패 결과 구조화
- [x] P0: 모든 업무 변경 버튼에 감사 로그 생성 기준 연결
- [x] P1: 모든 검색/필터/정렬 버튼의 상태가 URL 또는 localStorage에 복원되도록 연결
- [x] P1: 버튼별 키보드 접근, 포커스 이동, Enter/Space 작동 검증
- [x] P1: 버튼 액션별 E2E 테스트 케이스 추가
- 2026-07-08: `src/main.tsx` 하단에 남아 있던 미사용 레거시 `FilterBar`, `DataTable`, `DetailPanel`, `Timeline` 블록을 제거했다. 이 블록은 실제 화면에서 참조되지 않았지만 페이지네이션/승인/반려/보류 버튼이 핸들러 없이 남아 정적 검토에서 미작동 버튼으로 잡히던 부분이다. 홈/로고/로그인 홈 링크도 더미 `href="#"` 대신 `#landing`으로 명시했다.
- 2026-07-08: `ErpApplication` 내부의 `{false && ...}` 레거시 page-head/KPI 블록을 제거하고, 보고서 차트 `CardHeader`의 `월별`, `상위 6개` action 버튼을 원천 데이터 드릴다운 호출에 연결했다.
- [x] P2: 반복 작업 버튼에 최근 실행 조건 재사용 기능 추가

## 23. 데이터 연동성 및 실제 업무 검증 리스트

기준: 버튼 클릭, 화면 메시지, mock/local 상태 변경만으로는 실제 사용 가능으로 보지 않는다. 생성, 수정, 삭제, 상태 변경, 파일 업로드, 권한 변경, 보고서 생성이 API, DB, 파일 저장소, 감사 로그, 권한 정책까지 이어지고 새로고침/재접속 후에도 유지될 때 완료로 판정한다.

현재 검토 결과:

- 프론트 `erpApi`는 remote adapter를 갖고 있지만 기본 모드는 `mock`이며, remote 경로는 모든 화면 리소스(`/approvals`, `/disbursements`, `/budgets`, `/vendors`, `/reports`, `/settings`, `/favorites`)를 호출하도록 되어 있다.
- 백엔드에는 인증, 알림, 결제 요청, 승인, 지급, 예산, 거래처, 보고서, 설정, 즐겨찾기, 파일, 대시보드, 운영 점검, 보고서 예약 job worker route가 등록되어 있다. remote DB E2E, production object storage/scanner 실연동 증빙은 테스트/배포 단계에 남아 있다.
- 결제 요청 작성 중 autosave는 `PaymentRequest` DRAFT row 저장으로 전환했으며, 공통 표 UI 상태 복원은 아직 브라우저 상태 또는 화면 계산 성격이 남아 있다.
- Prisma schema와 주요 route는 연결되었지만 실사용 판정에는 remote DB E2E, staging 배포, 데이터 이관, 운영 백업/모니터링 검증이 추가로 필요하다.

진행 메모:

- 2026-07-05: 결제 요청 `GET detail`, `PATCH`, action route, 승인 `GET list/detail`, `PATCH`, action route, 지급 `GET list/detail`, `PATCH`, action route를 DB/감사 로그 기반 1차 구현으로 추가했다. 예산, 거래처, 보고서, 설정, 즐겨찾기 route와 파일 저장소, 알림 연동, production 인증은 아직 미완료다.
- 2026-07-05: 대시보드, 예산, 거래처, 보고서, 설정, 즐겨찾기 route를 추가해 `src/api/service.ts`의 remote resource path 전체가 backend route에 등록되도록 확장했다. 파일 본문 저장소, signed URL, 바이러스 검사, production 인증/세션, 실데이터 이관은 아직 미완료다.
- 2026-07-05: `/files/presign-upload`, `/files/complete`, `/files/{id}`, `/files/{id}/download`, `/files/{id}/content`, `DELETE /files/{id}` route와 로컬 파일 저장소 어댑터를 추가했다. 이후 S3-compatible storage와 external malware scan adapter, storage/file-security health check까지 연결했으며 실제 production bucket/scanner endpoint 증빙은 배포 단계에서 확인한다.
- 2026-07-05: 결제 요청 증빙과 거래처 증빙 UI를 `erpApi` 파일 메서드에 연결해 presign, 파일 본문 업로드, complete, signed download, delete 흐름을 타도록 수정했다. `npm test`, `npm run build`, `npm --prefix backend run build`, `npm --prefix backend run db:generate`는 로컬 기준 통과했다.
- 2026-07-05: 거래처 화면의 목록 조회, 신규 저장, 수정, 계좌 재확인, 비활성화를 `erpApi` vendor route에 연결했다. 저장 전 거래처 증빙 업로드는 대기 상태로 보관한 뒤 거래처 생성 성공 후 파일 저장소 업로드로 이어지게 했다. 서버 route에는 거래처명/사업자번호 중복 검증과 비활성화 사유 감사 로그 기록을 보강했다. 담당자/세금계산서 수신 정보는 `Vendor` DB 필드와 backend validation으로 확장했으며, remote DB E2E는 아직 미완료다.
- 2026-07-05: 시스템 설정의 권한 그룹 조회/생성/권한 토글/활성 상태 변경을 `/settings/roles` backend role route와 `Role.permissions` JSON에 연결했다. 사용자 권한 추가도 `/settings` 사용자 route를 통해 `UserRole` 배정으로 저장되도록 연결했다.
- 2026-07-05: 결재 정책/승인 한도, 알림 설정, 외부 연동 설정 저장 버튼을 `/settings/config` backend route에 연결해 `AuditLog`의 `system_setting` 스냅샷으로 append-only 저장/최신 조회가 가능하게 했다. 전용 설정 테이블, 알림 adapter 실제 호출, remote DB E2E는 아직 미완료다.
- 2026-07-06: 사용자 권한 탭에 사용자별 상세 권한 테이블을 추가해 부서, 권한 그룹, 역할, 활성 상태를 행 단위로 `/settings` 사용자 route에 저장하도록 보강했다. 부서 관리 탭은 부서 추가, 기본 권한 그룹, 승인 라우팅, 예산 담당자를 `approvalPolicy.departmentSettings` 스냅샷으로 저장한다.
- 2026-07-05: 저장된 `approvalPolicy.approvalLimits` 스냅샷을 신규 결제 요청 제출 시 backend 결재 단계 수 산정에 적용하도록 연결했다. 권한 그룹 삭제 route와 UI 버튼을 추가하고, 사용자가 배정된 그룹은 `ROLE_IN_USE`로 삭제를 차단하며 사용자 미배정 그룹만 삭제 감사 로그를 남기도록 했다.
- 2026-07-05: 외부 연동 설정에 `credentialRef`, HTTPS 테스트 endpoint, 실패 사유, 마지막 테스트 시각을 추가하고 `/settings/integrations/{integrationId}/test` route를 연결했다. 원문 secret은 저장하지 않고 서버 환경변수 참조만 저장하며, 테스트 결과는 `integrations` 설정 스냅샷과 감사 로그에 반영한다.
- 2026-07-05: 결제 요청, 승인, 지급, 파일, 거래처, 설정 권한 그룹/사용자 권한 등 민감 backend route에 `requireAuth`와 `FORBIDDEN` 권한 가드가 유지되는지 확인하는 직접 API 권한 회귀 테스트를 추가했다.
- 2026-07-05: 결제 요청 상세 폼의 거래처 선택 목록을 `/vendors` 조회 결과의 활성 거래처 기준으로, 부서/예산 잔액을 `/budgets` 조회 결과와 기존 부서 후보 기준으로 표시하도록 연결했다. 저장/제출 전 활성 거래처와 부서 후보 불일치도 화면 validation에 추가했다. 거래처 생성 후 결제 요청 폼은 master data를 재조회하고 최근 생성 거래처를 자동 재선택한다. remote DB E2E는 아직 미완료다.
- 2026-07-05: 결제 요청 전용 `/payment-requests/master-data` API를 추가해 활성 거래처, 부서별 최신 예산, 예산 항목, 승인 권한 보유자를 한 번에 조회하도록 연결했다. 결제 요청 폼은 이 master data로 거래처/부서/예산 항목 select와 예상 결재선을 구성하고, 저장/제출 patch에 `예산항목ID`를 포함한다. 또한 remote mode의 `POST /payment-requests`를 추가하고 제출 상태 전환 시 `ApprovalStep`과 승인 요청 알림을 생성하도록 보강했다. 이후 저장/제출 payload의 첨부 ID를 같은 트랜잭션 안에서 `Attachment` metadata와 재확정하도록 보강했다. remote DB E2E는 아직 미완료다.
- 2026-07-05: `/files?ownerType&ownerId` 첨부 목록 API를 추가하고 결제 요청 패널의 가짜 기본 첨부 표시를 제거했다. 결제 요청 제출은 backend에서 실제 업로드 완료/차단되지 않은 `Attachment`가 1개 이상 없으면 거절하도록 보강했고, E2E smoke도 실제 PDF 증빙 업로드 후 제출하도록 수정했다. production object storage/바이러스 검사/remote DB E2E는 계속 미완료다.
- 2026-07-05: 거래처 상세의 mock 기본 문서 표시를 제거하고 선택 거래처별 `/files?ownerType=VENDOR&ownerId=...` 재조회로 backend `Attachment` metadata 기준 문서 목록을 표시하도록 연결했다. 거래처 증빙 E2E는 저장 후 다운로드와 삭제 버튼까지 smoke에 포함했다. production object storage, 악성코드 검사 엔진, remote DB E2E, staging 배포 검증은 아직 미완료다.
- 2026-07-05: 파일 저장소를 local/S3-compatible adapter로 분리하고 `FILE_STORAGE_DRIVER=s3`에서 S3 REST 서명 기반 object storage PUT/GET/HEAD/DELETE를 사용하도록 보강했다. 업로드 전 malware scan adapter를 추가해 local EICAR 차단과 external scanner endpoint 연동을 지원하며, `/api/health/storage`, `/api/health/file-security`를 추가했다. 실제 production bucket, scanner endpoint, bucket policy, versioning, quarantine 운영 검증은 아직 미완료다.
- 2026-07-05: 즐겨찾기 화면의 조회, 바로가기 추가, 순서 편집, 삭제, undo 복구, 사용자 저장, 열기 사용 기록을 `FavoriteItem` backend API에 연결했다. backend row에는 `sortOrder`, `filters`, 공유 범위, 대상 화면을 포함하고, `frontendFavoritesRemote` 회귀 테스트로 `erp-favorites:{userId}` localStorage 경로가 되살아나지 않도록 고정했다.
- 2026-07-05: `docs/button-action-map.md`를 추가해 화면별 주요 버튼이 어떤 `erpApi` 메서드와 backend route, DB/file/audit 원천으로 연결되는지 정리하고, 아직 local/client-only 성격이 남은 항목을 별도 표시했다.
- 2026-07-05: 보고서 화면의 목록 조회와 생성 버튼을 `ReportRun` backend API에 연결하고, CSV/PDF 다운로드를 `GET /reports/{name}/download?format=csv|pdf` 서버 생성 파일 payload로 전환했다. 다운로드는 `report_run` 감사 로그에 `download_csv`/`download_pdf`로 기록하며, `frontendReportDownloads` 회귀 테스트로 client Blob export 회귀를 차단한다. 이후 2026-07-08에 보고서 생성/예약 job 성공 run의 CSV/PDF payload를 `reports/{reportRunId}.artifact.json` object storage artifact로 저장하고 `ReportRun.artifactKey`에 연결하도록 보강했으며, 기존 run은 최초 다운로드 시 자동 보관한다. production bucket versioning/backup restore 리허설 증빙은 운영 배포 검토 단계에서 확인한다.
- 2026-07-05: 보고서 예약 발송 목록/추가/수정/중지를 `ReportDefinition`/`ReportSchedule` backend API에 연결했다. 예약 변경은 감사 로그와 내부 알림을 같은 트랜잭션에 남기며, `frontendReportSchedules` 회귀 테스트로 브라우저 tuple state 경로가 되살아나지 않도록 고정했다. 이후 report job worker가 internal/webhook delivery, retry, dead-letter, circuit breaker를 처리하도록 확장되어 기능 공백은 해소됐고, 실제 staging/prod webhook endpoint 발송 리허설만 운영 증적으로 남긴다.
- 2026-07-06: 보고서 저장 목록의 더보기 메뉴를 이름 변경, 공유 권한 저장, 삭제 API 호출로 확장했다. backend는 `ReportRun.summary` 메타에서 `공유권한`을 읽고 갱신해 목록 재조회 후에도 권한 표시가 유지된다. 저장된 보고서 별표 버튼은 `FavoriteItem` backend API로 보고서 즐겨찾기를 조회, 생성, 재활성화, 비활성화한다. 차트 클릭 시 월별 지급/부서 지출/승인 상태 원천 행 테이블도 화면에 렌더링하도록 보강했다. 아직 같은 backend `ReportRun` snapshot ID 기준으로 drilldown query를 재조회하는 단계는 아니므로 해당 P1은 open으로 유지한다.
- 2026-07-05: 승인/반려/보류 처리는 pending `ApprovalStep`만 처리하도록 잠그고, `PaymentRequest` 상태, 감사 로그, requester/다음 승인자 알림을 같은 트랜잭션에서 갱신하도록 보강했다. 완료/반려/보류된 승인 건 재처리는 `WORKFLOW_LOCKED`로 차단한다. 대시보드 KPI, 긴급 결재, 최근 결제 요청은 `/dashboard` API row 기준으로 계산하고 최근 활동은 알림 센터 데이터와 연결했다. remote DB E2E와 staging UAT는 아직 미완료다.
- 2026-07-08: 대시보드 승인 추이와 부서별 지출 차트를 고정 배열이 아니라 `/dashboard` API row의 요청일, 결재상태, 부서, 금액으로 계산하도록 전환했다. 기능 구현 범위의 정적 chart fixture 의존은 제거했고, 실제 staging/prod 데이터 규모의 집계 정확도 증적은 테스트 단계에서 확인한다.

우선순위:

- P0: 실제 업무 사용 전 반드시 막아야 하는 데이터 손실/권한/파일/상태 변경 이슈
- P1: 운영 품질, 장애 복구, 반복 사용성을 보장하기 위한 연동 이슈
- P2: 고급 편의, 분석, 성능, 운영 개선 이슈

### 23.1 공통 데이터 계층

- [x] P0: 모든 화면 CRUD와 업무 액션이 `erpApi` remote mode 또는 실제 backend route로 연결되는지 버튼별 매핑표 작성
- [x] P0: mock API, React local state, `localStorage`만 변경하는 버튼 목록을 별도 표시하고 실제 저장소 연결 전 완료 처리 금지
- [ ] P0: 생성/수정/삭제/상태 변경 후 새로고침, 재로그인, 다른 브라우저 접속에서도 데이터가 유지되는지 검증
  - 진행 메모(2026-07-06): `tests/e2e/remote-ui-persistence.test.mjs`에 결제 요청 생성, 증빙 업로드, 제출, 승인자 2명 순차 승인, 두 번째 관리자 브라우저 재조회, 지급 보류, 새로고침 후 보류 상태 유지, Prisma `PaymentRequest`/`ApprovalStep`/`Attachment`/`Disbursement`/감사 로그 대사를 추가했다. `scripts/verify-db-test-evidence.mjs`는 이 payment workflow remote UI E2E가 없으면 실패한다. 단, 현재 로컬에는 `ERP_TEST_DATABASE_URL`이 없어 실제 DB 기반 실행 증적은 없으므로 P0는 계속 미완료로 유지한다.
- [x] P0: 서버 실패, 권한 실패, 부분 실패, 중복 요청 실패가 UI 메시지와 감사/보안 로그에 같은 requestId 기준으로 반영되는지 검증
  - 진행 메모(2026-07-06): `ApiErrorCode`에 `IDEMPOTENCY_CONFLICT`, `PARTIAL_FAILURE`, 파일/scan/rate limit 오류 코드를 맞추고, `securityEventTypeForFailure`가 `FORBIDDEN -> access_denied`, `IDEMPOTENCY_CONFLICT -> duplicate_request_blocked`, `PARTIAL_FAILURE -> partial_failure`, `SERVER_ERROR -> server_failure`로 분류하도록 고정했다. `failureCorrelation.test.ts`는 같은 실패 응답이 프론트 `ApiRequestError` 메시지와 backend `security_events`에서 동일 `requestId`/error code/path로 대사되는지 검증한다.
- [x] P0: 모든 mutation 요청에 idempotencyKey, rowVersion 또는 동시성 충돌 방지 기준 적용
  - 진행 메모(2026-07-06): 결제 요청 생성/임시저장/제출 수정, 예산 생성/직접수정, 보고서 생성/수정/삭제/예약 발송, 즐겨찾기 생성/수정/삭제, 파일 presign/complete/delete, 시스템 설정 스냅샷 저장에 `idempotencyKey`, `rowVersion` alias, 최신 `AuditLog.id` 기대값 또는 예외 기준, 조건부 backend update 및 회귀 테스트를 추가했다. 알림/운영성 route 예외 기준의 staging 증적과 staging 동시성 smoke가 남아 있어 전체 완료로 보지 않는다.
- [ ] P1: 목록 검색, 필터, 정렬, 페이지네이션을 서버 쿼리 파라미터와 동기화하고 DB 결과와 화면 결과 일치 검증
  - 진행 메모(2026-07-08): 공통 `useManagedTable`은 이미 `search`, `filter.*`, `sort`, `page`, `pageSize`를 서버 query로 보내고 있었고, 별도 구현이던 보고서 목록도 `/reports` 서버 query 기준 검색/유형/부서/거래처/정렬/pagination으로 전환했다. 보고서 생성 시 부서/거래처 scope를 `ReportRun.summary` metadata에 보존해 backend row 필터와 다운로드 artifact가 같은 run 기준을 쓰도록 했으며, 예산/거래처 목록은 API 빈 결과 또는 실패를 로컬 기본 데이터로 덮어쓰는 fallback을 제거했다. 실제 DB 대량 데이터에서 화면 total/page와 SQL 결과 일치 증빙은 테스트 단계에서 확인한다.
- [x] P1: optimistic update 적용 화면은 서버 원본 불일치 시 rollback 또는 재조회 기준 구현
  - 진행 메모(2026-07-07): `useManagedTable` 공통 mutation 처리에 요청 직전 rows/선택/total 스냅샷을 추가하고, 단건 수정/일괄 수정/action/create 실패 시 스냅샷 원복 후 `listPageRows` 재조회로 서버 원본에 수렴하도록 보강했다. 서버가 성공 응답에서 갱신 행을 반환하지 않는 경우도 임시 병합 대신 목록 재조회 기준을 사용한다. 실제 stale rowVersion 충돌과 다른 브라우저 변경 증빙은 최종 테스트 단계에서 확인해야 한다.
- [ ] P2: 화면별 캐시 무효화, 재검증, stale data 표시 정책 정의

### 23.2 파일 업로드/다운로드

- [x] P0: 결제 요청 증빙과 거래처 사업자등록증/통장사본/세금계산서 파일을 실제 업로드 API와 파일 저장소에 저장
- [x] P0: 첨부 다운로드가 브라우저에서 생성한 텍스트 `Blob`이 아니라 저장소 원본 파일 또는 signed URL을 내려받는지 검증
- [x] P0: 파일 삭제가 저장소 객체, DB metadata, 감사 로그에 모두 반영되는지 검증
- [x] P0: 파일 presign/complete/delete 중복 요청이 idempotencyKey 기준 replay 또는 conflict로 처리되는지 검증
- [x] P0: 파일 권한, 확장자/용량 제한, 바이러스 검사, 미리보기 가능 여부를 backend 상태와 연결
- [x] P0: 결제 요청 제출 시 화면 가짜 첨부가 아니라 backend `Attachment` 기준 업로드 완료 파일이 1개 이상인지 검증
- [x] P1: 업로드 진행률, 실패 재시도, 대용량 파일, 중복 파일명, 업로드 중 화면 이탈 복구 처리
  - 진행 메모(2026-07-07): `uploadFileContent`에 실제 업로드 progress callback을 추가하고, 결제 요청/거래처 증빙 row에 진행률 표시, 실패 파일 재업로드 버튼, 세션 내 원본 `File` 보관, 화면 이탈 후 미완료 upload recovery metadata 표시, 같은 선택 묶음의 중복 파일명 안내를 구성했다. 대용량/실제 중단 복구 증빙은 테스트 단계에서 확인해야 한다.
- [ ] P2: PDF/이미지 미리보기, 다운로드 만료, 접근 로그 조회 기능 검증

### 23.3 결제 요청 데이터 연동

- [x] P0: 거래처 추가 버튼이 실제 거래처 생성 API, 중복 사업자번호 검증, 생성 후 결제 요청 폼 재선택까지 연결
- [x] P0: 결제 요청 임시 저장/제출이 DB의 `PaymentRequest`, 결재선, 예산 항목, 첨부 파일 metadata를 하나의 트랜잭션 기준으로 저장
- [x] P0: 결제 요청 폼의 거래처, 부서, 예산 항목, 결재선 후보가 mock 배열이 아니라 backend master data에서 조회되도록 구현
- [x] P0: 위 결제 요청 master data와 생성/제출/승인 단계 생성 흐름을 remote DB E2E로 검증
- [x] P0: 제출 후 승인 관리 목록, 알림 센터, 대시보드 KPI가 같은 원천 데이터로 갱신되는지 검증
- [x] P1: autosave 복구 목록을 `localStorage`가 아니라 사용자별 서버 임시 저장 데이터와 연결
  - 진행 메모(2026-07-07): 결제 요청 작성 변경분을 1.2초 debounce 후 기존 `PaymentRequest` DRAFT row에 `PATCH /payment-requests/{requestCode}`로 자동 저장하도록 전환하고, autosave 성공 시 최신 `rowVersion`을 패널에서 보관해 수동 임시 저장/제출과 충돌하지 않게 했다. 서버 자동 저장 실패 시에만 localStorage fallback을 남긴다. 다른 브라우저/재로그인 복구 증빙은 테스트 단계에서 확인해야 한다.
- [x] P1: 예산 초과, 비활성 거래처, 첨부 누락, 결재선 없음 같은 서버 validation 결과를 필드별로 표시
  - 진행 메모(2026-07-07): 결제 요청 제출/임시 저장 전 클라이언트 validation을 거래처, 부서, 요청일, 예산, 금액, 증빙, 사유, 결재선 필드 오류로 분리해 표시하고, backend 실패 메시지도 주요 키워드 기준으로 필드 오류에 매핑하도록 구성했다. 실제 서버 validation 코드별 UI 증빙은 테스트 단계에서 확인해야 한다.

### 23.4 승인 관리 데이터 연동

- [x] P0: 승인/반려/보류/일괄 승인 버튼이 DB의 `ApprovalStep`, `PaymentRequest` 상태, 감사 로그, 알림을 트랜잭션으로 갱신
- [x] P0: 현재 사용자 권한과 결재 순서에 맞지 않는 승인 버튼은 서버에서도 거절되는지 검증
- [x] P0: 처리 후 다음 건 자동 선택이 서버 최신 목록 기준으로 동작하고 이미 처리된 건 중복 처리를 막는지 검증
- [x] P1: 결재선 단계별 이력, 사유, 처리자, 처리 시간을 DB 기준으로 조회
  - 진행 메모(2026-07-07): `/approvals`와 `/approvals/{requestCode}` row에 `ApprovalStep` 기반 `결재단계JSON`, `처리 이력`, `처리 사유`, `처리시간`, `요청 사유`를 포함하고, 승인 상세 패널은 이 JSON을 우선 파싱해 단계별 처리자, 상태, 사유, 처리시간을 표시하도록 연결했다. 실제 remote DB 승인/반려/보류 후 새로고침 재조회 증빙은 테스트 단계에서 확인해야 한다.
- [x] P1: 일괄 승인 일부 실패 시 성공/실패 건별 결과와 rollback 기준 명확화
  - 진행 메모(2026-07-07): 공용 테이블 일괄 update를 `Promise.allSettled` 기준으로 바꿔 건별 성공/실패를 집계하고, 성공 건은 서버 응답 row로 반영한 뒤 재조회하며 실패 건은 선택 상태로 유지해 재처리할 수 있게 했다. 승인 일괄 버튼 메시지는 권한/순서/상태 미충족으로 제외된 건과 서버 처리 실패 건을 구분하도록 정리했다. 실제 remote DB에서 일부 성공/일부 실패 시 감사 로그와 rowVersion 정합성은 테스트 단계에서 확인해야 한다.

### 23.5 지급 관리 데이터 연동

- [x] P0: 지급 실행이 실제 `Disbursement` 상태, `rowVersion`, `executedAt`, 알림, 감사 로그를 트랜잭션 기준으로 갱신
- [x] P0: 지급 보류/재처리/계좌 재확인이 실제 `Disbursement` 상태와 계좌 검증 결과를 갱신
- [x] P0: 은행 이체 파일 다운로드가 현재 DB 지급 대상과 승인 상태를 기준으로 생성되는지 검증
- [x] P0: 지급 실행 결과가 결제 요청 상태, 거래처 지급 이력, 은행 결과 대사까지 같은 원천 데이터로 연결되는지 검증
- [x] P1: 계좌 검증 API adapter, 실패 사유 코드, 재처리 가능/불가 정책 연결
  - 진행 메모(2026-07-07): `Disbursement` row에 `계좌검증Adapter`, `계좌검증코드`, `계좌검증사유`, `계좌검증재시도`, `거래처계좌확인`, `재처리가능`, `재처리차단코드`, `재처리정책`을 추가하고, 지급 실행/2인 확인/재처리 버튼은 지급 건 계좌와 거래처 계좌 상태를 함께 보는 정책 헬퍼로 활성화하도록 연결했다. 이후 `backend/src/integrations/bankAccountVerification.ts`를 추가해 `BANK_ACCOUNT_VERIFICATION_ENDPOINT`/`ERP_BANK_API_TOKEN` 기반 외부 은행 계좌 검증 adapter를 붙였고, 계좌 재확인은 외부 검증이 `VERIFIED`를 반환해야 `Disbursement`/`Vendor` 계좌 상태를 확인 완료로 갱신한다. 실제 은행 sandbox endpoint 응답 증빙은 테스트/연동 단계에서 확인해야 한다.
- [x] P1: 지급 예정일 업무일 계산과 휴일/마감 시간 정책을 backend에서 동일하게 적용
  - 진행 메모(2026-07-07): backend에 은행 영업일 정책(`ERP_BANK_HOLIDAYS` 확장 가능), 주말/휴일 제외, KST 16:00 이후 당일 변경 불가 검증을 추가하고, `지급예정일업무일`, `지급일정정책`, `다음지급가능일`, `지급일정경고` row 필드를 내려 화면 지급 일정 카드에 표시하도록 연결했다. 실제 운영 휴일 캘린더/마감 시간 UAT와 remote reschedule 검증은 테스트 단계에서 확인해야 한다.

### 23.6 예산 관리 데이터 연동

- [x] P0: 예산 조정 버튼이 DB 예산 조정 요청, 승인 필요 여부, 조정 이력을 실제 저장
- [x] P0: 최종 승인 시 `BudgetItem`과 상위 `Budget` 사용액/상태를 승인 트랜잭션에서 갱신
- [x] P0: 결제 요청 제출/승인/지급 상태 변경에 따라 예산 사용액과 잔액이 재계산되는지 검증
- [x] P0: 부서/항목/기간 필터 결과가 DB 예산 원장과 일치하는지 검증
- [x] P1: 예산 초과 경고와 알림을 backend rule 기준으로 생성
  - 진행 메모(2026-07-07): 최종 승인 처리 시 `applyBudgetUsageOnFinalApproval`이 `BudgetItem`과 상위 `Budget` 사용액을 증가시키고, 사용률 기준 `WARNING`/`EXCEEDED` 상태 상승이 발생하면 `BUDGET_EXCEEDED` 알림을 권한 있는 담당자에게 생성하도록 연결했다. 같은 예산/상태/담당자 조합의 중복 알림은 기존 알림 조회로 방지한다. 실제 알림 수신 UI와 원장 대사는 테스트 단계에서 확인한다.
  - 진행 메모(2026-07-07): 최종 승인 처리에서 `BudgetItem`/`Budget` 사용액을 반영한 뒤 backend `budgetStatusFor` 기준으로 `NORMAL -> WARNING`, `WARNING/NORMAL -> EXCEEDED` 상태 전이가 발생하면 `budget:read` 또는 `system:manage` 권한 사용자에게 `BUDGET_EXCEEDED` 알림을 생성하도록 구성했다. 같은 예산/상태 알림은 notification `entityId` 기준으로 중복 생성하지 않는다. 실제 승인 플로우 알림 증빙은 테스트 단계에서 확인해야 한다.
- [x] P1: 예산 조정 취소/반려 시 원장 rollback 또는 보정 전표 기준 정의
  - 진행 메모(2026-07-07): pending `BudgetAdjustment`는 `POST /budgets/{department}/adjustments/{id}/cancel|reject`로 취소/반려 처리하고, 이 경우 원장 미반영 상태 종료라 rollback이 없다는 `rollbackPolicy`와 감사 로그를 남긴다. 이미 `APPLIED`된 조정은 취소/반려를 차단하고 반대 조정 또는 보정 전표가 필요하다는 기준을 row `원장반영방식`과 화면 이력 카드에 표시했다. 실제 remote DB 상태 변경/감사 로그 증빙은 테스트 단계에서 확인해야 한다.

### 23.7 거래처 관리 데이터 연동

- [x] P0: 거래처 신규 등록/수정/비활성화가 `Vendor` DB와 감사 로그에 저장되고 새로고침 후 유지되는지 검증
- [x] P0: 거래처 수정/비활성화/계좌확인에 `거래처RowVersion`, `idempotencyKey`, 조건부 backend update 적용
- [x] P0: 사업자번호, 계좌번호, 세금계산서 수신 정보 검증을 backend validation/API adapter와 연결
- [x] P0: 거래처 문서 업로드/삭제/다운로드를 파일 저장소와 DB metadata에 연결
- [x] P0: 비활성화 시 진행 중 결제 요청과 지급 예약 영향 범위를 서버에서 계산
- [x] P1: 거래처 지급 이력 탭을 실제 `Disbursement` 및 `PaymentRequest` 데이터와 연결
  - 진행 메모(2026-07-06): 거래처 상세 패널이 `disbursement`와 `payment-request` API row를 함께 불러와 최근 지급/요청 및 전체 이력 탭을 표시하도록 전환했다. 지급 row에는 요청 부서도 포함되도록 backend `Disbursement` 응답을 보강했다.
- [x] P1: 거래처 목록 페이지네이션/검색/필터를 서버 쿼리로 처리
  - 진행 메모(2026-07-07): 거래처 목록 조회가 `search`, `filter.상태`, `filter.계좌확인`, `filter.구분`, `page`, `pageSize`, `sort`를 `/vendors` query로 전송하고, 테이블 페이지/전체 건수를 backend pagination 결과와 동기화하도록 전환했다.

### 23.8 보고서 데이터 연동

- [x] P0: 보고서 생성 버튼이 backend report job 또는 query service를 호출하고 `ReportRun` 결과를 저장
- [x] P0: 보고서 다운로드가 화면 mock 데이터가 아니라 저장된 report artifact 또는 서버 생성 파일을 내려받는지 검증
- [x] P0: 예약 발송 추가/수정/중지가 `ReportSchedule` DB와 실제 발송 queue/notification에 연결
- [x] P0: 보고서 생성/수정/삭제와 예약 발송 변경에 `보고서RowVersion`/`예약RowVersion`, `idempotencyKey`, 조건부 backend update 적용
- [x] P1: 저장된 보고서 즐겨찾기, 이름 변경, 삭제, 권한 변경을 서버 권한과 영속 데이터로 처리
- [x] P1: 차트와 원천 데이터 드릴다운이 같은 report run 기준으로 조회되는지 검증
  - 진행 메모(2026-07-07): 보고서 차트 드릴다운은 선택된 `ReportRun.summary`의 `드릴다운JSON` snapshot section을 우선 파싱해 월별 지급, 부서 지출, 승인 상태 원천 행을 표시하도록 연결했다. snapshot이 없는 로컬/mock 행은 fallback 데이터로만 표시되며, 재무 대사는 `ReportRun.summary` snapshot과 현재 원장을 별도 비교한다. 실제 remote 보고서 생성 후 같은 run 기준 드릴다운 재조회 증빙은 테스트 단계에서 확인한다.
  - 진행 메모(2026-07-07): ReportRun 생성 시 보고서 기간/부서/거래처 필터 기준으로 지급, 부서 지출, 승인 상태 원천 row snapshot을 만들고 `summary` metadata에 `드릴다운` payload로 저장한 뒤 `/reports` row의 `드릴다운JSON`으로 내려주도록 구성했다. 보고서 차트 클릭은 현재 선택된 보고서의 `드릴다운JSON`을 우선 파싱해 원천 테이블을 열고, snapshot이 없는 기존/mock 보고서만 기존 fallback row를 사용한다. 실제 remote DB에서 생성한 ReportRun과 차트 드릴다운 row 일치 증빙은 테스트 단계에서 확인해야 한다.

### 23.9 시스템 설정 및 권한 연동

- [x] P0: 결재 정책, 승인 한도, 권한 그룹, 사용자 권한, 알림 설정, 외부 연동 설정을 DB에 저장하는 backend route 구현
- [x] P0: 설정 변경 후 신규 결제 요청/승인/지급 화면의 서버 권한 판정에 즉시 반영되는지 검증
- [x] P0: 권한 그룹 추가/삭제/사용자 배정이 UI 표시뿐 아니라 인증 세션 권한과 backend permission check에 반영
- [x] P0: 권한 그룹 생성/수정/삭제와 사용자 권한 변경에 `rowVersion`, `idempotencyKey`, 조건부 backend update/delete 적용
- [x] P0: 결재 정책/알림/외부 연동 설정 스냅샷 저장에 `idempotencyKey`와 최신 `AuditLog.id` 기대값을 적용해 중복 저장 replay와 stale save conflict 처리
- [x] P0: 외부 연동 테스트 버튼이 실제 credential 저장, 테스트 호출, 실패 사유, 마지막 동기화 시각과 연결
- [x] P1: 설정 취소/저장 전 변경 비교가 서버 원본 기준으로 동작
  - 진행 메모(2026-07-06): 설정 화면 진입 시 backend에서 불러온 권한/사용자/정책/부서/알림/연동 상태를 서버 원본 snapshot으로 보관하고, 저장 성공 시 snapshot을 갱신한다. `취소 > 되돌리기`는 로컬 초기값이 아니라 마지막 backend 원본으로 복원한다.
- [x] P1: 설정 변경 이력 전체 필터와 더보기가 DB 감사 로그 기준으로 동작
  - 진행 메모(2026-07-06): `/settings/history` route를 추가해 시스템 설정, 권한 그룹, 사용자 권한 감사 로그를 화면 이력 row로 내려주고, 설정 화면의 이력 필터/더보기 원천을 서버 `AuditLog` 조회 결과로 전환했다.

### 23.10 즐겨찾기 및 사용자 설정 연동

- [x] P0: 바로가기 추가/삭제/순서 편집/사용자 저장을 `FavoriteItem` API와 DB에 연결
- [x] P0: 즐겨찾기 열기 시 저장된 라우트, 필터, 정렬 조건이 서버 저장값 기준으로 재현되는지 검증
- [x] P0: 즐겨찾기 생성/수정/삭제/복구/사용 기록 저장에 `즐겨찾기RowVersion`, `idempotencyKey`, 조건부 backend update 적용
- [x] P1: 최근 사용 목록을 사용자별 서버 이벤트 또는 감사 로그 기준으로 갱신
  - 진행 메모(2026-07-07): 즐겨찾기 `열기` 동작만 `FavoriteItem.lastUsedAt`을 갱신하도록 backend update 기준을 분리하고, 최근 사용 테이블은 서버 `최근사용` 시각 기준 내림차순으로 정렬한다. 일반 순서 편집/저장은 최근 사용 시각을 임의 갱신하지 않는다.
- [x] P1: 다른 브라우저/기기에서 같은 사용자 즐겨찾기와 저장 필터가 동기화되는지 검증
  - 진행 메모(2026-07-07): 즐겨찾기와 저장 필터는 `FavoriteItem` API의 `list/create/update/delete` 원천을 사용하고, route state/filter/sort/shared metadata를 서버 row로 저장하도록 연결했다. localStorage는 화면 이동 편의 상태에만 사용하며, 같은 사용자 재조회 시 backend FavoriteItem 목록을 다시 동기화한다. 실제 다른 브라우저 재로그인 증빙은 테스트 단계에서 확인한다.
  - 진행 메모(2026-07-07): 즐겨찾기 툴바에 `동기화` 버튼을 추가하고, 화면 포커스 복귀/visibility 복귀 시 `GET /favorites`를 다시 호출해 같은 사용자 `FavoriteItem`과 저장 필터를 backend 기준으로 재동기화하도록 구성했다. 테스트 단계에서 두 브라우저 또는 탭 간 생성/수정/삭제 반영 증빙을 확인해야 한다.
- [ ] P2: 비활성 메뉴, 권한 회수, 삭제된 필터 참조 시 대체 경로 처리

### 23.11 대시보드 및 알림 연동

- [x] P0: 대시보드 KPI, 긴급 결재, 최근 활동, 최근 결제 요청이 정적 mock 배열이 아니라 backend 집계/API에서 계산되는지 검증
- [x] P0: 업무 액션 후 알림 센터 읽음/미읽음, 연결 링크, 만료 처리가 DB와 일치하는지 검증
- [x] P1: KPI 카드 클릭 시 이동한 화면의 서버 필터 결과와 KPI 산식이 일치하는지 검증
  - 진행 메모(2026-07-07): 대시보드 KPI 클릭 시 `erp-favorite-route-state:{page}`와 `erp-table-state:{page}`에 KPI 산식과 동일한 필터/정렬을 저장하고 대상 화면이 해당 extra filter를 `listPageRows` 서버 query로 전송하도록 구성했다. `승인 대기`/`오늘 마감`은 `결재상태__in`, `처리기한__lte` 비교 필터를 사용하며, backend/mock row filter가 `__in`, `__lte`, `__gte` 연산자를 처리하도록 보강했다. 브라우저 클릭 증빙은 테스트 단계에서 확인해야 한다.
- [x] P1: 감사 로그와 최근 활동의 원천을 하나로 통일하고 사용자 권한에 맞게 마스킹
  - 진행 메모(2026-07-07): 대시보드 최근 활동은 `AuditLog`와 사용자 `Notification`을 함께 조회해 시간순으로 병합하고, 관리자/본인 외 항목은 actor/entity 상세를 권한 범위 내 사용자와 entity type 수준으로 마스킹한다. 설정 변경 이력은 `AuditLog` 기반으로 정책/권한/사용자/알림/연동/운영 변경 태그를 분류한다. 실제 역할별 마스킹 UI 증빙은 테스트 단계에서 확인한다.
  - 진행 메모(2026-07-07): `/dashboard` row에 `AuditLog`와 사용자 `Notification`을 생성시각 기준으로 합친 `최근활동JSON` snapshot을 포함하고, 일반 사용자는 감사 로그 actor/entity 상세를 마스킹하며 `system:manage` 또는 본인 actor 로그만 상세를 볼 수 있게 했다. 대시보드 최근 활동 카드는 이 backend snapshot을 우선 사용하고 없을 때만 notification fallback을 사용한다. 실제 역할별 마스킹/표시 증빙은 테스트 단계에서 확인해야 한다.

### 23.12 Backend/API/DB 완성도

- [x] P0: `src/api/service.ts`의 remote resource path 전체에 대응하는 backend route 구현 여부 확인
- [x] P0: 승인, 지급, 예산, 거래처, 보고서, 설정, 즐겨찾기 mutation API 명세와 route 구현
- [x] P0: 핵심 Prisma 모델의 schema/seed/backend delegate/frontend service coverage guard 추가
- [x] P0: Prisma schema의 모든 핵심 모델에 seed data, route, service, integration test 연결
- [ ] P0: frontend remote mode E2E를 실제 backend test server와 test DB로 실행
  - 진행 메모(2026-07-06): remote UI E2E 하네스 범위를 거래처/파일, 즐겨찾기/보고서/설정, 결제 요청/승인 handoff/지급 보류까지 확장했다. `node --test tests/e2e/remote-ui-persistence.test.mjs`는 test DB가 없는 현재 환경에서 3건 skip을 명시했고, strict release evidence mode는 `ERP_TEST_DATABASE_URL`뿐 아니라 `release/db-test-evidence.json`의 실제 실행 결과, 하네스 checksum, skip 없는 통과 상태까지 요구한다. 폐기 가능한 PostgreSQL test DB에서 실제 실행한 결과가 아직 없으므로 완료 처리는 보류한다.
- [x] P0: remote mode 로그인 화면과 backend/test DB 기반 browser auth E2E 하네스 추가
- [x] P1: API 응답 DTO와 화면 table row 매핑을 문서화하고 상태값/ID 불일치 제거
  - 진행 메모(2026-07-08): `docs/screen-data-mapping.md`를 실제 화면 route key와 숨김 ID/version 필드 기준으로 갱신했다. 지급 row에는 `지급RowVersion`, 예산/거래처/사용자 설정 row에는 공통 `rowVersion` alias를 추가해 화면별 alias와 공통 mutation guard가 같은 값을 참조하게 했다. 보고서 row는 `부서`, `거래처`, `드릴다운JSON`, artifact metadata 기준을 문서화했다. 실제 DB 응답과 화면 row 상태값/ID 대사는 테스트 단계에서 확인한다.
- [x] P1: 감사 로그, 알림, 파일 metadata, report artifact의 불변성/보관 기간 정책 구현
  - 진행 메모(2026-07-07): `backend/src/domain/retentionPolicy.ts`에 감사 로그 7년 append-only/물리 삭제 금지, 알림 90일 만료/정리 가능, 첨부 metadata 7년 보관/삭제 예외 감사, 보고서 산출물 3년 보관 후 `EXPIRED` 전환 정책을 정의하고 `/operations/retention-policy`에서 정책/점검/대상 카운트를 조회하도록 연결했다. 설정 화면에 `보관 정책` 탭을 추가해 운영자가 정책 버전, 불변 여부, 삭제 정책, 조치 대상 수를 확인하고 새로고침할 수 있게 했으며, 첨부 삭제 감사 로그에는 보관 정책/예외 사유를 함께 남긴다. 실제 오래된 데이터 기준 cleanup/archive 실행 증빙은 테스트/운영 리허설 단계에서 확인해야 한다.
- [ ] P2: migration, backup/restore, data retention, 장애 복구 리허설

### 23.13 데이터 연동 검증 자동화

- [x] P0: 화면별 생성/수정/삭제/상태 변경 후 `새로고침 후 유지` E2E 테스트 추가
  - 증거: `tests/e2e/remote-ui-persistence.test.mjs`가 remote mode 브라우저에서 거래처 등록/증빙 업로드, 즐겨찾기 추가, 보고서 생성, 설정 권한 그룹 추가, 결제 요청 생성/증빙 업로드/제출, 승인자 순차 승인, 지급 보류를 수행하고 새로고침 및 두 번째 브라우저 로그인 후 화면 유지와 Prisma DB/file/audit 대사를 확인하도록 확장했다. `scripts/verify-db-test-evidence.mjs`는 screen-level persistence와 payment workflow E2E harness가 없으면 release evidence gate를 실패시킨다. 실제 `ERP_TEST_DATABASE_URL` 기반 실행은 23.12 P0로 계속 남긴다.
- [x] P0: UI 액션 후 DB 상태를 직접 검증하는 integration test 추가
- [x] P0: remote browser auth E2E가 실제 backend server, test DB 사용자, 새로고침 세션 유지, 로그아웃을 검증하도록 준비
- [x] P0: backend app factory와 `app.inject` 기반 integration test 준비 구조 추가
- [x] P0: 거래처 생성 API의 test DB persistence, 세션 refresh, 새 로그인 재조회, Prisma 직접 DB/audit 검증 integration test 추가
- [x] P0: 설정 권한 그룹/사용자 권한/시스템 설정 스냅샷 API의 test DB persistence, 세션 refresh, 새 로그인 재조회, Prisma 직접 DB/audit 검증 integration test 추가
- [x] P0: 결제 요청 master data, draft 생성, 파일 업로드, 제출, 승인 단계 생성의 test DB persistence와 Prisma 직접 검증 integration test 추가
- [x] P0: 알림 읽음 멱등성과 운영 실패 알림 중복 방지의 test DB integration test 추가
- [x] P0: 거래처 등록과 증빙 파일 업로드를 remote mode 브라우저 UI에서 수행하고 새로고침 후 DB/file/audit 대사까지 확인하는 E2E 하네스 추가
- [x] P0: 파일 업로드/다운로드/삭제의 저장소 객체와 DB metadata 일치 테스트 추가
- [x] P0: 권한 없는 사용자의 버튼 숨김뿐 아니라 API 직접 호출 차단 테스트 추가
- [x] P0: 보고서와 즐겨찾기 mutation의 rowVersion/idempotencyKey/조건부 update 회귀 테스트 추가
- [x] P0: mutation safety matrix와 파일 upload lifecycle idempotency 회귀 테스트 추가
- [x] P0: 시스템 설정 스냅샷의 최신 AuditLog id/idempotencyKey 회귀 테스트 추가
- [ ] P1: 네트워크 실패, 서버 500, validation 실패, 중복 클릭, timeout/retry 테스트 추가
  - 진행 메모(2026-07-07): remote `requestRemoteEnvelope`에 15초 timeout, GET/HEAD/OPTIONS 한정 1회 재시도, 408/429/502/503/504 retry, 네트워크 오류/timeout/비JSON 서버 오류의 `ApiRequestError` 표준화를 추가했다. destructive mutation은 중복 처리 위험 때문에 자동 재시도하지 않는다. 실제 브라우저 network offline, 500 HTML, timeout, rate limit 응답 회귀 테스트는 테스트 단계에서 추가해야 한다.
- [ ] P1: 사용자 A/B 간 알림, 즐겨찾기, 권한, 승인 대기 목록 격리 테스트 추가
- [ ] P2: 대량 데이터 서버 페이지네이션, 보고서 생성 시간, 파일 업로드 성능 테스트 추가

## 24. 운영 준비, 보안, 장애 대응, 재무 통제 검증 리스트

기준: 23장 데이터 연동을 완료해도 곧바로 운영 가능으로 보지 않는다. 실제 운영 투입은 production 환경에서 보안, 배포, 백업/복구, 모니터링, 재무 통제, 감사, 데이터 이관, 장애 대응 리허설을 통과해야 승인한다.

현재 검토 결과:

- `docs/deployment-operations.md`, `docs/backend-runbook.md`, `docs/security-exception-rules.md`에 운영 기준은 문서화되어 있다.
- 백엔드 인증은 `User.passwordHash` scrypt 검증과 DB `auth_sessions` 저장소로 전환했다. 다만 production 계정 발급/권한 승인, CSRF/CORS 보안 smoke, session store 운영 백업/모니터링 검증은 아직 완료되지 않았다.
- health endpoint는 `/api/health`, `/api/health/db`, `/api/health/storage`, `/api/health/file-security`, `/api/health/jobs`, `/api/health/integrations`로 확장했다. `/api/health/jobs`는 보고서 예약 backlog와 worker/queue 설정을 확인하고, `/api/health/integrations`는 회계/은행 연동 credential reference, 서버 secret, HTTPS endpoint, 마지막 점검 상태를 확인한다. 실제 staging/prod worker, queue, 외부 회계/은행 endpoint 실연결 검증은 아직 남아 있다.
- 백업, PITR, rollback, migration 운영 원칙은 문서화되어 있으나 실제 자동화와 복구 리허설 결과는 체크리스트에 남아 있지 않다.
- 감사 로그는 append-only scanner와 DB trigger migration으로 수정/삭제 차단을 보강했고, 현재 감사 로그 생성 route는 requestId/IP/userAgent 공통 helper 사용과 주요 업무 mutation의 동일 트랜잭션 감사 로그 기록을 테스트로 고정했다.
- 2026-07-05에 `.github/workflows/ci.yml`을 추가해 PR/push에서 frontend test/build, backend build, Prisma generate를 실행하도록 했다. 실제 원격 CI 통과와 branch protection 적용은 아직 확인되지 않았다.
- 2026-07-05: `scripts/verify-release-env.mjs`와 `npm run release:check`를 추가해 staging/production release candidate에서 mock mode, localhost API/DB, 기본 파일 URL secret, local file storage, `DEV_LOGIN_PASSWORD` 사용을 자동 차단하도록 했다. 실제 staging/prod secret, DB, object storage, 도메인 값으로 통과한 것은 아직 아니다.
- 2026-07-05: `AuthSession` DB 모델과 세션 생성/조회/refresh/logout 로직을 추가해 서버 메모리 `Map` 세션을 제거했다. 세션 기본 정책은 유휴 30분, 절대 12시간, refresh 시 session id rotation, logout 전체 세션 무효화 옵션이다. 로그인은 `DEV_LOGIN_PASSWORD`가 아니라 `User.passwordHash`의 scrypt hash를 검증한다.
- 2026-07-05: `prisma/migrations/20260705000000_init/migration.sql` 초기 migration과 `migration_lock.toml`을 생성했다. 실제 staging DB에서 `migrate deploy` 또는 migration dry-run을 수행한 증적은 아직 없다.
- 2026-07-05: signed double-submit CSRF token을 추가해 `/auth/login`과 signed file content upload를 제외한 mutation API가 `erp_csrf` cookie와 `X-CSRF-Token` 헤더 일치를 요구하도록 했다. production에서는 `FRONTEND_ORIGIN`이 명시적 HTTPS non-local allowlist가 아니면 backend가 시작되지 않으며, release gate는 `FRONTEND_ORIGIN`과 `CSRF_SECRET`을 필수로 검증한다. 실제 production 도메인에서의 보안 smoke는 아직 미완료다.
- 2026-07-05: 지급 실행 API에 `idempotencyKey`, `rowVersion`, 승인번호, 금액, 거래처 일치 검증과 계좌/승인 완료 상태 차단을 추가했다. 이미 지급 완료된 건, 계좌 불일치 건, 승인 미완료 건은 backend에서 차단하며, 지급 실행 감사 로그와 requester 알림을 같은 트랜잭션에 남긴다.
- 2026-07-05: 지급 보류, 오류 재처리, 계좌 재확인, 지급 예정일 변경도 `idempotencyKey`/`rowVersion` 검증 후 backend rowVersion 조건으로 갱신하도록 보강했다. 계좌 재확인은 `Disbursement`와 `Vendor` 계좌 검증 상태를 함께 갱신한다. 외부 은행 계좌 검증 adapter는 `BANK_ACCOUNT_VERIFICATION_ENDPOINT`/`ERP_BANK_API_TOKEN` 기반으로 연결됐고, 실제 은행 sandbox/production endpoint 응답 증빙은 테스트/연동 단계에 남아 있다.
- 2026-07-05: 은행 이체 파일 export API를 추가해 지급 예정/오늘 지급, 승인 완료, 결재 단계 완료, 계좌 확인 완료, 활성 거래처, 복호화 가능한 암호화 계좌 조건을 backend에서 다시 검증한 뒤 CSV를 생성하도록 연결했다. 새 거래처 계좌 등록/수정은 AES-GCM 암호화 저장과 마스킹 표시를 적용하며, production release gate는 `BANK_ACCOUNT_SECRET`을 필수로 검증한다.
- 2026-07-05: 승인 route에서 요청자 본인 승인 완료 처리를 차단하고, 지급 실행 전 같은 `rowVersion`에 대한 다른 재무 담당자의 `execution_approval` 감사 로그가 없으면 실행을 거절하도록 2인 확인을 추가했다. 결제 요청자와 원 결재 승인자는 지급 실행 확인/실행 모두 차단한다.
- 2026-07-05: 은행 결과 대사 API를 추가해 지급번호, 승인번호, 금액, 은행 성공/실패 결과를 ERP `Disbursement`와 비교하고, 실패 결과는 `ERROR` 상태로 보정하며 `bank_result_reconcile` 감사 로그에 대상 건수/총액/행별 결과를 저장하도록 했다.
- 2026-07-05: 은행 이체 파일 생성 응답과 `bank_transfer_export` 감사 로그에 대상/생성 건수, 총액, 거래처 수, 지급/거래처 계좌 확인 건수, 승인 확인 건수, 지급 예정/오늘 지급 건수, 계좌번호를 제외한 행별 대사 스냅샷을 저장하고 지급 화면 성공 메시지에 같은 대사 지표를 표시하도록 보강했다.
- 2026-07-05: 마감 예산 기간 통제 함수를 추가해 `Budget` 또는 `BudgetItem`이 `CLOSED`인 경우 신규 결제 요청 제출, 예산 금액 조정/재오픈, 지급 예정일 변경, 오류 지급 복구를 backend에서 차단하도록 연결했다.
- 2026-07-05: `prisma/seedSafety.ts`를 추가해 `NODE_ENV=production` 또는 `RELEASE_TARGET=production`에서 `prisma db seed` 기본 실행을 차단했다. `scripts/verify-release-env.mjs`는 staging/production release candidate에서 `ALLOW_PRODUCTION_SEED` 설정을 실패 처리하고, `src/main.tsx`와 `src/api/service.ts`가 `mockData`/`mockApi`를 정적으로 import하지 않는지 검사한다.
- 2026-07-05: 운영 화면 카탈로그(`src/pageCatalog.ts`)와 local mock fixture(`src/mockData.ts`)를 분리하고, `src/api/service.ts`의 mock service를 lazy-load 구조로 바꿔 production remote 진입점이 local 샘플 데이터를 정적으로 끌어오지 않도록 보강했다. 이후 최신 remote production build artifact 문자열 스캔은 통과했으며, 실제 staging/production remote 배포 검증은 아직 남아 있다.
- 2026-07-05: `AuditLog`에 `ipAddress`, `userAgent`를 추가하고 감사 로그 생성 route가 `auditRequestContext(request)`를 통해 `requestId`와 함께 기록하도록 통일했다. 현재 로컬 검증은 통과했지만, 실제 staging/production DB migration deploy와 운영 로그 조회/UAT 증적은 아직 남아 있다.
- 2026-07-05: `BudgetAdjustment` DB 모델과 `/budgets/:departmentName/adjustments` 조회/생성 API를 추가하고, 예산 조정 버튼이 `rowVersion`/`idempotencyKey`와 함께 조정 요청을 저장하도록 연결했다. 1천만 원 이상은 승인 대기 이력으로 남기고, 미만은 같은 트랜잭션에서 예산 배정액/상태와 감사 로그를 갱신한다. `npm run test:unit`, `npx tsc --noEmit -p tsconfig.json`, `npm --prefix backend run build`, `npm run test:e2e`, `npm run release:migration-check`, `npm run release:audit-append-only`, `npm run release:check`, `npm run release:backend-smoke`는 통과했다. 실제 staging DB migration deploy와 remote DB E2E는 아직 남아 있다.
- 2026-07-05: 예산 사용액 생명주기 회귀 테스트를 추가해 제출/임시 저장은 예산 사용액을 차감하지 않고, 최종 승인 전환에서만 `BudgetItem.usedAmount`와 상위 `Budget.usedAmount`를 1회 증가시키며, 지급 실행/지급 상태 변경은 중복 차감하지 않는 기준을 고정했다. 예산 목록 잔액과 사용률은 backend `toBudgetRow`에서 persisted `allocatedAmount - usedAmount` 기준으로 재계산한다.
- 2026-07-05: 예산 목록 backend row에 `회계연도`, `기간`, `예산항목`, `예산항목수` 원장 필드를 추가하고, 예산 화면의 기간/부서/항목/상태 필터를 `/budgets` API query filter로 전달하도록 연결했다. mock budget fixture와 seed `BudgetItem.name`도 화면 항목 필터명과 맞췄으며, `budgetLedgerFilters` 회귀 테스트로 고정했다. `npm run test:unit`, `npx tsc --noEmit -p tsconfig.json`, `npm --prefix backend run build`, `npm run test:e2e`, `npm run release:migration-check`, `npm run release:audit-append-only`, `npm run release:check`, `npm run release:backend-smoke` 통과.
- 2026-07-05: 거래처 신규 등록/수정/비활성화가 `Vendor` DB route와 감사 로그 트랜잭션을 타고, 화면 저장 후 `listPageRows("vendors")` 재조회와 `erp:vendor-saved` 이벤트로 결제 요청 master data 재조회까지 이어지는지 `vendorPersistenceFlow` 회귀 테스트로 고정했다.
- 2026-07-05: 거래처 비활성화 시 backend가 같은 update 트랜잭션에서 active `PaymentRequest`와 open `Disbursement` 건수를 계산해 응답 row와 감사 로그에 `비활성화영향요청`, `비활성화영향지급예약`, `비활성화영향요약`으로 남기도록 보강했다. 화면 메시지도 서버 응답 기준 영향 범위를 표시한다. `npm run test:unit`, `npx tsc --noEmit -p tsconfig.json`, `npm --prefix backend run build`, `npm run release:migration-check`, `npm run release:audit-append-only`, `npm run release:check`, `npm run release:backend-smoke` 통과.
- 2026-07-05: 즐겨찾기 backend row에 구조화 필터(`필터JSON`)와 정렬(`정렬`) 메타데이터를 노출하고, 즐겨찾기 열기 시 서버 저장 대상 화면, 필터, 정렬을 `erp-table-state:{pageKey}`와 화면별 route state에 복원하도록 연결했다. 예산/거래처처럼 자체 필터 state를 쓰는 화면도 즐겨찾기 route state를 초기값으로 읽는다. `frontendFavoritesRemote` 회귀 테스트를 확장했고 `npx tsc --noEmit -p tsconfig.json`, `npm run test:unit`, `npm --prefix backend run build` 통과를 확인했다.
- 2026-07-05: 승인/반려/보류 mutation 성공 시 서버 응답 row를 즉시 현재 테이블 rows에 반영하고, 현재 사용자가 처리 가능한 다음 승인 건을 선택하도록 `UpdateSelectedRowOptions`와 `selectNextProcessableApprovalRow`를 추가했다. 일괄 승인 성공 후에는 선택을 비워 새 서버 목록이 도착하기 전 중복 bulk 클릭을 차단한다. `frontendApprovalNextSelection` 회귀 테스트를 추가했고 `npx tsc --noEmit -p tsconfig.json`, `npm run test:unit`, `npm --prefix backend run build` 통과를 확인했다.
- 2026-07-05: 알림 DTO와 프론트 `NotificationItem`에 `expiresAt`을 포함하고, `GET /notifications`, 단건 읽음, 전체 읽음이 모두 같은 활성 알림 scope(`expiresAt IS NULL OR expiresAt > now`)를 사용하도록 통일했다. 새 업무 알림은 `notificationExpiresAt()`으로 90일 만료일을 저장하며, 프론트는 active 알림만 카운트/표시하고 `linkPath`를 실제 접근 가능한 페이지로 검증한 뒤 이동한다. `notificationLifecycle` 회귀 테스트를 추가했고 `npx tsc --noEmit -p tsconfig.json`, `npm --prefix backend run build`, `npm run test:unit` 통과를 확인했다.
- 2026-07-05: `docs/api-spec.md`의 승인, 지급, 예산, 거래처, 보고서, 시스템 설정, 즐겨찾기 mutation 표를 실제 backend route와 맞춰 보강했다. `apiMutationSpecCoverage` 회귀 테스트를 추가해 핵심 mutation route 구현과 명세 항목이 같이 유지되는지 검사한다. `npm run test:unit` 통과를 확인했다.
- 2026-07-05: signed upload 본문이 10MB 정책을 초과하면 이미 저장된 storage object를 삭제한 뒤 validation 오류를 반환하도록 보강했다. `backendFileSecurity` 테스트에 presign metadata/audit transaction, upload object write와 checksum 반영, complete의 stored byte size 대사, delete의 DB metadata와 storage object 삭제 흐름을 고정하는 검증을 추가했다. `npm --prefix backend run build`, `npm run test:unit` 통과를 확인했다.
- 2026-07-05: `BudgetAdjustment` seed 샘플을 추가하고 `coreModelCoverage` 회귀 테스트를 추가해 핵심 Prisma 모델이 schema, deterministic seed, backend route/auth/security delegate, frontend API service contract, 고위험 회귀 테스트 세트에서 빠지지 않도록 고정했다. `AuthSession`과 `SecurityEvent`는 운영 runtime 모델이므로 seed 미대상으로 검증한다. `npm --prefix backend run build`, `npm run test:unit`, `npx tsc --noEmit -p tsconfig.json` 통과를 확인했다. 실제 backend test DB 기반 UI/DB integration E2E는 별도 P0로 남아 있다.
- 2026-07-05: `backend/src/app.ts`의 `buildApp()`으로 Fastify app 조립을 분리하고 `server.ts`는 listen 전용 진입점으로 정리했다. `backendAppFactory` 테스트는 서버 포트 listen 없이 `/api/health`를 `app.inject`로 검증한다. 이후 실제 test DB를 붙인 integration test와 remote-mode E2E harness를 붙일 수 있는 기반이며, `npm --prefix backend run build`, `npm run test:unit`, `npx tsc --noEmit -p tsconfig.json` 통과를 확인했다.
- 2026-07-05: `npm run test:integration`과 `tests/integration/backendDataPersistence.test.ts`, `tests/integration/backendSettingsPersistence.test.ts`, `tests/integration/backendPaymentRequestFlow.test.ts`를 추가했다. `ERP_TEST_DATABASE_URL`이 지정된 폐기 가능한 test DB에서 실제 로그인/CSRF 쿠키로 거래처 생성 persistence, 설정 권한 변경 persistence, 결제 요청 master data 조회, draft 생성, 실제 파일 presign/upload/complete, 제출, `ApprovalStep` 생성, 승인자 알림, 감사 로그를 Prisma로 직접 검증한다. CI는 `ERP_TEST_DATABASE_URL` secret이 있으면 integration test와 remote auth E2E에 test DB URL을 전달하고 없으면 명시적으로 skip한다. 현재 로컬은 test DB URL이 없어 `npm run test:integration` 4건 skip이며, `npm run test:unit`, `npm run test:integration` skip 동작 통과를 확인했다. 전체 업무 화면 remote-mode E2E와 UI 액션 후 DB 상태 검증은 아직 별도 P0로 남아 있다.
- 2026-07-05: 거래처 backend row에 `거래처RowVersion`을 노출하고, 거래처 수정/비활성화/계좌확인이 `rowVersion`과 `idempotencyKey`를 요구하도록 보강했다. backend는 `tx.vendor.updateMany({ id, rowVersion })` 조건부 갱신으로 stale write를 `CONFLICT` 처리하고, `AuditLog.idempotencyKey` 기준 replay/충돌을 판정한다. 화면 저장/계좌확인/비활성화 버튼도 같은 값을 전송하고 진행 중 중복 클릭을 차단한다. `vendorPersistenceFlow` 회귀 테스트를 확장했으며 `npm --prefix backend run build`, `npx tsc --noEmit -p tsconfig.json`, `npm run test:unit`, `npm run test:integration`(test DB 미설정 skip) 통과를 확인했다.
- 2026-07-05: 시스템 설정 권한 그룹과 사용자 권한 변경에 `Role.rowVersion`, `User.rowVersion` migration을 추가하고, 권한 그룹 생성/수정/삭제 및 사용자 권한 생성/수정/비활성화가 `idempotencyKey`와 rowVersion 조건부 갱신을 사용하도록 보강했다. 화면은 `/settings` 사용자 권한 목록을 backend에서 불러와 최신 `사용자RowVersion`을 보존하고, 권한 토글/상태/삭제/사용자 배정 버튼에 중복 요청 키를 전송한다. `settingsConcurrencyFlow`와 `backendSettingsPersistence` 회귀 테스트를 추가했으며 `npm --prefix backend run db:generate`, `npm --prefix backend run build`, `npx tsc --noEmit -p tsconfig.json`, `npm run test:unit`, `npm run test:integration`(test DB 미설정 2건 skip), `npm run release:migration-check`, `npm run release:check`, `npm test` 통과를 확인했다. staging/production DB migration deploy와 실제 `ERP_TEST_DATABASE_URL` 기반 설정 권한 integration 실행은 아직 별도 P0로 남아 있다.
- 2026-07-05: 결제 요청/거래처 첨부 다운로드 화면에서 production bundle에 `mock-download:` 문자열이 남지 않도록 직접 다운로드 가능 URL 판정 helper로 변경했다. `VITE_ERP_API_MODE=remote npm run build`로 최신 production artifact를 생성한 뒤 `npm run release:frontend-artifact`와 `rg` 검사에서 `mock-download`, `mock-upload`, `mockData`, `mockService`, `mockApi`, `example.local`, `127.0.0.1`, `localhost` 문자열이 dist에 남지 않음을 확인했다. `npx tsc --noEmit -p tsconfig.json`, `npm run test:unit`, `npm run test:e2e`도 통과했다. 실제 staging/production 배포 환경의 CDN/도메인 artifact 고정과 동일 artifact 승격 검증은 아직 별도 P0로 남아 있다.
- 2026-07-05: `scripts/verify-migrations.mjs`에 `REQUIRE_SHADOW_DATABASE_URL=true` strict 모드를 추가하고 CI의 `Verify Prisma Migrations` step이 `SHADOW_DATABASE_URL` secret 없이는 실패하도록 연결했다. 이제 CI/CD 필수 경로에 Prisma generate, migration static guard, Prisma schema validate, migration diff dry-run, unit/e2e, remote frontend build, frontend artifact scan, backend build, backend production smoke가 포함된다. `ciReleaseGates` 회귀 테스트와 strict 모드 실패 테스트를 추가했으며 `npm run release:migration-check`, `REQUIRE_SHADOW_DATABASE_URL=true npm run release:migration-check` 실패 확인, `npm run test:unit`, `npm --prefix backend run build`, `npm run release:check` 통과를 확인했다. 실제 GitHub Actions에서 `SHADOW_DATABASE_URL` secret과 disposable shadow DB 구성이 필요하다.
- 2026-07-05: `scripts/generate-release-manifest.mjs`와 `npm run release:manifest`를 추가해 `dist`, `backend/dist`, `prisma/migrations`, package/CI/release script 입력 파일의 SHA-256 manifest를 생성하도록 했다. CI는 backend smoke 후 `release/release-manifest.json`과 `release/migration-review.json`을 만들고 `erp-release-evidence` artifact로 업로드한다. 현재 로컬 manifest는 `manifestSha256=50d896c49957173f6671b687650cc805dc27453ad46081db69232f9bad81fef1`이며, `releaseManifest`, `releaseManifestVerify`, `migrationReview`, `ciReleaseGates` 회귀 테스트를 추가했다. `npm run release:manifest`, `npm run release:verify-manifest`, `npm run test:unit` 통과를 확인했다. 실제 release branch/tag와 staging/prod 동일 artifact 승격 검증은 아직 별도 P0로 남아 있다.
- 2026-07-06: 결제 요청 `POST/PATCH /payment-requests`와 예산 `POST/PATCH /budgets`에 감사 로그 기반 idempotency replay/충돌 처리, rowVersion stale 검증, 조건부 DB update를 보강했다. 프론트 결제 요청 생성/임시저장/제출 payload도 `idempotencyKey`와 `요청RowVersion`을 전송하도록 맞췄고, `raceConditionControls`와 첨부 payload 회귀 테스트로 고정했다. `npm --prefix backend run build`, `npm run test:unit`, `npm run build`, `npm run test:e2e`(remote auth skip), `npm run test:integration`(DB URL 없음으로 4건 skip), `npm run release:check`, `npm run release:backend-smoke`, remote mode `npm run build`, `npm run release:frontend-artifact`, `npm run release:audit-append-only`, `npm run release:migration-check`(SHADOW_DATABASE_URL 없음으로 diff dry-run skip), `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 최신 로컬 manifest hash는 `50d896c49957173f6671b687650cc805dc27453ad46081db69232f9bad81fef1`다. staging/prod 동시성 smoke는 아직 별도 P0로 남아 있다.

우선순위:

- P0: 운영 투입 전 미충족 시 데이터 손실, 보안 사고, 중복 지급, 장애 장기화가 발생할 수 있는 필수 게이트
- P1: 안정적 운영과 반복 배포 품질을 높이는 관리 게이트
- P2: 운영 효율, 감사 대응, 장기 유지보수를 위한 개선 게이트

### 24.1 운영 환경 및 배포 게이트

- [ ] P0: dev, staging, production의 DB, object storage, auth, secret, 도메인, 로그 저장소를 완전히 분리
  - 진행 메모(2026-07-08): `docs/environment-separation-matrix-template.md`와 `npm run release:environment-separation`을 추가해 dev/staging/production의 DB, object storage, auth/session, secret scope, domain/API origin, logs/monitoring, data policy 분리와 동일 artifact/migration promotion 증빙을 release gate에서 검증하도록 했다. 실제 cloud 리소스와 접근 권한 분리 증빙은 `ENVIRONMENT_SEPARATION_PATH` 완성본으로 확인해야 하므로 항목은 배포 전까지 미완료로 유지한다.
- [x] P0: production에서는 `VITE_ERP_API_MODE=remote`와 production API base URL만 사용하도록 배포 환경 검증
- [x] P0: production-like 환경에서 `prisma db seed` 기본 실행 차단과 release gate의 `ALLOW_PRODUCTION_SEED` 금지 검증 추가
- [x] P0: frontend production 진입점의 `mockData`/`mockApi` 정적 import 차단과 화면 카탈로그/local mock fixture 분리
- [x] P0: frontend production artifact scanner와 CI build 후 실행 gate 추가
- [x] P0: production build artifact에서 `mockData`, local seed, 테스트 이메일, 테스트 계좌, 샘플 첨부파일 문자열이 포함되지 않는지 검증
- [x] P0: CI/CD에서 Prisma schema validate, migration lock/provider 확인, destructive/risky migration SQL static guard 실행
- [x] P0: CI/CD에서 frontend test/build, backend build, Prisma generate, migration dry-run 또는 deploy 검증을 배포 필수 조건으로 설정
- [ ] P0: staging에서 동일 artifact와 동일 migration으로 smoke test를 통과한 빌드만 production 배포
- [ ] P0: DB migration은 하위 호환 원칙, 2단계 배포, rollback 영향 검토 기록을 남긴 뒤 승인
- [x] P0: migration review evidence에 하위 호환 static guard, rollback 영향, production seed 차단 여부를 기록하고 CI artifact로 보관
- [x] P1: 배포 전후 health check, 로그인, 결제 요청, 승인, 지급, 파일, 보고서, 알림 핵심 경로 자동 점검
  - 진행 메모(2026-07-08): `scripts/run-core-smoke-check.mjs`, `npm run release:core-smoke`, `docs/core-smoke-runbook.md`를 추가했다. 이 CLI는 `CORE_SMOKE_API_BASE_URL`과 smoke 계정으로 `/health/*`, `/auth/login`, `/auth/me`, 알림, 대시보드, 결제 요청, 승인, 지급, 예산, 거래처, 보고서, 설정, 운영 상태 endpoint를 조회하고 requestId를 출력한다. 실제 staging/prod 실행 로그는 각 배포 증빙 항목에서 확인한다.
- [x] P1: 정적 프론트, API 서버, DB migration 각각의 rollback 절차와 담당자 승인 흐름 문서화
  - 진행 메모(2026-07-08): `docs/rollback-break-glass-runbook.md`를 추가하고 `docs/deployment-operations.md`의 rollback 절차를 frontend artifact, API, DB migration/PITR, object storage/report artifact별 승인·검증 흐름으로 확장했다. `release:operational-docs`와 release manifest input에도 연결해 문서 누락을 release gate에서 잡도록 했다.
- [x] P2: 릴리즈 노트, 변경 영향 범위, 사용자 공지 템플릿 운영
  - 진행 메모(2026-07-08): `docs/release-note-template.md`, `npm run release:release-note`, CI release note gate, production strict `RELEASE_NOTE_PATH` 검증을 추가해 기능 변경, DB 변경, 권한 변경, 운영 영향, known issue, rollback 조건, 사용자 공지/승인 항목이 빠진 release를 차단하도록 했다.

### 24.2 인증, 세션, 권한 보안

- [x] P0: production 로그인에서 `DEV_LOGIN_PASSWORD` 제거 후 password hash 검증, SSO, MFA 중 승인된 방식 적용
- [x] P0: 세션 저장소를 서버 메모리 `Map`이 아닌 Redis/DB 등 다중 인스턴스와 재시작에 안전한 저장소로 전환
- [x] P0: 유휴 세션 만료 30분, 절대 세션 만료, 세션 rotation, logout 전체 무효화 정책 구현
- [x] P0: HttpOnly, Secure, SameSite, CSRF 방어, CORS allowlist를 production 도메인 기준으로 검증
- [x] P0: mutation API CSRF double-submit 검증과 production CORS allowlist runtime/release gate 구현
- [x] P0: 모든 API route에서 프론트 버튼 숨김과 별개로 backend 권한 검증을 반복 수행
- [x] P0: 전체 backend route inventory가 `requireAuth`, signed file token, auth/health 예외 패턴 중 하나를 유지하는 회귀 테스트 추가
- [x] P0: 관리자, 재무팀, 승인자, 외부 감사 권한을 최소 권한 원칙으로 검증하고 production 계정 권한 목록 승인
  - 증거: `src/domain/rolePolicy.ts`가 요청자/승인자/재무팀/외부 감사/관리자 기본 권한을 단일 기준으로 정의하고, `prisma/seed.ts`, 시스템 설정 화면 fallback, `workflowRules.permissionMatrix`가 같은 정책을 참조한다. `tests/unit/rolePolicy.test.ts`는 관리자 전용 wildcard/system 권한, 재무팀 지급 실행, 승인자 승인 처리, 외부 감사 read-only 경계를 고정한다. `release:check`는 production 후보에서 `PRODUCTION_ACCESS_REVIEW_APPROVED`, `PRODUCTION_ACCESS_REVIEW_ID`, `PRODUCTION_ACCESS_REVIEW_APPROVER`를 요구해 실제 운영 계정 승인 증빙이 없으면 배포를 차단한다.
- [x] P1: 비밀번호 정책, 로그인 실패 잠금, 휴면 계정, 퇴사자 계정 비활성화 배치 구현
  - 진행 메모(2026-07-07): `backend/src/auth/loginPolicy.ts`를 추가해 `security_events` 기준 최근 로그인 실패 횟수로 계정 잠금을 계산하고, `/auth/login`에서 `LOGIN_FAILURE_LOCK_THRESHOLD`/`LOGIN_FAILURE_WINDOW_MINUTES` 기준 잠금(`ACCOUNT_LOCKED`)과 `DORMANT_ACCOUNT_DAYS` 기준 휴면 계정 차단(`DORMANT_ACCOUNT`)을 적용했다. 비밀번호 hash 검증은 기존 scrypt 방식을 유지하며, 환경변수/문서/API spec에 정책을 반영했다.
  - 진행 메모(2026-07-07): `GET /operations/account-lifecycle`와 `POST /operations/account-lifecycle/deactivate`를 추가해 휴면 계정과 `OFFBOARDING_USER_EMAILS`/`TERMINATED_USER_EMAILS` 환경변수 기반 퇴사자 후보를 조회하고, 사유/idempotencyKey가 있는 실행 요청에서 사용자 비활성화, 활성 세션 revoke, `account_lifecycle_deactivate` 감사 로그를 같은 트랜잭션에 남기도록 했다. 설정 화면 `보관 정책` 탭에 계정 수명주기 카드와 `후보 비활성화` 버튼을 연결했다.
  - 진행 메모(2026-07-07): `PASSWORD_MIN_LENGTH`/`PASSWORD_MAX_AGE_DAYS` 기반 비밀번호 정책, `PASSWORD_EXPIRED` 로그인 차단, `POST /auth/password/change-expired`, `POST /auth/password/change`, `GET /auth/password-policy`를 추가했다. 변경 시 현재 비밀번호를 검증하고 새 scrypt hash 저장, 다른 활성 세션 revoke, `password_change` 감사 로그를 남긴다. 로그인 화면은 만료 시 새 비밀번호 설정 모드로 전환되고, 설정 화면에는 `보안` 탭과 본인 비밀번호 변경 카드가 추가되었다. HR 직접 API 연동은 향후 고도화로 남지만 운영 배치 입력 원천은 환경변수로 구성되어 있다.
- [x] P1: 권한 변경 즉시 세션/토큰 권한이 갱신되거나 재로그인을 요구하는 기준 적용
  - 진행 메모(2026-07-07): 권한 그룹 permission/status 변경 시 해당 그룹 사용자들의 활성 `AuthSession`을 같은 트랜잭션에서 revoke하고 `settings_role_session_revoke` 감사 로그를 남기도록 했다. 사용자 권한 그룹/상태/부서 변경 시에도 대상 사용자 세션을 revoke하고 `settings_user_session_revoke` 감사 로그와 화면 메시지에 재로그인 필요 안내를 표시한다. 서버는 매 요청마다 최신 Role/UserRole을 읽으므로 남은 세션도 최신 권한 기준으로 평가되며, 권한 변경 대상은 다음 요청부터 재로그인이 필요하다. 실제 다중 브라우저 세션 차단 증빙은 테스트 단계에서 확인해야 한다.
- [ ] P2: 정기 권한 검토 리포트와 예외 권한 만료일 관리

### 24.3 개인정보, 계좌정보, 파일 보안

- [x] P0: 계좌번호, 사업자등록증, 통장사본, 세금계산서, 개인정보 필드의 저장 암호화와 전송 암호화 검증
  - 증거: 계좌번호는 `BANK_ACCOUNT_SECRET` 기반 AES-GCM 암호화와 `release:sensitive-data` 마스킹 검증으로 고정했다. 파일류(사업자등록증, 통장사본, 세금계산서, 개인정보 첨부)는 object storage private bucket에 저장하고 `release:check`가 HTTPS API/storage/scanner endpoint, PostgreSQL TLS(`sslmode=require|verify-ca|verify-full`, `sslaccept=strict`, 또는 `PGSSLMODE`), `S3_SERVER_SIDE_ENCRYPTION_ENABLED=true` 또는 `FILE_STORAGE_ENCRYPTION_AT_REST=true`, 공개 object URL 부재를 검증한다.
- [x] P0: 마스킹되지 않은 계좌/개인정보가 목록, 로그, 다운로드, 오류 메시지, 브라우저 콘솔에 노출되지 않는지 검증
  - 증거: `release:sensitive-data`와 `tests/unit/sensitiveDataExposure.test.ts`가 거래처/지급 목록의 마스킹 계좌, 거래처 계좌 암호화 저장, data quality/은행 결과 대사 오류의 원문 계좌 미노출, 브라우저 production entrypoint console 미사용을 검사한다. 원문 계좌가 필요한 은행 이체 CSV는 `disbursement:execute` 권한과 감사 로그가 붙는 통제 예외로 분리하고, 화면/감사 대사 summary에는 계좌번호를 제외한다.
- [x] P0: object storage bucket 비공개, signed URL 만료, 파일별 권한 검증, 직접 URL 접근 차단
  - 증거: `tests/unit/backendFileSecurity.test.ts`가 upload/download signed path의 10분 TTL, 목적 불일치 차단, 만료 차단, API path만 반환하고 object storage 직접 URL을 노출하지 않는지, URL 발급 전 파일별 write/read 권한을 검증하는지 고정한다. `release:check`는 `S3_BUCKET_PUBLIC_ACCESS_BLOCKED=true` 또는 `FILE_STORAGE_BUCKET_PRIVATE=true` 증적과 공개 object storage URL env 부재를 요구한다.
- [x] P0: 파일 업로드 악성코드 검사, 확장자/Content-Type 검증, 용량 제한, quarantine 정책 적용
- [x] P0: S3-compatible storage adapter, storage health check, external malware scan adapter, release gate 필수 env 검증 구현
- [x] P0: 새 거래처 계좌번호 AES-GCM 암호화 저장, 마스킹 표시, 은행 이체 파일 생성 시 복호화 가능 여부 차단 구현
- [x] P1: 개인정보 보관 기간, 파기 기준, 다운로드 사유 기록, 접근 로그 보관 정책 구현
  - 진행 메모(2026-07-07): `retentionPolicy`의 감사 로그/첨부 metadata/보고서/알림 보관 기준에 더해 `GET /files/{id}/download`가 `reason`을 필수로 받고, 권한/scan 상태 통과 후 `AuditLog(action=download_request)`에 파일명, owner, 크기, signed URL 만료 시각, 첨부 metadata 보관 정책, 감사 로그 보관 기준을 기록하도록 보강했다. 결제 요청/거래처 증빙 다운로드 버튼은 업무 맥락별 다운로드 사유를 함께 전송한다. signed URL token 원문은 감사 로그에 저장하지 않는다. 실제 오래된 데이터 archive/파기 리허설 증빙은 운영 리허설 단계에서 확인해야 한다.
- [ ] P1: 운영 로그와 APM trace에서 secret, cookie, 계좌번호, 파일 URL이 마스킹되는지 확인
  - 진행 메모(2026-07-07): backend 기본 Fastify logger를 `createSafeLoggerOptions`로 전환해 signed file URL query, cookie, authorization, CSRF token, credential/secret/checksum/token 필드, 계좌번호 패턴을 redaction하도록 구성했다. 보안 이벤트는 기존 sanitize 경로를 유지한다. 실제 외부 APM/로그 수집기에 같은 redaction rule이 적용됐는지는 staging/production 관측 도구 연결 후 증적으로 확인해야 하므로 완료 처리는 보류한다.
  - 진행 메모(2026-07-08): `docs/production-environment-inventory-template.md`와 strict `release:environment-inventory` 검증에 APM/trace tool, trace redaction rule evidence, APM trace masking verification, sensitive data masking verification 필드를 추가했다. 운영 APM에서 secret/cookie/계좌번호/file URL 마스킹이 확인되지 않으면 production release gate가 실패하도록 했다.
- [ ] P2: 개인정보 처리 현황과 외부 감사용 접근 리포트 생성

### 24.4 재무 통제 및 중복 지급 방지

- [x] P0: 요청자 본인 승인 금지, 승인자/재무팀 직무 분리, 지급 실행 2인 확인 또는 승인 기준 적용
- [x] P0: 지급 실행 API에 idempotencyKey, 지급 고유키, rowVersion, 승인번호, 금액, 거래처 일치 검증 적용
- [x] P0: 이미 지급 완료된 건의 재지급, 계좌 불일치 건 지급, 승인 미완료 건 지급을 backend에서 차단
- [x] P0: 은행 이체 파일 생성 전후 대상 건수, 총액, 거래처, 계좌 검증 상태를 승인 화면과 대사
- [x] P0: 지급 완료 후 은행 결과 파일 또는 외부 지급 결과와 ERP 지급 상태를 대사하는 절차 구현
- [x] P0: 마감 기간의 신규 제출, 예산 조정, 지급 예정일 변경, 지급 오류 복구 제한을 backend에서 강제
- [x] P1: 예산 사용액, 지급 완료액, 보고서 금액을 일/월 단위로 자동 대사하고 불일치 알림 생성
  - 진행 메모(2026-07-07): `GET /operations/financial-reconciliation`을 추가해 `Budget.usedAmount`, `BudgetItem.usedAmount`, 승인 완료 결제 요청, 지급 완료 원장, `ReportRun.summary` 드릴다운 스냅샷의 요청번호/지급번호/금액/상태를 대사하고, critical 불일치 시 HTTP 409와 상세 mismatch 목록을 반환하도록 했다. `POST /operations/financial-reconciliation/notify`는 같은 날짜의 담당자/점검 항목 중복을 피하면서 활성 `system:manage` 담당자에게 `OPERATIONAL_ALERT`를 생성한다. 설정 화면 `보관 정책` 탭에 재무 대사 카드, 새로고침, 알림 발송 버튼, 월별 승인/지급 차이, 상세 불일치 테이블을 연결했다. 실제 staging/production DB 금액 대사 증빙은 테스트/배포 검토 단계에서 확인한다.
- [x] P1: 관리자 수동 복구는 사유, 전/후 상태, 검토자, 승인자를 감사 로그에 남기고 2차 승인 적용
  - 진행 메모(2026-07-07): `GET/POST /operations/manual-recoveries`와 `POST /operations/manual-recoveries/{id}/approve|reject`를 추가해 지급 건 수동 복구 요청, 대기 목록 조회, 다른 관리자 2차 승인/반려를 처리하도록 했다. 복구 요청은 지급번호, 복구 상태, 계좌 확인 상태, 지급예정일, 사유, idempotencyKey를 받고, 승인 시 `Disbursement` 상태를 rowVersion 조건으로 갱신한다. 요청/승인/반려 모두 `manual_recovery` 감사 로그에 요청자, 승인자, 전/후 상태, 사유, requestId를 남기며 요청자 본인 승인은 backend에서 차단한다. 설정 화면 `보관 정책` 탭에 수동 복구 2차 승인 카드를 연결했다. 실제 DB에서 승인자 분리와 상태 변경 증빙은 테스트 단계에서 확인한다.
- [x] P2: 재무 통제 예외 리포트와 월말 결산 점검표 생성
  - 진행 메모(2026-07-07): `GET /operations/financial-control-report`를 추가해 재무 대사 불일치, 수동 복구 승인 대기, 은행 결과 대사 감사 로그, 지급 변경 감사 로그, 보고서 스냅샷 검토 여부를 월 단위 결산 점검표로 반환하도록 했다. critical 예외나 미통과 점검이 있으면 HTTP 409와 `data.ok=false`를 반환하며, 설정 화면 `보관 정책` 탭에 재무 통제 리포트 카드, 새로고침, 예외 목록, 결산 점검표를 연결했다. 실제 월말 결산 증빙과 운영자 확인 서명은 테스트/배포 검토 단계에서 확인한다.

### 24.5 감사 로그 및 컴플라이언스

- [x] P0: 주요 업무 생성/수정/삭제/승인/반려/보류/지급/설정 변경 mutation에서 감사 로그를 같은 DB 트랜잭션으로 기록하고 회귀 테스트로 고정
- [x] P0: 감사 로그 수정/삭제 API를 만들지 않고 DB 권한에서도 update/delete를 제한하는 append-only 정책 적용
- [x] P0: 앱 코드와 migration에서 감사 로그 수정/삭제/upsert를 차단하는 append-only release gate 추가
- [x] P0: `audit_logs` UPDATE/DELETE를 거부하는 DB trigger migration 추가
- [x] P0: 감사 로그에 actorId, requestId, ip/userAgent, entityType, entityId, before/after, reason, idempotencyKey 저장
- [x] P0: 지급 실행 감사 로그에 actorId, requestId, entityType, entityId, before/after, idempotencyKey 저장
- [x] P0: 권한 실패, validation 실패, 중복 처리 차단, 파일 접근 거부도 감사/보안 이벤트로 기록
- [x] P0: 파일 업로드 validation 실패, signed URL 거부, 파일 조회/다운로드/삭제 권한 실패를 `security_events`에 기록
- [x] P0: 로그인 실패, 인증 필요, 공통 permission guard 권한 부족, CSRF 실패, rate limit 초과를 `security_events`에 기록
- [x] P0: 표준 오류 응답 `onSend` hook으로 업무 route의 `fail(...)` 응답을 `security_events`에 자동 기록
- [x] P1: 감사 로그 보관 기간, 아카이브, 검색 성능, 외부 감사 조회 권한을 분리
  - 진행 메모(2026-07-07): `audit_logs`에 `action, createdAt`과 `requestId` 인덱스를 추가하고, `GET /operations/audit-logs`를 `audit:read` 또는 `system:manage` 읽기 전용 route로 구현했다. 검색 query는 `search`, `entityType`, `action`, `requestId`, `actor`, 기간, page/pageSize를 지원하고, 응답에는 actor/entity/action/reason/requestId/ip/userAgent/요약과 감사 로그 보관·아카이브 정책을 포함한다. `beforeValue`/`afterValue` 원문 JSON은 외부 감사 조회 응답에서 제외한다. 보고서 화면에는 `AuditLogSearchCard`를 추가해 외부 감사자가 시스템 설정 권한 없이 감사 로그를 조회할 수 있게 했다. 실제 외부 감사 계정 UAT 증빙은 운영 전 UAT 단계에서 확인해야 한다.
- [x] P1: 운영자 직접 DB 수정 금지 원칙과 break-glass 절차 문서화
  - 진행 메모(2026-07-08): `docs/admin-manual.md`와 `docs/rollback-break-glass-runbook.md`에 운영자 직접 DB 수정 금지, time-boxed credential, 다중 승인, backup/PITR 확인, before/after 대사, AuditLog/security_events 직접 수정 금지, 권한 revoke 기준을 문서화했다.
- [ ] P2: 감사 로그 무결성 검증 hash chain 또는 외부 보관소 연계 검토

### 24.6 백업, 복구, 재해 대응

- [ ] P0: PostgreSQL full backup, WAL/PITR, object storage versioning, report artifact backup 정책을 실제 환경에 적용
  - 진행 메모(2026-07-06): `docs/backup-restore-rehearsal-template.md`와 `npm run release:backup-restore-evidence`를 추가해 PostgreSQL full backup schedule/retention, WAL/PITR retention, object storage versioning, report artifact backup, backup encryption/access 증적을 production 승격 전 필수 문서로 묶었다. 실제 production/staging backup 정책 적용 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: RPO/RTO 목표를 정하고 staging에서 DB point-in-time restore와 파일 저장소 복구 리허설 수행
  - 진행 메모(2026-07-06): backup/restore evidence 템플릿에 RPO/RTO, staging restore environment, point-in-time restore timestamp, row count/총액/예산/거래처 지급 이력/첨부 orphan/data-quality 대사, object storage와 report artifact restore rehearsal 항목을 추가했다. 실제 리허설 실행 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: backup 성공/실패 알림, backup 암호화, backup 접근 권한, 복구 계정 권한을 검증
  - 진행 메모(2026-07-06): backup/restore evidence 템플릿이 backup success/failure monitor, WAL/PITR continuity monitor, backup encryption owner, backup decrypt/restore permission owner, break-glass approval, restore account least privilege evidence를 요구하도록 했다. 실제 알림/권한 검증 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: migration 실패, partial deploy, DB 장애, object storage 장애, API 장애별 rollback/복구 절차를 실제로 리허설
  - 진행 메모(2026-07-06): backup/restore evidence 템플릿이 migration failure, partial deploy rollback, DB outage, object storage outage, API outage rehearsal, compensating migration decision record, previous release manifest rollback evidence를 요구하고 production release gate strict 검증에 연결했다. 실제 장애별 리허설 증적은 아직 없으므로 완료 처리는 보류한다.
- [x] P1: 복구 후 데이터 정합성 검증 쿼리와 업무 smoke test 자동화
  - 진행 메모(2026-07-08): 복구 후 `npm run release:core-smoke`, `/api/operations/data-quality`, `/api/operations/financial-reconciliation`, 업무별 metadata/AuditLog/security_events 대사를 `docs/rollback-break-glass-runbook.md`에 표준 검증 절차로 묶었다. 실제 staging/prod 복구 리허설 증빙은 P0 리허설 항목에서 별도로 유지한다.
- [x] P1: 장애 상황에서 읽기 전용 운영, 지급 일시 중지, 파일 업로드 중지 같은 기능 제한 모드 정의
  - 진행 메모(2026-07-07): `backend/src/operations/operationMode.ts`가 `ERP_OPERATION_MODE=normal|read_only|payments_paused|uploads_paused|maintenance`와 `ERP_DISABLED_CAPABILITIES=business_mutations,payments,file_uploads`를 해석하고, Fastify 전역 preHandler가 읽기 전용/점검 모드의 business mutation, 지급 일시 중지의 `/api/disbursements` mutation, 파일 업로드 중지의 presign/content/complete route를 `OPERATION_MODE_RESTRICTED`로 차단한다. `GET /operations/mode`, `erpApi.getOperationMode`, 설정 화면 `OperationModeCard`, 버튼 액션 매핑, API 문서, 배포/장애 대응 문서에 조회와 운영 절차를 연결했다. 실제 staging/production 환경변수 전환 rehearsal 증적은 배포 검증 단계에서 확인해야 한다.
- [ ] P2: DR 환경 전환, DNS failover, 장기 장애 커뮤니케이션 템플릿 준비

### 24.7 모니터링, 알림, 장애 대응

- [x] P0: API 5xx, DB 연결 실패, slow query, 로그인 실패 급증, 권한 실패 급증, 파일 업로드 실패 알림 설정
- [x] P0: 처리되지 않은 서버 예외를 `SERVER_ERROR` 표준 응답과 보안 이벤트로 기록
- [x] P0: 승인/지급/보고서/알림/파일 처리 실패를 업무 이벤트 단위로 추적하고 담당자에게 알림
- [x] P0: `/api/health`, `/api/health/db` 외 object storage, 외부 은행/회계 연동, queue/job 상태 health check 추가
- [x] P0: `/api/health/storage`, `/api/health/file-security` 추가
- [x] P0: requestId 기반으로 프론트 오류, API 로그, DB 감사 로그를 연결 추적
- [x] P1: 장애 등급, 담당자, 응답 시간, 사용자 공지, 사후 분석 템플릿을 운영 문서에 연결
  - 진행 메모(2026-07-08): `docs/incident-response.md`에 P0/P1/P2 incident commander, 필수 호출 담당자, 최초 응답/업데이트 주기, 사용자 공지 기준, 사후 분석 템플릿을 추가하고 rollback/break-glass runbook과 교차 참조했다.
- [x] P1: 운영 대시보드에 처리량, 오류율, p95 latency, 지급 실패, 보고서 실패, 업로드 실패를 표시
  - 진행 메모(2026-07-07): `/operations/alerts` 응답에 `metrics.eventsReviewed`, `ruleFailureRatePercent`, critical/warning triggered 수, slow query `durationMs` 기반 p95/p99/max latency와 DB latency를 추가했다. Dashboard는 `system:manage` 사용자에게 `DashboardOperationalMetrics` 카드를 표시하고 `getOperationalAlerts`, `getBusinessFailureAlerts`를 함께 조회해 처리량, 오류율, p95 latency, 지급 실패, 보고서 실패, 업로드 실패 count를 보여준다. 실제 production APM/분산 request count와의 대사는 운영 검증 단계에서 확인해야 한다.
- [ ] P2: synthetic monitoring으로 로그인부터 지급 전 단계까지 주요 경로 주기 점검

### 24.8 데이터 이관 및 품질

- [ ] P0: production 초기 데이터 이관 계획, 원천 시스템, 컬럼 매핑, 검증 쿼리, 책임자 승인 흐름 확정
  - 진행 메모(2026-07-06): `docs/data-migration-evidence-template.md`와 `npm run release:data-migration-evidence`를 추가해 source system, 이관 범위, freeze window, 컬럼 매핑, validation query, 책임자 승인, rollback 조건을 production 승격 전 필수 증빙으로 묶었다. 실제 원천 시스템/담당자 승인 증적은 아직 없으므로 완료 처리는 보류한다.
- [x] P0: 거래처, 계좌, 사용자, 권한, 부서, 예산, 미결 결제 요청의 중복/누락/상태 불일치 검증 자동 점검 구현
- [x] P0: mockData, local seed, 테스트 이메일, 테스트 계좌, 샘플 첨부파일이 production에 섞이지 않는지 자동 점검 구현
- [x] P0: 데이터 이관 후 총액, 건수, 상태별 집계, 예산 잔액, 지급 상태 대사용 summary 구현
- [ ] P0: staging/production 이관 후 총액, 건수, 상태별 집계, 예산 잔액, 거래처 지급 이력 실제 대사 통과
  - 진행 메모(2026-07-06): data migration evidence 템플릿이 staging rehearsal과 production reconciliation에서 row count, status aggregate, payment/disbursement total, budget balance, vendor payment history, attachment metadata/orphan, `/api/operations/data-quality`, mock/local seed/test marker 대사 증빙을 요구하도록 했다. 실제 staging/production 이관 실행 증적은 아직 없으므로 완료 처리는 보류한다.
- [x] P1: 이관 실패 rollback 또는 재실행 전략과 cutover freeze window 정의
  - 진행 메모(2026-07-08): `docs/cutover-runbook.md`를 추가해 freeze window, cutover abort condition, idempotent rerun, failed row quarantine, rollback owner, user communication, go/no-go decision 기준을 정의했다.
- [ ] P1: 운영 전 사용자 승인 테스트(UAT) 결과와 미해결 이슈 승인 기준 기록
- [ ] P2: 데이터 품질 리포트와 반복 정합성 점검 배치 운영

검토 메모: 2026-07-06에 `/api/operations/data-quality`를 추가해 사용자/권한/부서, 거래처 계좌/세금계산서 정보, 예산 배정/사용액, 미결 결제 요청, 결재 단계, 지급, 첨부파일 orphan, production test marker를 점검하도록 했다. critical 실패는 HTTP 409와 `data.ok=false`로 반환하며, 운영 이관 계획 템플릿은 `docs/data-migration-readiness.md`에 정리했다. 단, 원천 시스템 확정, 담당자 승인, staging/prod 실제 대사는 운영 환경 증적이 필요하므로 미완료로 유지한다.

### 24.9 성능, 용량, 안정성

- [x] P0: 예상 production 데이터량 기준으로 목록 조회, 필터, 정렬, 보고서 생성, 파일 다운로드 부하 테스트
  - 증거: `npm run release:performance-capacity`가 기본 20,000건 목록 search/filter/sort/page workload와 5,000행 보고서 다운로드 payload workload를 synthetic capacity budget 안에서 실행한다. 실제 staging/prod DB row count와 네트워크 조건의 부하 smoke는 25.3/25.4 배포 검증에서 별도 수행한다.
- [x] P0: DB index, pagination, report query timeout, file upload size, API body limit, rate limit 검증
  - 증거: `release:performance-capacity`가 Prisma list/report index, list `pageSize` 100 상한, shared pagination/filter/sort, 결제 요청 DB `skip/take/count`, backend report download 생성/감사 로그, 10MB upload policy, `API_BODY_LIMIT_BYTES`/`RATE_LIMIT_*` release gate 연결을 자동 대사한다.
- [x] P0: 10MB 첨부 정책을 수용하는 Fastify body limit과 health check 제외 기본 API rate limit 구현
- [x] P0: 중복 클릭, 동시 승인, 동시 지급, 동시 설정 변경 race condition 테스트
- [x] P1: p95/p99 응답 시간 목표, report job 최대 처리 시간, 대량 다운로드 제한 기준 정의
  - 진행 메모(2026-07-08): `backend/src/operations/performancePolicy.ts`를 추가해 `PERFORMANCE_P95_TARGET_MS`, `PERFORMANCE_P99_TARGET_MS`, `REPORT_JOB_MAX_PROCESSING_MS`, `REPORT_DOWNLOAD_MAX_ROWS`, `REPORT_DOWNLOAD_MAX_BYTES` 기준을 env 기반 정책으로 정의했다. `/operations/alerts`는 현재 latency와 목표 대비 상태를 포함하고, `GET /operations/performance-policy`와 설정 화면 `PerformancePolicyCard`에서 p95/p99 목표, report job 최대 처리 시간, 다운로드 row/byte 제한을 조회한다. `/reports/{name}/download`는 `ReportRun.rowCount` 또는 base64 payload가 제한을 넘으면 HTTP 413으로 차단한다. `release:performance-capacity` 정적 guard에도 정책/다운로드 제한 확인을 추가했으며, 실제 테스트/빌드 실행은 사용자의 “기능 우선, 테스트는 마지막” 지시에 따라 후속 검증 단계에서 진행한다.
- [x] P1: 백그라운드 job retry, dead-letter, timeout, circuit breaker 정책 구현
  - 진행 메모(2026-07-08): `backend/src/operations/reportJobWorker.ts`와 `GET /operations/report-jobs`, `POST /operations/report-jobs/run`을 추가해 due `ReportSchedule` batch dry-run/실행, 성공 `ReportRun(READY)`, retry backoff, dead-letter 비활성화, timeout, circuit breaker, 감사 로그, 내부 알림을 연결했다. 설정 화면 보관 정책 탭의 보고서 예약 job 카드에서 대기 확인/실행 버튼을 제공하고 `/health/jobs`에도 정책과 dead-letter 상태를 노출한다. 테스트/빌드와 staging/prod scheduler·webhook 발송 리허설 증빙은 사용자의 “기능 우선, 테스트는 마지막” 지시에 따라 후속 검증 단계에서 진행한다.
- [ ] P2: capacity planning과 월별 데이터 증가량 예측 리포트 작성

검토 메모: 2026-07-06에 `scripts/verify-performance-capacity.mjs`와 `npm run release:performance-capacity`를 추가해 성능/용량 P0를 로컬/CI release gate로 검증한다. 이 게이트는 회귀 방어와 release candidate 최소 기준이며, public internet 노출, 다중 인스턴스 분산 rate limit, 실제 object storage/download 지연은 staging/prod smoke에서 계속 확인한다.

### 24.10 운영 인수, 사용자 지원, Go-Live 승인

- [x] P0: 관리자 매뉴얼, 사용자 매뉴얼, 장애 대응 문서, 배포 절차가 실제 화면/운영 절차와 일치하는지 최종 검토
  - 증거: `docs/user-manual.md`, `docs/admin-manual.md`, `docs/incident-response.md`, `docs/deployment-operations.md`를 실제 화면, 버튼 매핑, API/운영 endpoint 기준으로 갱신했다. `npm run release:operational-docs`는 사용자/관리자/장애 대응/배포 문서, 버튼 액션 매핑, API spec, 이관 문서의 필수 화면·권한·파일·보고서·requestId·health/data-quality/business-failure alert·rollback·release gate 커버리지를 검증한다.
- [ ] P0: 운영자 계정, 재무팀 계정, 승인자 계정, 외부 감사 계정으로 역할별 UAT 통과
  - 진행 메모(2026-07-06): `docs/role-uat-evidence-template.md`와 `npm run release:role-uat-evidence`를 추가해 요청자, 승인자, 재무팀, 관리자, 외부 감사 실제 계정/권한 검증, 권한 경계, UAT 시나리오, 파일럿 범위, 지급 dry-run, 이슈 처리, 교육/지원, 최종 sign-off를 production 승격 전 필수 증빙으로 묶었다. 실제 역할별 UAT 실행 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: go-live 승인표에 기능 책임자, 보안 책임자, 재무 책임자, 운영 책임자 승인 기록
  - 진행 메모(2026-07-06): role UAT evidence 템플릿과 go-live handoff 템플릿이 기능/보안/재무/운영 책임자 승인자, 승인 시각, 증적 링크를 요구하고 production release gate strict 검증에 연결됐다. 실제 승인자 서명/승인 ID는 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: 운영 시작 전 known issue, 우회 절차, rollback 기준, 지원 연락망을 확정
  - 진행 메모(2026-07-06): `docs/go-live-handoff-template.md`와 `npm run release:go-live-handoff`를 추가했다. CI는 템플릿 구조를 audit하고, `RELEASE_TARGET=production npm run release:check`는 strict handoff 검증을 실행해 `TBD`, `pending`, `<...>` 같은 미확정 known issue/우회 절차/rollback/지원 연락망/sign-off 값이 남아 있으면 실패한다. 실제 담당자, 연락 채널, UAT 증적, 승인 ID가 아직 없으므로 완료 처리는 보류한다.
- [x] P1: 사용자 교육, FAQ, 오류 신고 양식, requestId 수집 방법 안내
  - 진행 메모(2026-07-08): `docs/user-training-faq.md`를 추가해 역할별 교육 실습, 운영 FAQ, 오류 신고 양식, requestId 수집/전달 방법, 교육 완료 기준을 문서화했다.
- [x] P1: 운영 첫 주 hypercare 점검표와 일일 상태 보고 기준 운영
  - 진행 메모(2026-07-08): `docs/hypercare-runbook.md`를 추가해 첫 주 daily check, 일일 상태 보고 템플릿, hypercare 리포트 포함 항목, 2주차 안정화 회고 기준을 문서화했다. 실제 첫 주 실행 증빙은 post go-live P0/P1 항목에서 유지한다.
- [ ] P2: 정기 점검 일정, 릴리즈 캘린더, 개선 요청 backlog 관리 방식 정의

## 25. 배포 및 실사용 전환 단계 검토 리스트

기준: 23장 데이터 연동과 24장 운영 준비가 완료되어도, 실제 사용자는 별도 전환 절차 없이 바로 받지 않는다. release candidate 생성, staging 검증, production 인프라 확정, 실데이터 이관, 파일럿, go-live, 안정화 기간을 단계별로 통과해야 실사용 가능으로 판정한다.

현재 검토 결과:

- 루트 `package.json`에는 `npm test`, `npm run build`가 있고 backend에는 `build`, `start`, `db:deploy` 스크립트가 있다.
- 배포 문서에는 정적 frontend, Node.js backend, PostgreSQL, S3-compatible object storage 방식이 정리되어 있다.
- 2026-07-05에 `.github/workflows/ci.yml`은 추가됐지만, 실제 원격 CI 실행 결과, branch protection, production 도메인, secret manager, object storage bucket, DB instance, 배포 플랫폼 설정 파일은 아직 확인되지 않았다.
- 2026-07-06에는 production 환경 inventory 템플릿과 `release:environment-inventory` gate를 추가해 배포 플랫폼, production 도메인, DB, object storage, secret manager, monitoring, backup/PITR, CDN/WAF 증적이 미확정이면 production release gate가 실패하도록 연결했다. 실제 운영 값과 증적 파일은 아직 없으므로 production 후보 판정은 계속 보류한다.
- 현재 상태는 `로컬 검증 통과 + 일부 운영 게이트 자동화` 단계다. production 후보 또는 실사용 가능 단계로 분류하지 않는다.
- production object storage/scanner 실연동, production 인증 계정/UAT, 실데이터 이관, remote mode staging E2E, migration deploy/diff dry-run, 운영 모니터링/백업 리허설이 아직 23장/24장/25장 P0로 남아 있어 현 상태는 실사용 배포 후보로 볼 수 없다.
- 실사용 가능 판정은 `문서 존재`가 아니라 `운영 환경에서 사용자가 실제 업무를 수행하고 데이터가 보존/복구/감사되는 것`을 기준으로 한다.
- 2026-07-05 로컬 release check: `npm test`, `npm run build`, `npm --prefix backend run build`, `npm --prefix backend run db:generate` 통과. 단, 원격 CI 통과, staging 배포, production secret/DB/storage/monitoring 확정은 아직 미검증이다.
- 2026-07-05 추가 release check: 결제 요청 master data API, `POST /payment-requests`, 제출 시 `ApprovalStep`/승인 알림 생성 보강 후 `npm test`, `npm run build`, `npm --prefix backend run build`, `npm --prefix backend run db:generate` 재통과. 여전히 remote DB E2E, staging 배포, production 인증/파일 저장소/모니터링은 미검증이므로 실사용 가능 판정은 보류한다.
- 2026-07-05 추가 release check: 첨부 목록 API, 가짜 기본 첨부 제거, 제출 전 backend `Attachment` 필수 검증, E2E 실제 PDF 업로드 반영 후 `npm test`, `npm run build`, `npm --prefix backend run build`, `npm --prefix backend run db:generate`, `npx tsc --noEmit -p tsconfig.json` 재통과. production object storage, 바이러스 검사 엔진, remote DB E2E, staging/prod 배포 검증은 아직 미완료다.
- 2026-07-05 추가 release check: 거래처 문서 목록을 backend `Attachment` 기준으로 재조회하고 거래처 증빙 다운로드/삭제 E2E를 추가한 뒤 `npm test`, `npm run build`, `npm --prefix backend run build`, `npm --prefix backend run db:generate`, `npx tsc --noEmit -p tsconfig.json` 재통과. 단, 이는 로컬 검증이며 production object storage, 악성코드 검사, remote DB E2E, staging/prod 배포 검증은 아직 미완료다.
- 2026-07-05 추가 release check: `npm run release:check`를 추가하고 CI에 연결해 local gate 통과를 확인했다. 이어서 `npm test`, `npm run build`, `npm --prefix backend run build`, `npm --prefix backend run db:generate`, `npx tsc --noEmit -p tsconfig.json` 재통과. staging/production gate는 실제 env/secret/storage/DB 값이 없어 아직 통과 검증 전이다.
- 2026-07-05 추가 release check: `User.passwordHash` scrypt 검증과 DB `AuthSession` 세션 저장소를 추가하고 서버 메모리 세션/`DEV_LOGIN_PASSWORD` 인증 경로를 제거했다. `npm run release:check`, production 형식 샘플 env의 `npm run release:check`, `npm test`, `npm run build`, `npm --prefix backend run build`, `npm --prefix backend run db:generate`, `npx tsc --noEmit -p tsconfig.json` 통과. 실제 migration 검토, staging/prod 보안 smoke는 아직 미완료다.
- 2026-07-05 추가 release check: Prisma schema 기준 초기 migration SQL을 생성하고 더미 PostgreSQL `DATABASE_URL`로 `npx prisma validate --schema prisma/schema.prisma` 통과를 확인했다. 단, 원격 DB 연결 정보가 없어 `db:deploy`/rollback rehearsal/branch protection과 묶인 실제 migration gate는 아직 미검증이다.
- 2026-07-05 추가 release check: CSRF double-submit 검증, frontend `X-CSRF-Token` 헤더 자동 첨부, production CORS allowlist runtime 차단, `FRONTEND_ORIGIN`/`CSRF_SECRET` release gate를 추가했다. local `npm run release:check`, production 형식 샘플 env의 `npm run release:check`, localhost origin 차단 케이스, `npm test`, `npm run build`, `npm --prefix backend run build`, `npm --prefix backend run db:generate`, `npx tsc --noEmit -p tsconfig.json`, 더미 PostgreSQL `DATABASE_URL`의 `npx prisma validate --schema prisma/schema.prisma` 통과. 실제 production 도메인 보안 smoke는 아직 미완료다.
- 2026-07-05 추가 release check: S3-compatible object storage adapter, external malware scan adapter, storage/file-security health endpoint, S3/scanner env release gate를 추가했다. local storage adapter와 EICAR 차단 unit test를 추가했고 `npm run release:check`, production 형식 샘플 env의 `npm run release:check`, local scan mode 차단 케이스, `npm test`, `npm run build`, `npm --prefix backend run build`, `npm --prefix backend run db:generate`, `npx tsc --noEmit -p tsconfig.json`, 더미 PostgreSQL `DATABASE_URL`의 `npx prisma validate --schema prisma/schema.prisma` 통과. 실제 production bucket policy, object versioning, quarantine, external scanner 응답, staging object storage health는 아직 미검증이다.
- 2026-07-05 추가 release check: 지급 실행 API에 `idempotencyKey`, `rowVersion`, 승인번호, 금액, 거래처 일치 검증과 중복 지급 차단을 추가했고, 지급 보류/재처리/계좌 재확인/일정 변경도 backend rowVersion 조건과 감사 로그 기준으로 갱신하도록 보강했다. 은행 이체 파일 export는 DB 지급 대상, 승인 완료, 결재 단계 완료, 계좌 확인 완료, 활성 거래처, 복호화 가능한 암호화 계좌 기준으로 생성하며 `BANK_ACCOUNT_SECRET` release gate를 추가했다. 요청자 본인 승인 금지, 결재 승인자/재무 실행자 분리, 지급 실행 2인 확인 감사 로그 검증을 추가했다. 은행 결과 대사는 지급번호/승인번호/금액/은행 결과 상태를 ERP 지급 상태와 비교하고 실패 결과를 `ERROR`로 보정한다. `npm test`, `npm --prefix backend run build`, `npm --prefix backend run db:generate`, `npx tsc --noEmit -p tsconfig.json`, 더미 PostgreSQL `DATABASE_URL`의 `npx prisma validate --schema prisma/schema.prisma`, local `npm run release:check` 통과. 실제 `RELEASE_TARGET=production npm run release:check`는 운영 env 미설정으로 실패했으며 `VITE_ERP_API_MODE=remote`, production API URL, `DATABASE_URL`, `FRONTEND_ORIGIN`, `FILE_URL_SECRET`, `CSRF_SECRET`, `BANK_ACCOUNT_SECRET`, S3/object storage, external malware scanner, `NODE_ENV=production` 값이 필요하다. 이후 최신 Vite remote production build와 artifact scan은 통과했다.
- 2026-07-05 추가 release check: 은행 이체 파일 생성 summary와 감사 로그에 대상/생성 건수, 총액, 거래처 수, 계좌/승인 검증 건수, 예정/오늘 지급 건수, 계좌번호 제외 행별 대사 스냅샷을 추가하고 지급 화면 성공 메시지에 같은 지표를 표시했다. `npm --prefix backend run build`, `npx tsc --noEmit -p tsconfig.json`, `npm run test:unit`, `npm test`, 더미 PostgreSQL `DATABASE_URL`의 `npx prisma validate --schema prisma/schema.prisma`, local `npm run release:check` 통과. 실제 `RELEASE_TARGET=production npm run release:check`는 운영 env 미설정으로 실패했고, 이후 최신 Vite remote production build와 artifact scan은 통과했다.
- 2026-07-05 추가 release check: 마감 예산 기간 통제를 추가해 신규 결제 요청 제출, 예산 조정/재오픈, 지급 예정일 변경, 오류 지급 복구를 backend에서 차단했다. `npm --prefix backend run build`, `npx tsc --noEmit -p tsconfig.json`, `npm run test:unit`, `npm test`, `npm --prefix backend run db:generate`, 더미 PostgreSQL `DATABASE_URL`의 `npx prisma validate --schema prisma/schema.prisma`, local `npm run release:check` 통과. 실제 `RELEASE_TARGET=production npm run release:check`는 운영 env 미설정으로 실패했고, 이후 최신 Vite remote production build와 artifact scan은 통과했다.
- 2026-07-05 추가 release check: 거래처 `Vendor` 모델에 담당자, 세금계산서 수신 이메일, 발행 방식을 추가하고 migration/seed/API/master data/UI 저장 흐름을 연결했다. 거래처 저장 시 사업자번호, 계좌번호, 세금계산서 이메일, 발행 방식 검증을 backend와 화면에 적용했고, 새 거래처 저장 후 결제 요청 폼이 master data를 재조회해 자동 선택하도록 연결했다. `npm --prefix backend run db:generate`, 더미 PostgreSQL `DATABASE_URL`의 `npx prisma validate --schema prisma/schema.prisma`, `npm --prefix backend run build`, `npx tsc --noEmit -p tsconfig.json`, `npm run test:unit`, `npm test`, local `npm run release:check` 통과. 실제 `RELEASE_TARGET=production npm run release:check`는 운영 env 미설정으로 실패했고, 이후 최신 Vite remote production build와 artifact scan은 통과했다.
- 2026-07-05 추가 release check: 시스템 설정의 저장된 승인 한도 정책을 신규 결제 요청 결재 단계 산정에 적용하고, 권한 그룹 삭제 route/UI를 추가했다. 사용자 배정 권한 그룹 삭제는 `ROLE_IN_USE`로 차단하며, 인증은 매 요청 DB `UserRole`/`Role.permissions`를 다시 읽어 권한 변경이 다음 API 호출부터 backend permission check에 반영된다. `npm --prefix backend run build`, `npx tsc --noEmit -p tsconfig.json`, `npm run test:unit`, `npm test`, 더미 PostgreSQL `DATABASE_URL`의 `npx prisma validate --schema prisma/schema.prisma`, local `npm run release:check` 통과. 실제 `RELEASE_TARGET=production npm run release:check`는 운영 env 미설정으로 실패했고, 이후 최신 Vite remote production build와 artifact scan은 통과했다.
- 2026-07-05 추가 release check: 외부 연동 설정에 credential reference, HTTPS 테스트 endpoint, 실패 사유, 마지막 테스트 시각을 추가하고 `/settings/integrations/{integrationId}/test` route와 화면 테스트 버튼을 연결했다. 테스트 route는 서버 환경변수 secret과 HTTPS endpoint를 사용해 호출하고 결과를 `integrations` 설정 스냅샷/감사 로그에 저장한다. `npm --prefix backend run build`, `npx tsc --noEmit -p tsconfig.json`, `npm run test:unit`, `npm test`, 더미 PostgreSQL `DATABASE_URL`의 `npx prisma validate --schema prisma/schema.prisma`, local `npm run release:check` 통과. 실제 `RELEASE_TARGET=production npm run release:check`는 운영 env 미설정으로 실패했고, 이후 최신 Vite remote production build와 artifact scan은 통과했다.
- 2026-07-05 추가 release check: 민감 route 직접 호출 권한 가드 회귀 테스트를 추가해 결제 요청, 승인, 지급, 파일, 거래처, 설정 권한 그룹/사용자 권한 route가 `requireAuth`, `FORBIDDEN`, backend permission predicate를 유지하는지 검증한다. `npm run test:unit`, `npm test`, local `npm run release:check` 통과. 실제 `RELEASE_TARGET=production npm run release:check`는 운영 env 미설정으로 실패했고, 이후 최신 Vite remote production build와 artifact scan은 통과했다.
- 2026-07-05 추가 release check: production-like `prisma db seed` 기본 차단, `ALLOW_PRODUCTION_SEED` release gate 금지, frontend production 진입점의 static `mockData`/`mockApi` import 차단을 추가했다. 화면 카탈로그와 local mock fixture를 분리하고 mock service를 lazy-load로 전환했으며, 예산/거래처 상세 패널은 API 지연 또는 빈 데이터에서도 화면 전체가 죽지 않도록 fallback을 보강했다. `npm test`, `npx tsc --noEmit -p tsconfig.json`, `npm --prefix backend run build`, `npm --prefix backend run db:generate`, 더미 PostgreSQL `DATABASE_URL`의 `npx prisma validate --schema prisma/schema.prisma`, local `npm run release:check`, `ALLOW_PRODUCTION_SEED=true` production gate 실패, `ALLOW_PRODUCTION_SEED` 미설정 production gate의 seed 항목 pass를 확인했다. 이후 최신 Vite remote production build artifact 스캔도 통과했으며, 실제 운영 env가 없어 production gate 전체 통과는 아직 미검증이다.
- 2026-07-05 추가 release check: `scripts/verify-backend-production-start.mjs`와 `npm run release:backend-smoke`를 추가해 `backend/dist/server.js`를 `NODE_ENV=production`, HTTPS non-local `FRONTEND_ORIGIN` 조건으로 직접 실행하고 `/api/health` 응답을 확인하도록 했다. CI는 backend build 후 이 smoke를 실행한다. `npm --prefix backend run build` 후 `node scripts/verify-backend-production-start.mjs` 통과를 확인했다. 이 smoke는 API 프로세스 기동과 기본 health만 검증하며 실제 production DB/S3/scanner health는 운영 env에서 별도 확인해야 한다.
- 2026-07-05 추가 release check: `scripts/frontendArtifactScanner.mjs`, `scripts/verify-frontend-artifact.mjs`, `npm run release:frontend-artifact`를 추가해 프론트 `dist` 산출물의 mock fixture, test email, local endpoint, seed marker, dev secret 문자열을 차단하도록 했다. CI는 `VITE_ERP_API_MODE=remote`로 frontend build 후 artifact scan을 실행한다. `npm run test:unit`, `npx tsc --noEmit -p tsconfig.json`, local `npm run release:check` 통과를 확인했고, 이후 최신 remote production build 후 `npm run release:frontend-artifact` 통과를 확인했다.
- 2026-07-05 추가 release check: `scripts/migrationGuard.mjs`, `scripts/verify-migrations.mjs`, `npm run release:migration-check`를 추가해 migration lock/provider 일치, migration.sql 존재/비어있지 않음, DROP/TRUNCATE/DELETE/RENAME/SET NOT NULL/NOT NULL without DEFAULT 같은 위험 SQL을 차단하고 Prisma schema validate를 실행하도록 했다. CI는 Prisma generate 후 `REQUIRE_SHADOW_DATABASE_URL=true`로 migration check를 실행해 shadow DB 기반 migration diff dry-run 없이는 실패한다. 로컬 `npm run release:migration-check`, `npm run test:unit`, `npx tsc --noEmit -p tsconfig.json`, local `npm run release:check` 통과를 확인했다. 실제 GitHub Actions 통과에는 `SHADOW_DATABASE_URL` secret과 disposable shadow DB 구성이 필요하다.
- 2026-07-05 추가 release check: `SecurityEvent`/`security_events`를 추가하고 파일 업로드 validation 실패, signed URL 거부, 파일 조회/다운로드/삭제 권한 실패, malware scan 차단/장애를 업무 감사 로그와 분리해 기록하도록 했다. 보안 이벤트 metadata는 token, credential, checksum, secret, cookie, authorization 값을 마스킹하고 signed URL query string은 path에서 제거한다. `npm --prefix backend run db:generate`, `npm run test:unit`, `npm --prefix backend run build`, `npm run release:migration-check`, `npm test`, `npx tsc --noEmit -p tsconfig.json`, `npm run release:check`, `npm run release:backend-smoke` 통과를 확인했다. 당시 기준으로는 전체 업무 route의 표준 실패 자동 기록이 남아 있었고, 이후 `onSend` hook 보강으로 보완했다.
- 2026-07-05 추가 release check: Fastify `bodyLimit`을 `API_BODY_LIMIT_BYTES`로 명시하고 기본값을 10MB 첨부 정책보다 큰 11MiB로 설정했다. health check와 CORS preflight를 제외한 API 요청에는 process 단위 고정 윈도우 rate limit을 적용하고, staging/production release gate에서 `RATE_LIMIT_DISABLED`, `RATE_LIMIT_WINDOW_MS`, `RATE_LIMIT_MAX`, `API_BODY_LIMIT_BYTES`를 검증하도록 했다. `npm run test:unit`, `npm --prefix backend run build`, `npm run release:check`, `npm run release:backend-smoke` 통과를 확인했다. 당시에는 다중 인스턴스 production의 WAF/API gateway/로드밸런서 분산 rate limit과 실제 부하 테스트가 남아 있어 24.9 P0를 미완료로 유지했고, 이후 2026-07-06 `release:performance-capacity`로 로컬/CI capacity gate를 보강했다.
- 2026-07-05 추가 release check: backend route inventory 회귀 테스트를 추가해 health/auth/signed file content 예외를 제외한 모든 route가 `requireAuth`를 호출하거나 보호된 route로 `app.inject` 위임하는지 확인한다. 기존 민감 route별 권한 predicate 테스트는 유지했다. `npm run test:unit` 통과를 확인했다. route 존재/인증 안전망은 추가됐지만 production 계정별 최소 권한 UAT와 모든 데이터 범위 권한 검증은 아직 남아 있다.
- 2026-07-05 추가 release check: `securityEventTypeForFailure`, `failureSecurityEventInput`, `failWithFailureSecurityEvent`를 추가해 로그인 실패, 세션 조회/갱신 실패, 공통 `requireAuth`/`requirePermission` 실패, CSRF 실패, rate limit 초과를 `security_events`에 기록하도록 확장했다. token/cookie/secret 계열 metadata는 계속 마스킹하고, DB URL이 없는 로컬 테스트 환경에서는 이벤트 쓰기만 건너뛴다. `npm run test:unit`, `npm --prefix backend run build`, `npm test`, `npx tsc --noEmit -p tsconfig.json`, `npm run release:check`, `npm run release:backend-smoke` 통과를 확인했다. 당시 기준으로는 개별 업무 route의 validation 실패와 중복 처리 차단 자동 기록이 남아 있었고, 이후 표준 오류 응답 hook으로 보완했다.
- 2026-07-05 추가 release check: `createSecurityEventFailureHook`을 서버 `onSend`에 연결해 표준 오류 응답(`fail(...)`)을 자동으로 `security_events`에 기록하도록 했다. `requireAuth` 성공 시 request actor를 매핑하고, 명시 기록된 파일/인증/CSRF/rate-limit 실패는 request-level marker로 중복 기록을 방지한다. 이제 권한 실패, validation 실패, idempotency conflict, workflow lock, 파일 접근 거부 같은 표준 오류 응답은 route별 수동 코드 없이 보안 이벤트로 남는다. `npm run test:unit`, `npm test`, `npm --prefix backend run build`, `npx tsc --noEmit -p tsconfig.json`, `npm run release:check`, `npm run release:backend-smoke` 통과를 확인했다.
- 2026-07-05 추가 release check: 백엔드 `fail(...)` 응답에도 `meta.requestId`를 포함하고, remote frontend API 오류는 `ApiRequestError` 메시지에 requestId를 붙이도록 했다. 성공 응답, 실패 응답, `AuditLog.requestId`, `SecurityEvent.requestId`, Fastify request log를 같은 requestId로 연결할 수 있다. `npm run test:unit`, `npm --prefix backend run build`, `npx tsc --noEmit -p tsconfig.json`, `npm test`, `npm run release:check`, `npm run release:backend-smoke` 통과를 확인했다.
- 2026-07-05 추가 release check: 전역 `createServerErrorHandler`를 추가해 처리되지 않은 서버 예외를 내부 상세 노출 없이 `SERVER_ERROR` 표준 응답으로 변환하고, 서버 로그와 `security_events`에 requestId 기준으로 기록하도록 했다. `npm run test:unit`, `npm --prefix backend run build`, `npm test`, `npx tsc --noEmit -p tsconfig.json`, `npm run release:check`, `npm run release:backend-smoke` 통과를 확인했다.
- 2026-07-05 추가 release check: `scripts/auditAppendOnlyScanner.mjs`, `scripts/verify-audit-append-only.mjs`, `npm run release:audit-append-only`를 추가해 `backend/src`와 `prisma/migrations`의 `auditLog.update/delete/upsert`, `audit_logs` UPDATE/DELETE/TRUNCATE/DROP SQL, 감사 로그 수정/삭제 route를 차단한다. `release:check`와 CI에도 연결했다. `npm run test:unit`, `npm run release:audit-append-only`, `npm run release:check`, `npm test`, `npm --prefix backend run build`, `npx tsc --noEmit -p tsconfig.json`, `npm run release:backend-smoke` 통과를 확인했다.
- 2026-07-05 추가 release check: `20260705030000_audit_log_append_only_trigger` migration을 추가해 `audit_logs`에 `BEFORE UPDATE OR DELETE` trigger를 적용하고, DB 레벨에서 감사 로그 수정/삭제 시도를 예외로 차단하도록 했다. `npm run test:unit`, `npm run release:migration-check`, `npm run release:audit-append-only`, `npm --prefix backend run db:generate`, `npm --prefix backend run build`, `npm test`, `npm run release:check`, `npm run release:backend-smoke`, `npx tsc --noEmit -p tsconfig.json` 통과를 확인했다. `SHADOW_DATABASE_URL`이 없어 migration diff dry-run은 여전히 미검증이며, 실제 staging/production DB 적용 여부는 migration deploy 검증 항목에서 확인해야 한다.
- 2026-07-05 추가 release check: `20260705040000_audit_log_request_context` migration을 추가해 `audit_logs`에 `ipAddress`, `userAgent`를 저장하고, 감사 로그 생성 route가 공통 `auditRequestContext(request)`를 사용하도록 통일했다. 새 회귀 테스트는 모든 `auditLog.create` 호출이 requestId/IP/userAgent helper를 포함하는지 검사한다. `npm --prefix backend run db:generate`, `npm run test:unit`, `npm run release:migration-check`, `npm run release:audit-append-only`, `npm --prefix backend run build`, `npx tsc --noEmit -p tsconfig.json`, `npm test`, `npm run release:check`, `npm run release:backend-smoke` 통과를 확인했다. `SHADOW_DATABASE_URL`이 없어 migration diff dry-run은 여전히 미검증이며, 실제 staging/production DB migration deploy와 감사 로그 조회 UAT는 별도 확인해야 한다.
- 2026-07-05 추가 release check: 보고서 삭제와 즐겨찾기 생성/수정/삭제를 DB 변경과 같은 `prisma.$transaction` 안에서 감사 로그를 남기도록 보강하고, 핵심 업무 mutation route가 `createAudit(tx, request, ...)` 또는 `tx.auditLog.create(...)`를 유지하는지 검사하는 `backendAuditTransactionCoverage` 회귀 테스트를 추가했다. `npm run test:unit`, `npm --prefix backend run build`, `npx tsc --noEmit -p tsconfig.json`, `npm run release:audit-append-only`, `npm test`, `npm run release:migration-check`, `npm run release:check`, `npm run release:backend-smoke` 통과를 확인했다. `SHADOW_DATABASE_URL`이 없어 migration diff dry-run은 여전히 미검증이다.
- 2026-07-05 추가 release check: 결제 요청 임시 저장/제출 payload에 준비 완료된 backend 첨부 ID를 포함하고, `POST/PATCH /payment-requests` 트랜잭션 안에서 `Attachment` metadata의 업로드 완료/차단 여부/요청자 소유/현재 요청 귀속을 검증 후 재확정하도록 보강했다. 생성/수정 감사 로그에도 연결 첨부 ID와 건수를 남긴다. `npm run test:unit`, `npx tsc --noEmit -p tsconfig.json`, `npm --prefix backend run build`, `npm run test:e2e`, `npm run release:check`, `npm run release:migration-check`, `npm run release:audit-append-only`, `npm run release:backend-smoke` 통과. remote DB E2E, staging 배포, production object storage/scanner 실연동 검증은 아직 별도 P0로 남아 있어 실사용 가능 판정은 보류한다.
- 2026-07-05 추가 release check: 승인 route가 pending 결재 단계만 처리하고 완료/반려/보류된 건은 `WORKFLOW_LOCKED`로 차단하도록 보강했다. 승인/반려/보류 시 `ApprovalStep`, `PaymentRequest`, 감사 로그, requester/다음 승인자 알림을 같은 트랜잭션에서 갱신한다. 대시보드는 `/dashboard` API row와 알림 센터 데이터를 사용해 KPI, 긴급 결재, 최근 활동, 최근 결제 요청을 계산한다. `npm run test:unit`, `npx tsc --noEmit -p tsconfig.json`, `npm --prefix backend run build`, `npm run test:e2e`, `npm run release:check`, `npm run release:migration-check`, `npm run release:audit-append-only`, `npm run release:backend-smoke` 통과. remote DB E2E, staging 배포/UAT, production artifact scan은 아직 별도 검증이 필요하다.
- 2026-07-05 추가 release check: 승인 처리 payload에 `요청RowVersion`, `결재RowVersion`, `결재StepID`, `idempotencyKey`를 포함하고, backend가 클라이언트 rowVersion 불일치, transaction 안의 조건부 `updateMany` 실패, 중복 idempotencyKey를 충돌로 차단하도록 보강했다. `npm run test:unit`, `npx tsc --noEmit -p tsconfig.json`, `npm --prefix backend run build`, `npm run test:e2e`, `npm run release:check`, `npm run release:migration-check`, `npm run release:audit-append-only`, `npm run release:backend-smoke` 통과. 이 보강은 승인 처리 영역에 우선 적용했으며 모든 mutation 공통 동시성 기준 항목은 다른 화면 확장이 필요해 미완료로 유지한다.
- 2026-07-05 추가 release check: 승인 최종 완료 시 `BudgetItem.usedAmount`, `Budget.usedAmount`, 예산 상태(`NORMAL/WARNING/EXCEEDED`)를 같은 승인 트랜잭션에서 갱신하고 `approval_budget_usage` 감사 로그를 남기도록 보강했다. `npm run test:unit`, `npx tsc --noEmit -p tsconfig.json`, `npm --prefix backend run build`, `npm run test:e2e`, `npm run release:check`, `npm run release:migration-check`, `npm run release:audit-append-only`, `npm run release:backend-smoke` 통과. 제출 예약액, 지급 완료액, 월별 예산 원장 대사 정책은 아직 별도 P0/P1로 남아 있다.
- 2026-07-05 추가 release check: 보고서 생성 화면을 `POST /reports` `ReportRun` 생성으로 연결하고, CSV/PDF 다운로드를 `GET /reports/{name}/download?format=csv|pdf` 서버 생성 payload와 `report_run` 다운로드 감사 로그로 전환했다. `frontendReportDownloads` 회귀 테스트와 보고서 다운로드 권한/감사 로그 static guard를 추가했고, `npm test`, `npm --prefix backend run build`, `npx tsc --noEmit -p tsconfig.json`, `npm run release:check`, `npm run release:migration-check`, `npm run release:audit-append-only`, `npm run release:backend-smoke` 통과를 확인했다. `SHADOW_DATABASE_URL`이 없어 migration diff dry-run은 미검증이며, 예약 발송 queue/job, report artifact object storage 영구 보관, staging remote mode 다운로드 검증은 아직 미완료다.
- 2026-07-05 추가 release check: 보고서 예약 발송 목록/추가/수정/중지를 `GET/POST/PATCH/DELETE /reports/schedules`와 `ReportSchedule` DB 저장, 감사 로그, 내부 알림 생성으로 연결했다. `frontendReportSchedules` 회귀 테스트, 보고서 예약 권한 guard, 감사 트랜잭션 guard를 추가했고, 이후 report job worker가 `REPORT_DELIVERY_MODE=internal|webhook`, retry, dead-letter, circuit breaker를 처리하도록 확장됐다. 직접 SMTP/메신저는 webhook 수신 서비스 책임으로 분리하고, staging/prod webhook 발송 리허설 증적은 별도 운영 검증으로 남긴다.
- 2026-07-05 추가 release check: `scripts/verify-release-manifest.mjs`와 `npm run release:verify-manifest`를 추가해 생성된 release manifest가 현재 frontend/backend/migration/release-input 산출물과 일치하는지 재계산한다. CI는 manifest 생성 직후 검증을 통과해야 artifact를 보관하며, production 승격 시 `EXPECTED_RELEASE_MANIFEST_SHA256`에 staging manifest hash를 넣어 다른 산출물이 섞이면 실패하도록 했다. 최신 로컬 manifest hash는 `50d896c49957173f6671b687650cc805dc27453ad46081db69232f9bad81fef1`이고, `npm run release:manifest`, `npm run release:verify-manifest`, checksum mismatch 실패 케이스, `npm run test:unit`, `npm run release:check` 통과를 확인했다. 실제 release tag 고정, staging 배포, production 승격 실행은 아직 별도 P0로 남긴다.
- 2026-07-05 추가 release check: `/api/health/jobs`와 `/api/health/integrations`를 추가해 보고서 예약 job backlog, 최근 실패 실행, worker/queue 설정, 회계/은행 외부 연동 credential reference, 서버 secret 존재 여부, HTTPS endpoint, 마지막 점검 상태를 운영 health로 노출했다. 미충족 항목은 HTTP 503과 `data.ok=false`로 반환한다. `backendOperationalHealth` 회귀 테스트와 운영 문서를 갱신했으며 `npm run test:unit`, `npm --prefix backend run build`, `npm run release:backend-smoke`, `npm run release:check`, `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 실제 staging/prod worker, queue, 외부 회계/은행 endpoint 실연결 검증은 아직 별도 P0 운영 검증으로 남긴다.
- 2026-07-05 추가 release check: `tests/integration/backendPaymentRequestFlow.test.ts`를 추가해 `ERP_TEST_DATABASE_URL` 기반 test DB에서 결제 요청 master data, draft 생성, 실제 파일 presign/upload/complete, 제출, `ApprovalStep` 생성, 승인자 알림, 감사 로그를 Prisma로 직접 대사하도록 했다. CI의 DB integration step은 test DB secret이 있을 때 `npm run test:integration`을 실행한다. 현재 로컬은 test DB URL이 없어 integration 4건 skip이며, `npm run test:integration`, `npm run test:unit` 통과를 확인했다. 브라우저 UI에서 결제 요청 전체 액션 후 DB 상태를 직접 검증하는 remote-mode E2E는 아직 별도 P0로 남긴다.
- 2026-07-05 추가 release check: `/api/operations/alerts`를 추가해 `security_events`와 DB 연결 check 기준으로 API 5xx, DB 연결 실패, slow query, 로그인 실패 급증, 권한 실패 급증, 파일 업로드 실패를 운영 alert rule로 평가한다. `SLOW_QUERY_MS`, `ALERT_WINDOW_MINUTES`, `ALERT_API_5XX_THRESHOLD`, `ALERT_SLOW_QUERY_THRESHOLD`, `ALERT_LOGIN_FAILURE_THRESHOLD`, `ALERT_PERMISSION_FAILURE_THRESHOLD`, `ALERT_FILE_UPLOAD_FAILURE_THRESHOLD`로 임계치를 조정하며, alert 발생 시 HTTP 503과 `data.ok=false`를 반환한다. Prisma slow query 이벤트는 duration/threshold만 `security_events`에 남기고 raw SQL/parameter는 저장하지 않는다. `backendOperationalAlerts`와 route permission guard를 추가했고 `npm run test:unit`, `npm --prefix backend run build` 통과를 확인했다.
- 2026-07-05 추가 release check: `scripts/generate-migration-review.mjs`와 `npm run release:migration-review`를 추가해 각 migration SQL의 checksum, statement count, 주요 operation, 하위 호환 static guard 결과, rollback 영향, production seed 차단 증거를 `release/migration-review.json`에 남긴다. CI는 `RELEASE_VERSION=${{ github.sha }}`로 review를 생성하고 `erp-release-evidence` artifact에 manifest와 함께 보관한다. 최신 로컬 review hash는 `dfdab11fa7ed007ddd26c13eb3171f041262784e576cdf3168f8033a6d24a763`이고, `npm run release:migration-review`, `npm run release:migration-check`, `npm run release:verify-manifest`, `npm run test:unit`, `npm run release:check` 통과를 확인했다. 실제 staging DB deploy, shadow DB diff dry-run 실연결, DBA/운영 승인과 rollback 리허설은 아직 별도 P0로 남긴다.
- 2026-07-05 추가 release check: CI `push.tags`에 `v*` release tag를 추가하고 `Verify Release Manifest` 단계가 `EXPECTED_RELEASE_GIT_COMMIT=${{ github.sha }}`로 manifest의 기록 commit을 대사하도록 했다. `ciReleaseGates`와 `releaseManifestVerify` 테스트가 tag trigger와 commit mismatch 실패를 고정한다. `npm run release:manifest`, `npm run release:verify-manifest`, `npm run test:unit`, `npm run release:migration-review`, `npm run release:migration-check`, `npm run release:check` 통과를 확인했다. 실제 production 배포 플랫폼에서 해당 tag artifact를 staging과 production에 동일하게 승격하는 실행 증적은 아직 남아 있다.
- 2026-07-05 추가 release check: remote mode에서 `/auth/me` 실패 시 빈 ERP 화면으로 남지 않도록 실제 로그인 화면을 추가하고, topbar 로그아웃 버튼을 `/auth/logout`에 연결했다. `tests/e2e/remote-auth-smoke.test.mjs`는 `ERP_TEST_DATABASE_URL`이 있을 때 backend test server와 Vite remote frontend를 띄워 실제 브라우저 로그인, 새로고침 세션 유지, 설정 화면 접근, 로그아웃을 검증한다. CI `Run Tests` 단계에도 `ERP_TEST_DATABASE_URL` secret을 전달하도록 연결했다. 현재 로컬은 test DB URL이 없어 remote auth E2E 1건 skip이며, `npm run test:unit`, `npm run test:e2e`(권한 실행, remote auth skip), `npm run test:integration`(4건 skip), remote production build, `npm run release:frontend-artifact`, `npm --prefix backend run build`, `npm run release:backend-smoke`, `npm run release:manifest`, `npm run release:verify-manifest`, `npm run release:check` 통과를 확인했다.
- 2026-07-06 추가 release check: `/api/operations/data-quality`를 추가해 이관/운영 전 사용자/권한/거래처/계좌/예산/미결 결제 요청/지급/첨부파일 정합성과 production test marker를 `system:manage` 권한으로 점검하도록 했다. critical 실패는 HTTP 409와 `data.ok=false`로 반환하며, `docs/data-migration-readiness.md`와 운영 문서에 cutover 품질 게이트를 추가했다. Vite config loader가 sandbox 밖 부모 폴더를 읽어 E2E dev server가 뜨지 않던 문제는 `--configLoader runner`와 E2E child process `unref`로 보강했다. `npm run test:unit`, `npm run test:e2e`(remote auth skip), `npm run test:integration`(DB URL 없음으로 4건 skip), `npm --prefix backend run build`, remote mode `npm run build`, `npm run release:frontend-artifact`, `npm run release:backend-smoke`, `npm run release:check`, `npm run release:migration-review`, `npm run release:migration-check`(SHADOW_DATABASE_URL 없음으로 diff dry-run skip), `npm run release:audit-append-only`, `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 최신 로컬 manifest hash는 `50d896c49957173f6671b687650cc805dc27453ad46081db69232f9bad81fef1`다.
- 2026-07-06 추가 release check: `/api/operations/business-failure-alerts`와 `POST /api/operations/business-failure-alerts/notify`를 추가해 승인/지급/보고서/알림/파일 route의 실패를 `security_events.path`와 `eventType` 기준으로 업무 도메인별 집계하고, 활성 `system:manage` 담당자에게 `OPERATIONAL_ALERT` 알림을 중복 없이 생성하도록 했다. 도메인별 임계치는 `ALERT_APPROVAL_FAILURE_THRESHOLD`, `ALERT_DISBURSEMENT_FAILURE_THRESHOLD`, `ALERT_REPORT_FAILURE_THRESHOLD`, `ALERT_NOTIFICATION_FAILURE_THRESHOLD`, `ALERT_FILE_PROCESSING_FAILURE_THRESHOLD`로 조정한다. `20260706010000_operational_alert_notifications` migration과 `backendBusinessFailureAlerts` 회귀 테스트를 추가했다. `npm --prefix backend run db:generate`, `npm --prefix backend run build`, `npm run test:unit`, `npm run test:e2e`(remote auth skip), `npm run test:integration`(DB URL 없음으로 4건 skip), `npm run release:migration-check`(SHADOW_DATABASE_URL 없음으로 diff dry-run skip), `npm run release:migration-review`, `npm run release:audit-append-only`, `npm run release:check`, `npm run release:backend-smoke`, remote mode `npm run build`, `npm run release:frontend-artifact`, `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 최신 로컬 migration review hash는 `dfdab11fa7ed007ddd26c13eb3171f041262784e576cdf3168f8033a6d24a763`, manifest hash는 `50d896c49957173f6671b687650cc805dc27453ad46081db69232f9bad81fef1`이다.
- 2026-07-06 추가 release check: `tests/unit/raceConditionControls.test.ts`를 추가해 중복 클릭 중 table mutation 재진입 차단, 승인 처리 idempotencyKey/요청 rowVersion/결재 step rowVersion 조건부 갱신, 지급 처리 idempotencyKey/rowVersion/2인 확인/조건부 updateMany, 설정 권한 그룹·사용자 권한 rowVersion/idempotencyKey 방어가 유지되는지 통합 검증한다. `npm run test:unit`, `npm --prefix backend run build` 통과를 확인했다. 모든 mutation route의 표준 idempotency/rowVersion 적용 여부는 이후 `npm run release:mutation-safety` gate로 승격해 23.1 P0 완료 증거로 고정했다.
- 2026-07-06 추가 release check: `ReportRun`, `ReportSchedule`, `FavoriteItem`에 `rowVersion` migration을 추가하고 보고서 생성/수정/삭제, 예약 발송 생성/수정/중지, 즐겨찾기 생성/수정/삭제/action adapter가 `idempotencyKey` replay/충돌 처리와 조건부 `updateMany`를 사용하도록 보강했다. 화면 payload도 `보고서RowVersion`, 예약 `rowVersion`, `즐겨찾기RowVersion`을 전송하고 API 응답의 최신 rowVersion으로 로컬 상태를 갱신한다. `reportFavoriteConcurrencyFlow` 회귀 테스트를 추가했고, `npm --prefix backend run db:generate`, `npm --prefix backend run build`, `npm run build`, `npm run test:unit`, `npm run test:e2e`(remote auth skip), `npm run test:integration`(DB URL 없음으로 4건 skip), `npm run release:check`, `npm run release:backend-smoke`, `npm run release:audit-append-only`, `npm run release:migration-check`(SHADOW_DATABASE_URL 없음으로 diff dry-run skip), `npm run release:migration-review`, remote mode `npm run build`, `npm run release:frontend-artifact`, `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 최신 로컬 migration review hash는 `dfdab11fa7ed007ddd26c13eb3171f041262784e576cdf3168f8033a6d24a763`, manifest hash는 `50d896c49957173f6671b687650cc805dc27453ad46081db69232f9bad81fef1`다. staging/prod 동시성 smoke는 아직 별도 P0로 남긴다.
- 2026-07-06 추가 release check: 파일 metadata lifecycle(`POST /files/presign-upload`, `POST /files/complete`, `DELETE /files/{id}`)에 `idempotencyKey` replay/conflict 처리를 추가하고, 프론트 결제 요청/거래처 첨부 업로드와 삭제가 안정적인 file mutation key를 전송하도록 보강했다. `docs/mutation-safety-matrix.md`에 표준 mutation 방어와 signed file content, 알림 읽음, 인증 세션, 운영 알림 예외 기준을 분리했고 `mutationSafetyMatrix` 회귀 테스트로 고정했다. `npm --prefix backend run build`, `npm run build`, `npm run test:unit`, `npm run test:e2e`(remote auth skip), `npm run test:integration`(DB URL 없음으로 4건 skip), `npm run release:check`, `npm run release:backend-smoke`, `npm run release:audit-append-only`, `npm run release:migration-check`(SHADOW_DATABASE_URL 없음으로 diff dry-run skip), `npm run release:migration-review`, remote mode `npm run build`, `npm run release:frontend-artifact`, `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 최신 로컬 migration review hash는 `dfdab11fa7ed007ddd26c13eb3171f041262784e576cdf3168f8033a6d24a763`, manifest hash는 `50d896c49957173f6671b687650cc805dc27453ad46081db69232f9bad81fef1`이다.
- 2026-07-06 추가 release check: 시스템 설정 스냅샷 `GET /settings/config`가 최신 `AuditLog.id` 메타데이터를 반환하고, `PATCH /settings/config/{key}`가 `{ value, expectedAuditLogId, idempotencyKey }` payload로 중복 저장 replay와 stale save conflict를 처리하도록 보강했다. 화면 저장 버튼은 설정 값 기반 stable idempotency key와 최신 audit id를 전송하며, 외부 연동 테스트 후에는 설정 메타데이터를 재조회한다. `settingsConfigSnapshotConcurrency` 회귀 테스트와 API/Mutation Safety 문서 기준을 추가하고, `backendSettingsPersistence.test.ts`가 test DB에서 스냅샷 저장/replay/conflict/audit log를 직접 검증하도록 확장했다. `npm --prefix backend run build`, `npm run build`, `npm run test:unit`, `npm run test:e2e`(remote auth skip), `npm run test:integration`(DB URL 없음으로 4건 skip), `npm run release:check`, `npm run release:backend-smoke`, `npm run release:audit-append-only`, `npm run release:migration-check`(SHADOW_DATABASE_URL 없음으로 diff dry-run skip), `npm run release:migration-review`, remote mode `npm run build`, `npm run release:frontend-artifact`, `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 최신 로컬 migration review hash는 `dfdab11fa7ed007ddd26c13eb3171f041262784e576cdf3168f8033a6d24a763`, manifest hash는 `50d896c49957173f6671b687650cc805dc27453ad46081db69232f9bad81fef1`이다.
- 2026-07-06 추가 release check: `backendNotificationOperationsFlow.test.ts`를 추가해 `ERP_TEST_DATABASE_URL` 기반 test DB에서 알림 단건 읽음 재호출이 같은 `readAt`으로 수렴하는지, `/operations/business-failure-alerts/notify`가 같은 업무 실패 window 안에서 담당자별 `OPERATIONAL_ALERT`를 중복 생성하지 않는지 검증하도록 했다. 현재 로컬은 test DB URL이 없어 `npm run test:integration` 5건 skip이지만, CI/staging DB secret이 있으면 알림/운영성 예외 route의 DB 증적을 남긴다.
- 2026-07-06 추가 release check: `backendOperatingDataFlow.test.ts`를 추가해 `ERP_TEST_DATABASE_URL` 기반 test DB에서 예산 조정, 지급 보류, 보고서 생성, 보고서 예약 생성/수정/중지, 즐겨찾기 생성/열기/삭제가 실제 backend route와 Prisma row에 연결되는지 검증하도록 했다. `coreModelCoverage`는 DB integration 소스가 `BudgetAdjustment`, `Disbursement`, `ReportRun`, `ReportDefinition`, `ReportSchedule`, `FavoriteItem`, `AuditLog`, `Notification` delegate를 직접 대사하는지 고정하며, `release:db-test-evidence` 필수 harness에도 포함한다. `npm run test:integration`은 test DB URL 없음으로 5건 skip, `npm run release:db-test-evidence`는 harness 7개 확인, `npm run test:unit`은 288건 통과, `npm run release:go-live-readiness`는 23장 63/67 P0 완료 및 전체 69개 open P0를 확인했다. `release:manifest` 입력도 release gate script까지 19개로 확장했고 최신 manifest hash는 `50d896c49957173f6671b687650cc805dc27453ad46081db69232f9bad81fef1`이다. 이로써 Prisma 핵심 모델의 schema/seed/route/service/integration 연결 P0는 완료 처리한다. 단, 실제 remote UI 전체 액션 후 test DB 대사는 별도 P0로 계속 남긴다.
- 2026-07-06 추가 release check: `remote-ui-persistence.test.mjs`를 추가해 `ERP_TEST_DATABASE_URL` 기반 test DB에서 backend test server와 Vite remote frontend를 기동하고, 브라우저 UI로 거래처 등록과 증빙 PDF 업로드를 수행한 뒤 새로고침/검색 후 거래처와 파일 metadata가 유지되는지 확인하도록 했다. 같은 테스트가 Prisma로 `Vendor`, `Attachment`, 계좌 암호화/마스킹, 거래처 생성 감사 로그를 직접 대사한다. 현재 로컬은 test DB URL이 없어 remote auth/UI persistence E2E 2건과 DB integration 5건이 skip이며, `node --test tests/e2e/remote-ui-persistence.test.mjs`, `npm --prefix backend run build`, `npm run test:unit`, `npm run test:e2e`, `npm run test:integration`, remote mode `npm run build`, `npm run release:frontend-artifact`, `npm run release:check`, `npm run release:verify-manifest` 통과를 확인했다.
- 2026-07-06 추가 release check: `goLiveReadiness.mjs`와 `npm run release:go-live-readiness`를 추가해 23/24/25장 P0 미완료 항목을 자동 집계한다. `READINESS_TARGET=production-candidate`는 23장과 25.1/25.2 P0가 열려 있으면 실패하고, `READINESS_TARGET=go-live`는 운영 시작 전 범위의 P0가 열려 있으면 실패하며, `READINESS_TARGET=stable-operation`은 go-live 이후 안정화 P0까지 포함한다. CI는 audit 모드로 미완료 P0를 매번 출력하고, `RELEASE_TARGET=production npm run release:check`는 production 후보 범위 미완료 P0가 있으면 실패하도록 연결했다.
- 2026-07-06 추가 release check: `scripts/verify-performance-capacity.mjs`와 `npm run release:performance-capacity`를 추가해 list `pageSize` 상한, shared pagination/filter/sort, 결제 요청 DB `skip/take/count`, report download backend 생성/감사 로그, Prisma list/report index, 10MB upload policy, API body/rate limit gate를 자동 대사한다. 기본 synthetic workload는 20,000건 목록 필터/정렬/page와 5,000행 보고서 다운로드 payload를 기준 시간/크기 안에서 실행하며, CI `Verify Performance Capacity`와 release manifest input에 연결했다. 실제 staging/prod row count, object storage 지연, WAF/API gateway 분산 rate limit은 25.3/25.4 배포 검증에서 별도 확인한다.
- 2026-07-06 추가 release check: `docs/user-manual.md`, `docs/admin-manual.md`, 신규 `docs/incident-response.md`, `docs/deployment-operations.md`를 실제 화면/버튼/API/운영 endpoint 기준으로 갱신하고 `scripts/verify-operational-docs.mjs`와 `npm run release:operational-docs`를 추가했다. 이 gate는 사용자/관리자/장애 대응/배포 문서, 버튼 액션 매핑, API spec, 이관 문서가 핵심 화면, 권한, 파일, 보고서, requestId, health/data-quality/business-failure alert, rollback, release gate를 빠뜨리지 않는지 검증하며 CI와 release manifest input에 연결했다. 역할별 UAT, go-live 승인 서명, 지원 연락망 확정은 실제 운영 책임자 증적이 필요하므로 별도 P0로 유지한다.
- 2026-07-06 추가 release check: `docs/go-live-handoff-template.md`와 `scripts/verify-go-live-handoff.mjs`, `npm run release:go-live-handoff`를 추가해 release identity, 역할별 UAT, known issue, 우회 절차, rollback 기준, 지원 연락망, 최종 sign-off 섹션을 audit한다. Production `release:check`는 strict mode로 handoff를 검증해 미확정 placeholder가 남아 있으면 실패하며, 실제 go-live에서는 `GO_LIVE_HANDOFF_PATH`로 완성된 handoff 파일을 지정해야 한다. 운영 문서 파일과 handoff template도 release manifest input hash에 포함했다.
- 2026-07-06 추가 release check: `docs/production-environment-inventory-template.md`와 `scripts/verify-production-environment-inventory.mjs`, `npm run release:environment-inventory`를 추가했다. CI는 production 환경 inventory 템플릿을 audit하고, Production `release:check`는 strict mode로 inventory를 검증해 배포 플랫폼, production 도메인, DB, object storage, secret manager, monitoring/structured logs/alerting, backup/PITR/WAL, CDN/WAF, rollback 증적에 미확정 placeholder가 남아 있으면 실패한다. `npm run release:environment-inventory`, `npm run release:operational-docs`, `npm run test:unit` 323건, `npm run release:go-live-readiness` 54 open P0, `npm run release:check`, `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 최신 로컬 manifest hash는 `5295d1a63d472a19a38b86e53b12b6e3b22596309ffef0f43525988eb0ef360c`이며 release-inputs는 37개 파일이다.
- 2026-07-06 추가 release check: production release candidate에서 `VITE_ERP_API_MODE=remote`를 강제하고 `VITE_ERP_API_BASE_URL`이 `EXPECTED_PRODUCTION_API_BASE_URL`과 정확히 일치해야 통과하도록 `scripts/verify-release-env.mjs`를 보강했다. `tests/unit/releaseEnvGate.test.ts`는 mock mode, loopback API, staging API 오지정, expected production API URL 미설정 실패를 검증한다.
- 2026-07-06 추가 release check: 민감 route 직접 호출 권한 가드 기준을 24.2 P0 증거로 반영했다. `tests/unit/backendRoutePermissionGuards.test.ts`는 결제 요청, 승인, 지급, 파일, 운영 알림, 데이터 품질, 예산 조정, 보고서, 거래처, 설정 권한 그룹/사용자 권한 route가 `requireAuth`, `FORBIDDEN`, backend permission predicate를 유지하는지 반복 검증한다.
- 2026-07-06 추가 release check: `tests/e2e/remote-ui-persistence.test.mjs`를 확장해 remote mode 브라우저에서 거래처 등록과 증빙 업로드를 수행한 뒤 새로고침과 두 번째 브라우저 로그인 후에도 거래처/첨부가 유지되는지 확인하고, Prisma로 `Vendor`, `Attachment`, `AuditLog`를 직접 대사한다. `scripts/verify-db-test-evidence.mjs`도 이 강화된 E2E 이름을 요구해 release evidence에서 약한 UI persistence 검증으로 되돌아가지 않게 했다.
- 2026-07-06 추가 release check: 같은 `remote-ui-persistence.test.mjs`에 결제 요청 생성, 결제 증빙 업로드, 제출, 승인자 2명 순차 승인, 두 번째 관리자 브라우저의 승인 완료 재조회, 지급 관리 보류 처리, 새로고침 후 보류 상태 유지, Prisma `PaymentRequest`/`ApprovalStep`/`Attachment`/`Disbursement`/감사 로그 대사를 추가했다. `scripts/verify-db-test-evidence.mjs`는 이 payment workflow remote UI E2E harness가 없으면 실패하며, `docs/button-action-map.md`, `docs/test-automation.md`, `docs/deployment-operations.md`에 해당 범위를 반영했다. 현재 로컬은 `ERP_TEST_DATABASE_URL`이 없어 `node --test tests/e2e/remote-ui-persistence.test.mjs` 3건 skip, `npm run release:db-test-evidence` audit pass로 확인했으며 실제 DB 실행 증적은 23.12/25.3 P0로 유지한다. `npm run test:unit` 323건, `npm run test:e2e` 2건 통과/4건 skip, `npm run release:operational-docs`, `npm run release:go-live-readiness` 54 open P0, `npm run release:check`, `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 최신 로컬 manifest hash는 `9036f700eb1a0eeba8fd383bcb6506ee1e72bb632b589288c83bbbb31de6507a`이며 release-inputs는 45개 파일이다.
- 2026-07-06 추가 release check: `docs/staging-smoke-evidence-template.md`, `scripts/verify-staging-smoke-evidence.mjs`, `npm run release:staging-smoke-evidence`를 추가해 동일 release artifact/migration, staging 환경 분리, health check, remote frontend, 결제 요청/첨부/승인/지급 보류/거래처/설정 권한/보고서/즐겨찾기 smoke, 새로고침/재로그인/다른 브라우저 유지, CSRF/signed URL/session 만료 보안 smoke, object storage/malware scanner 증빙을 production 승격 전 필수 문서로 묶었다. CI는 템플릿을 audit하고, `RELEASE_TARGET=production npm run release:check`는 strict staging smoke evidence 검증을 실행해 미확정 placeholder가 남아 있으면 실패한다. `npm run release:staging-smoke-evidence`, `npx tsx --test tests/unit/stagingSmokeEvidence.test.ts tests/unit/releaseEnvGate.test.ts tests/unit/ciReleaseGates.test.ts tests/unit/releaseManifest.test.ts`, `npm run release:operational-docs`, `npm run test:unit` 327건, `npm run release:check`, `npm run release:go-live-readiness` 54 open P0, `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 최신 로컬 manifest hash는 `2b4b07cbfe3a80eb7376e4c17a4242b13965c52a36e17dc8bce2de108b8576e1`이며 release-inputs는 47개 파일이다.
- 2026-07-06 추가 release check: `docs/backup-restore-rehearsal-template.md`, `scripts/verify-backup-restore-evidence.mjs`, `npm run release:backup-restore-evidence`를 추가해 RPO/RTO, PostgreSQL full backup, WAL/PITR, object storage versioning, report artifact backup, staging restore rehearsal, row count/총액/예산/거래처 지급 이력/첨부 orphan/data-quality 대사, migration failure/partial deploy/DB/object storage/API 장애 rollback rehearsal, backup alert/encryption/access/restore 계정 권한 증빙을 production 승격 전 필수 문서로 묶었다. CI는 템플릿을 audit하고, `RELEASE_TARGET=production npm run release:check`는 strict backup/restore evidence 검증을 실행해 미확정 placeholder가 남아 있으면 실패한다. `npm run release:backup-restore-evidence`, `npx tsx --test tests/unit/backupRestoreEvidence.test.ts tests/unit/releaseEnvGate.test.ts tests/unit/ciReleaseGates.test.ts tests/unit/releaseManifest.test.ts`, `npm run release:operational-docs`, `npm run test:unit` 331건, `npm run release:check`, `npm run release:go-live-readiness` 54 open P0, `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 최신 로컬 manifest hash는 `405b852e01beee1ea492992a0502ff5f9cbd40195ce85852e1601c91295a87c0`이며 release-inputs는 49개 파일이다.
- 2026-07-06 추가 release check: `docs/data-migration-evidence-template.md`, `scripts/verify-data-migration-evidence.mjs`, `npm run release:data-migration-evidence`를 추가해 원천 시스템, 사용자/부서/권한/거래처/계좌/예산/미결 결제/첨부 metadata 이관 범위, freeze window, 컬럼 매핑, validation query, staging rehearsal, production row count/상태별 집계/총액/예산 잔액/거래처 지급 이력/첨부 orphan 대사, mock/local seed/test marker 제거, 계좌 암호화/마스킹, 개인정보 접근 권한, rollback/rerun, 담당자 승인 증빙을 production 승격 전 필수 문서로 묶었다. CI는 템플릿을 audit하고, `RELEASE_TARGET=production npm run release:check`는 strict data migration evidence 검증을 실행해 미확정 placeholder가 남아 있으면 실패한다. `npm run release:data-migration-evidence`, `npx tsx --test tests/unit/dataMigrationEvidence.test.ts tests/unit/releaseEnvGate.test.ts tests/unit/ciReleaseGates.test.ts tests/unit/releaseManifest.test.ts`, `npm run release:operational-docs`, `npm run test:unit` 335건, `npm run release:check`, `npm run release:go-live-readiness` 54 open P0, `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 최신 로컬 manifest hash는 `26752cddf74dd063a923c7113cc331194395cefa79f59c837697e685569d7806`이며 release-inputs는 51개 파일이다.
- 2026-07-06 추가 release check: `docs/role-uat-evidence-template.md`, `scripts/verify-role-uat-evidence.mjs`, `npm run release:role-uat-evidence`를 추가해 요청자/승인자/재무팀/관리자/외부 감사 실제 계정과 권한 경계, 파일럿 부서/기간, 실제 금액 지급 전 dry-run/제한 금액/테스트 계좌 정책, 결제 요청/첨부/예산/승인/반려/보류/지급/거래처/설정/보고서/감사 조회 UAT 시나리오, P0/P1 이슈 처리, 교육/지원, 최종 책임자 sign-off 증빙을 production 승격 전 필수 문서로 묶었다. CI는 템플릿을 audit하고, `RELEASE_TARGET=production npm run release:check`는 strict role UAT evidence 검증을 실행해 미확정 placeholder가 남아 있으면 실패한다. `npm run release:role-uat-evidence`, `npx tsx --test tests/unit/roleUatEvidence.test.ts tests/unit/releaseEnvGate.test.ts tests/unit/ciReleaseGates.test.ts tests/unit/releaseManifest.test.ts tests/unit/goLiveHandoff.test.ts`, `npm run release:operational-docs`, `npm run release:go-live-handoff`, `npm run test:unit` 339건, `npm run release:check`, `npm run release:go-live-readiness` 54 open P0, `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 최신 로컬 manifest hash는 `d337502113f8b77180015d17fd507653811989842671f5d4e7f3c300aa7df744`이며 release-inputs는 53개 파일이다.
- 2026-07-06 추가 release check: `docs/production-go-live-evidence-template.md`, `scripts/verify-production-go-live-evidence.mjs`, `npm run release:production-go-live-evidence`를 추가해 release version/source ref/git commit, manifest/migration review hash, frontend/backend/migration/env checksum, production migration deploy, backend health, frontend smoke, 업무 smoke, open P0/예외 승인, rollback 기준/담당자/예상 시간/사용자 공지, change freeze/incident channel/status cadence/hypercare, 최종 production sign-off 증빙을 production 승격 전 필수 문서로 묶었다. CI는 템플릿을 audit하고, `RELEASE_TARGET=production npm run release:check`는 strict production go-live evidence 검증을 실행해 미확정 placeholder가 남아 있으면 실패한다. `npm run release:production-go-live-evidence`, `npx tsx --test tests/unit/productionGoLiveEvidence.test.ts tests/unit/releaseEnvGate.test.ts tests/unit/ciReleaseGates.test.ts tests/unit/releaseManifest.test.ts`, `npm run release:operational-docs`, `npm run release:go-live-handoff`, `npm run test:unit` 343건, `npm run release:check`, `npm run release:go-live-readiness` 54 open P0, `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 최신 로컬 manifest hash는 `f2faf10014a2ab2ed5c2632d42de6359c364ce542ddedc68d70b308e753fecc3`이며 release-inputs는 55개 파일이다.
- 2026-07-06 추가 release check: `docs/post-go-live-stabilization-evidence-template.md`, `scripts/verify-post-go-live-stabilization-evidence.mjs`, `npm run release:post-go-live-stabilization-evidence`를 추가해 운영 첫 주 로그인 실패/API 5xx/승인 실패/지급 실패/파일 업로드 실패/보고서 실패 daily check, 첫 지급 은행 결과/ERP 상태/AuditLog/거래처 지급 이력/report totals 대사, go-live 이후 production data backup/PITR, severity 기준, P0/P1 same-day response, requestId 문의 접수, hypercare 처리 건수/실패 건수/평균 처리 시간/주요 inquiry/remediation, 2주차 backlog review, 기능/보안/재무/운영 sign-off 증빙을 안정화 판정 전 필수 문서로 묶었다. CI는 템플릿을 audit하고, `RELEASE_TARGET=production npm run release:check`는 strict post go-live stabilization evidence 검증을 실행해 미확정 placeholder가 남아 있으면 실패한다. `npm run release:post-go-live-stabilization-evidence`, `npx tsx --test tests/unit/postGoLiveStabilizationEvidence.test.ts tests/unit/releaseEnvGate.test.ts tests/unit/ciReleaseGates.test.ts tests/unit/releaseManifest.test.ts` 26건, `npm run release:operational-docs` 23건, `npm run test:unit` 347건, `npm run release:check`, `npm run release:go-live-readiness` 54 open P0, `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 최신 로컬 manifest hash는 `ca0e5fe53eba7e5da51a53606755f46298a8d768b809ee4a4fb772addee5c86f`이며 release-inputs는 57개 파일이다.
- 2026-07-06 추가 release check: `docs/final-acceptance-evidence-template.md`, `scripts/verify-final-acceptance-evidence.mjs`, `npm run release:final-acceptance-evidence`를 추가해 실제 production 사용자 결제 요청/증빙/승인자 처리/재무팀 지급 전 단계/보고서 업무, DB/object storage 저장과 새로고침/재로그인/다른 기기 유지, 권한 없는 사용자 UI/API 차단, AuditLog/security_events/requestId, 중복 승인/중복 지급/승인 전 지급/마감 후 변경/계좌 불일치 지급 backend 차단, rollback/복구/읽기 전용/사용자 공지, 배포/모니터링/백업/장애 대응/사용자 지원 운영 인수, KPI/오류율, backlog/운영 릴리즈 계획, 최종 sign-off 증빙을 실사용 가능 최종 판정 전 필수 문서로 묶었다. CI는 템플릿을 audit하고, `RELEASE_TARGET=production npm run release:check`는 strict final acceptance evidence 검증을 실행해 미확정 placeholder가 남아 있으면 실패한다. `npm run release:final-acceptance-evidence`, `npx tsx --test tests/unit/finalAcceptanceEvidence.test.ts tests/unit/releaseEnvGate.test.ts tests/unit/ciReleaseGates.test.ts tests/unit/releaseManifest.test.ts` 27건, `npm run release:operational-docs` 24건, `npm run test:unit` 351건, `npm run release:check`, `npm run release:go-live-readiness` 54 open P0, `npm run release:manifest`, `npm run release:verify-manifest` 통과를 확인했다. 최신 로컬 manifest hash는 `668275133d7c6ec5ed5b467cd8e0f7defc53377e00461a8aff0f951c1fbdb13e`이며 release-inputs는 59개 파일이다.
- 2026-07-06 추가 release evidence: `scripts/generate-go-live-readiness-report.mjs`와 `npm run release:go-live-readiness-report`를 추가해 `release/go-live-readiness-report.json`/`.md`에 23/24/25장 전체 open P0, 목표별 차단 상태, section, 필요 증빙 category를 모두 남기도록 했다. 기존 `release:go-live-readiness` CLI preview는 25개까지만 출력하지만, 이 report는 현재 54개 open P0 전체를 보관한다. CI는 `Generate Go-Live Readiness Report` 단계를 실행하고 `erp-release-evidence` artifact에 readiness report JSON/Markdown을 함께 업로드한다. `npx tsx --test tests/unit/goLiveReadinessReport.test.ts tests/unit/ciReleaseGates.test.ts tests/unit/releaseManifest.test.ts tests/unit/operationalDocs.test.ts` 12건, `npm run release:operational-docs` 24건, `npm run test:unit` 354건, `npm run release:check`, `npm run release:go-live-readiness`, `npm run release:go-live-readiness-report`, `npm run release:manifest` 통과를 확인했다. 최신 로컬 manifest hash는 `b24547f401cbd394d72cc27525b76d959a60f214e86f3e7eef32a93439f37ce2`이며 release-inputs는 60개 파일이다.
- 2026-07-06 추가 release evidence hardening: release manifest에 `release-evidence` 섹션을 추가해 `release/migration-review.json`, `release/go-live-readiness-report.json`, `release/go-live-readiness-report.md`가 checksum manifest에 포함되도록 했다. Readiness report에는 원천 `erp-system-checklist.md`의 SHA-256을 기록해 전체 blocker 목록이 어떤 체크리스트 기준인지 증명한다. Manifest hash를 체크리스트 본문에 다시 쓰면 checklist/report/manifest 간 순환이 생기므로, 최신 hash는 `release/release-manifest.json`과 `npm run release:verify-manifest` 출력으로 확인한다.
- 2026-07-06 추가 release gate hardening: `RELEASE_TARGET=production npm run release:check`가 `EXPECTED_RELEASE_MANIFEST_SHA256`와 `EXPECTED_RELEASE_SOURCE_REF`를 필수로 요구하고, `release/release-manifest.json`의 `release-evidence` 섹션이 현재 `release/migration-review.json`, `release/go-live-readiness-report.json`, `release/go-live-readiness-report.md` 및 산출물과 일치하는지 재검증하도록 묶었다. 운영자가 `release:verify-manifest`를 별도로 빠뜨려도 production 후보 gate에서 stale artifact, 다른 release ref, 다른 staging manifest hash를 차단한다. 최신 manifest hash는 순환 증적을 피하기 위해 이 체크리스트가 아니라 `release/release-manifest.json`과 검증 출력으로 확인한다.
- 2026-07-06 추가 staging promotion hardening: `scripts/verify-staging-smoke-evidence.mjs` strict mode가 placeholder 제거뿐 아니라 `Release manifest hash`와 `EXPECTED_RELEASE_MANIFEST_SHA256 promotion hash`의 64자리 SHA-256 형식 및 상호 일치, `Migration review hash` 형식, `VITE_ERP_API_MODE=remote`, `Production promotion decision=approved`, `Open blocker count=0`을 검증하도록 강화했다. Production `release:check`에서 staging smoke 증적이 이 조건을 통과하지 못하면 같은 artifact promotion 증적으로 인정하지 않는다. 실제 staging 환경에서 smoke를 수행한 증적은 아직 open P0로 유지한다.
- 2026-07-06 추가 production inventory hardening: `scripts/verify-production-environment-inventory.mjs` strict mode가 placeholder 제거뿐 아니라 release/migration hash 형식, HTTPS non-local production frontend/API domain, `VITE_ERP_API_BASE_URL`/`EXPECTED_PRODUCTION_API_BASE_URL` 일치, `FRONTEND_ORIGIN`/`EXPECTED_PRODUCTION_FRONTEND_ORIGIN` 일치, `DATABASE_URL` 및 application secret의 secret manager reference, PostgreSQL TLS 정책, S3 HTTPS endpoint/bucket/public block/encryption, backend scaling, body/rate limit, external malware scanner endpoint를 검증하도록 강화했다. 실제 production 플랫폼/도메인/DB/storage/secret/monitoring 값은 아직 없으므로 25.1 P0는 open으로 유지한다.
- 2026-07-06 추가 release identity runtime check: backend `/api/health/version`을 추가해 `RELEASE_VERSION`, `RELEASE_SOURCE_REF`, `RELEASE_GIT_COMMIT`, `RELEASE_MANIFEST_SHA256`를 노출하고 누락/형식 오류 시 503으로 응답하도록 했다. Frontend는 `VITE_RELEASE_VERSION`, `VITE_RELEASE_SOURCE_REF`, `VITE_RELEASE_GIT_COMMIT`와 backend version health를 비교하는 `verifyRemoteReleaseIdentity()` helper를 제공한다. Production `release:check`는 frontend/backend release identity env가 누락되거나 불일치하면 실패하며, CI frontend build/backend smoke에도 동일 GitHub sha/ref를 주입한다. 실제 staging/prod endpoint 응답 증적은 아직 open P0로 유지한다.
- 2026-07-06 추가 release check: production 보안 기준 회귀 테스트를 추가해 session cookie가 `HttpOnly`, `Secure`, `SameSite=Lax`, CSRF cookie가 `Secure`, `SameSite=Lax`로 발급되는지 고정했다. `scripts/verify-release-env.mjs`는 production release candidate에서 `FRONTEND_ORIGIN`이 `EXPECTED_PRODUCTION_FRONTEND_ORIGIN`과 일치해야 통과하며, unit test는 local/wildcard/non-HTTPS CORS origin과 staging origin 오지정을 실패로 검증한다. 실제 staging/prod 도메인에서의 보안 smoke는 25.3/25.4 P0로 계속 남긴다.
- 2026-07-06 추가 release check: 파일 업로드 보안 정책을 `backend/src/security/attachmentPolicy.ts`로 분리해 PDF/JPG/PNG/XLSX 확장자와 `Content-Type` 일치, 10MB 제한, scan 상태(`pending`, `clean`, `blocked`)를 같은 기준으로 검증한다. signed upload 본문도 사전 등록 `Content-Type`과 다르면 거부하고, malware scan blocked 결과는 object storage에 본문을 쓰지 않은 채 `Attachment.checksum=blocked:*`, `scan_blocked` 감사 로그, `file_malware_blocked` 보안 이벤트로 quarantine 처리한다. `tests/unit/backendFileSecurity.test.ts`는 정책과 route 연결을 회귀 테스트로 고정한다.
- 2026-07-06 추가 release check: `scripts/mutationSafetyCatalog.mjs`와 `npm run release:mutation-safety`를 추가해 backend `POST/PATCH/PUT/DELETE` 51개 route 전체를 표준 mutation, 위임 route, 읽기 전용 reject, 승인된 예외로 분류하고 `idempotencyKey`, 감사 로그, rowVersion/조건부 update/최신 audit id 증거를 자동 검증한다. 외부 연동 테스트 `POST /settings/integrations/{integrationId}/test`도 `idempotencyKey` 필수, 감사 로그 replay/conflict, frontend/mock stable key 전송으로 보강했으며 CI release gate에 mutation safety 검증을 추가했다.
- 2026-07-06 추가 release check: `scripts/verify-db-test-evidence.mjs`와 `npm run release:db-test-evidence`를 추가해 DB integration test와 remote browser E2E harness 존재, test DB URL 안전성, CI release tag strict mode를 검증한다. 로컬 audit 모드는 `ERP_TEST_DATABASE_URL` 부재를 warning으로만 보고하지만, `v*` tag release candidate에서는 `REQUIRE_DB_TEST_EVIDENCE=true`로 실행되어 폐기 가능한 PostgreSQL test DB가 없으면 실패한다. 실제 test DB로 remote UI/E2E를 실행한 증적은 아직 23.12/23.13 P0로 남긴다.
- 2026-07-06 추가 DB evidence hardening: `scripts/generate-db-test-evidence.mjs`와 `npm run release:db-test-evidence-run`을 추가해 폐기 가능한 PostgreSQL test DB에서 `npm run test:integration`, remote auth E2E, remote UI persistence E2E를 실제 실행하고 `release/db-test-evidence.json`을 생성하도록 했다. Strict `release:db-test-evidence`는 이 증적의 release identity, DB URL fingerprint, 하네스 SHA-256, 명령 exit status, 필수 테스트명 출력, skip 없는 통과 상태를 검증한다. CI `v*` release candidate는 실행 후 검증 순서로 바뀌었고, stale 하네스 또는 skip된 DB/UI 검증은 통과하지 못한다. 실제 `ERP_TEST_DATABASE_URL`이 있는 환경에서 생성한 증적은 아직 없으므로 23.12/25.3 P0는 open으로 유지한다.
- 2026-07-06 추가 production go-live evidence hardening: `scripts/verify-production-go-live-evidence.mjs` strict mode가 placeholder 제거뿐 아니라 release manifest hash/`EXPECTED_RELEASE_MANIFEST_SHA256` 64자리 SHA-256 형식과 상호 일치, migration review hash 형식, release ref/commit/version env 일치, `VITE_ERP_API_MODE=remote`, production HTTPS API/frontend URL, `/api/health` 계열과 frontend/business smoke의 pass-like 결과, numeric open P0 count와 승인 예외, evidence link, 최종 sign-off 시각/증적 링크를 검증하도록 강화했다. 실제 production 배포/health/smoke/sign-off 증적은 아직 없으므로 25.7/25.9 P0는 open으로 유지한다.

우선순위:

- P0: 실사용 배포 전 반드시 완료해야 하는 release/go-live 차단 조건
- P1: 파일럿과 초기 운영 안정성을 높이는 전환 조건
- P2: 장기 운영 편의와 개선 관리 조건

### 25.1 현재 배포 가능성 판정

- [x] P0: 현재 상태를 `로컬 mock/문서화`, `remote staging 후보`, `production 후보`, `실사용 가능` 중 하나로 명시하고 근거 기록
- [x] P0: 23장 P0 데이터 연동 항목이 모두 완료되기 전에는 production 배포 후보로 분류하지 않음
- [x] P0: 24장 P0 운영/보안/재무 통제 항목이 모두 완료되기 전에는 실사용 승인 금지
- [ ] P0: 배포 대상 플랫폼, production 도메인, DB, object storage, secret manager, monitoring 도구를 확정
  - 진행 메모(2026-07-06): `docs/production-environment-inventory-template.md`와 `npm run release:environment-inventory`를 추가했다. CI는 템플릿 구조를 audit하고, `RELEASE_TARGET=production npm run release:check`는 strict inventory 검증을 실행해 배포 플랫폼, production 도메인, DB, object storage, secret manager, monitoring/structured logs/alerting, backup/PITR/WAL, CDN/WAF, rollback 증적에 `TBD`, `pending`, `<...>` 같은 미확정 값이 남아 있으면 실패한다. 실제 운영 플랫폼과 계정/도메인/스토리지/모니터링 증적이 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: 실제 배포 환경에서 frontend `remote` mode와 backend API가 같은 release version을 바라보는지 확인
  - 진행 메모(2026-07-06): `docs/staging-smoke-evidence-template.md`와 `npm run release:staging-smoke-evidence`를 추가해 staging frontend/backend artifact version, `VITE_ERP_API_MODE=remote`, `VITE_ERP_API_BASE_URL`, backend `/api/health/version`, promotion manifest hash를 증빙하도록 했다. Backend `/api/health/version`은 `RELEASE_VERSION`/`RELEASE_SOURCE_REF`/`RELEASE_GIT_COMMIT`/`RELEASE_MANIFEST_SHA256`를 노출하고, frontend `verifyRemoteReleaseIdentity()`는 `VITE_RELEASE_VERSION`/`VITE_RELEASE_SOURCE_REF`/`VITE_RELEASE_GIT_COMMIT`와 backend 값을 비교한다. Production `release:check`는 frontend/backend release identity env가 누락되거나 불일치하면 실패한다. 실제 staging/prod 배포 endpoint 응답과 증빙 파일은 아직 없으므로 완료 처리는 보류한다.
- [x] P1: 배포 가능성 판정표를 release마다 갱신하고 미충족 사유를 backlog와 연결
  - 진행 메모(2026-07-08): `docs/release-readiness-decision.md`를 추가하고 `release:go-live-readiness-report` 결과를 release별 open blocker, 예외 승인, backlog owner/deadline, go/no-go 결정에 연결하는 판정표로 정리했다.

### 25.2 Release Candidate 생성

- [x] P0: release branch 또는 tag 기준으로 frontend/backend artifact를 고정하고 동일 artifact를 staging과 production에 사용
  - 증거: `release:manifest`가 frontend/backend/migration/release input checksum과 함께 `sourceRef`를 기록하고, CI는 `RELEASE_SOURCE_REF=${{ github.ref_name }}`로 manifest를 생성한다. `release:verify-manifest`는 `EXPECTED_RELEASE_SOURCE_REF`와 `EXPECTED_RELEASE_MANIFEST_SHA256`을 대사해 release branch/tag와 checksum이 다른 산출물의 promotion을 차단한다. `tests/unit/releaseManifest.test.ts`와 `tests/unit/releaseManifestVerify.test.ts`가 이 고정을 회귀 테스트로 검증한다.
- [x] P0: frontend/backend/migration release manifest와 checksum을 생성하고 CI artifact로 보관
- [x] P0: release manifest를 현재 산출물과 재검증하고 `EXPECTED_RELEASE_MANIFEST_SHA256`으로 staging-to-production 승격 checksum을 고정
- [x] P0: CI가 `main`과 `v*` tag push에서 release evidence를 생성하고 manifest git commit을 `github.sha`와 대사
- [x] P0: `npm test`, `npm run build`, `npm --prefix backend run build`, `npm --prefix backend run db:generate` 통과
- [x] P0: Prisma schema 기준 초기 migration SQL과 migration lock 파일 생성
- [x] P0: migration diff, backward compatibility, rollback 영향, seed 미실행 여부를 release 승인 전에 검토
  - 증거: `release:migration-check`가 CI에서 `REQUIRE_SHADOW_DATABASE_URL=true`로 Prisma migration diff dry-run과 static compatibility guard를 요구한다. `release:migration-review`는 migration SQL checksum, operation, rollback 영향, backward compatibility, production seed 차단 증거를 `release/migration-review.json`에 기록하고, `release:verify-migration-review`가 현재 migration과 review hash/release version/rollback review/seed policy를 재대사해 stale review나 미검토 migration을 차단한다. 실제 staging DB deploy, DBA/운영 승인, backup/PITR 리허설은 24.1/24.6/25.3 P0로 별도 유지한다.
- [x] P0: migration review JSON을 생성해 현재 migration의 하위 호환 static guard, rollback 영향, seed 차단 정책을 release evidence로 보관
- [x] P0: migration static guard와 Prisma schema validate를 CI release gate에 추가
- [x] P0: frontend bundle에 mock-only debug UI, 테스트 계정, 샘플 secret, local API endpoint가 섞이지 않았는지 검사
- [x] P0: frontend bundle artifact scanner를 CI frontend build 이후 실행하도록 설정
- [x] P0: staging/production release gate에서 mock mode, localhost API/DB, 기본 파일 secret, local file storage, DEV 로그인 비밀번호를 자동 차단
- [x] P0: staging/production release gate에서 `ALLOW_PRODUCTION_SEED`와 production 진입점의 static mock fixture import를 자동 차단
- [x] P0: staging/production release gate에서 API body limit과 rate limit 비활성화 여부를 자동 검증
- [x] P0: backend production build가 `node dist/server.js`로 실행되고 health check를 통과하는지 검증
- [x] P1: release note에 기능 변경, DB 변경, 권한 변경, 운영 영향, known issue, rollback 조건 기재
  - 진행 메모(2026-07-08): `docs/release-note-template.md`, `scripts/verify-release-note.mjs`, `npm run release:release-note`를 추가하고 CI, production `release:check`, release manifest input에 연결했다. Production strict mode는 `RELEASE_NOTE_PATH`가 없거나 기능 변경/DB 변경/권한 변경/운영 영향/known issue/rollback 조건/승인 섹션에 미확정 placeholder가 남으면 실패한다.
- [x] P2: artifact checksum, SBOM 또는 dependency license/security report 보관

### 25.3 Staging 배포 및 원격 검증

- [ ] P0: staging DB, object storage, auth, secret, 도메인을 production과 분리된 실제형 환경으로 구성
  - 진행 메모(2026-07-06): staging smoke evidence 템플릿에 staging frontend/backend domain, DB service, object storage bucket, secret manager project, auth/session store, production resource separation evidence를 필수 증빙으로 추가했다. 실제 staging 인프라 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: staging에 DB migration을 먼저 적용하고 `/api/health`, `/api/health/db`, storage health, job health를 확인
  - 진행 메모(2026-07-06): staging smoke evidence 템플릿과 gate가 DB migration command/result, `/api/health`, `/api/health/db`, `/api/health/storage`, `/api/health/file-security`, `/api/health/jobs`, `/api/health/integrations`, `/api/operations/data-quality`, requestId/log evidence를 요구하도록 했다. 실제 staging 실행 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: staging frontend를 `VITE_ERP_API_MODE=remote`로 배포하고 mock fallback 없이 동작하는지 검증
  - 진행 메모(2026-07-06): staging smoke evidence 템플릿에 remote mode confirmation, mock fallback disabled evidence, browser console error check, network API base URL check를 추가했고 Production release gate strict 검증에 연결했다. 실제 staging 프론트 배포 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: 결제 요청 생성, 첨부 업로드, 승인, 지급 보류/실행 전 단계, 거래처 등록, 설정 권한 변경, 보고서 생성/다운로드 smoke test 수행
  - 진행 메모(2026-07-06): test DB 기반 remote UI E2E 하네스가 거래처 등록/증빙 업로드, 결제 요청 생성/증빙/제출, 승인자 순차 승인, 지급 보류, 설정 권한 그룹 추가, 보고서 생성, 즐겨찾기 추가를 브라우저에서 수행하도록 확장됐다. 이어서 staging smoke evidence gate를 추가해 동일 artifact staging 환경에서 해당 업무 smoke 실행 증빙이 없으면 production release gate가 실패하게 했다. 다만 staging DB/object storage/scanner와 동일 artifact 배포 환경에서 실행한 증적은 아직 없으므로 staging smoke P0는 계속 미완료다.
- [ ] P0: staging에서 새로고침/재로그인/다른 브라우저 접속 후 데이터 유지 검증
  - 진행 메모(2026-07-06): staging smoke evidence 템플릿이 새로고침 후 거래처/첨부 유지, 재로그인 후 결제 요청 상태 유지, 다른 브라우저 접속 후 승인/설정 권한 유지, Prisma DB row evidence, file/object storage metadata evidence, AuditLog evidence를 요구하도록 했다. 실제 staging 실행 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: staging에서 API 직접 호출 권한 우회, CSRF, 파일 직접 접근, session 만료를 보안 smoke로 확인
  - 진행 메모(2026-07-06): staging smoke evidence 템플릿이 API 직접 호출 권한 우회 차단, CSRF 거부, signed URL 직접 접근 차단, session 만료, Secure/HttpOnly/SameSite cookie, CORS allowlist, security event requestId evidence를 요구하도록 했다. 실제 staging 보안 smoke 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P1: staging synthetic monitoring을 go-live 전 최소 24시간 운영하고 오류율/latency 기준 통과
- [ ] P1: 운영자와 재무팀이 staging에서 실제 업무 시나리오를 end-to-end로 수행

### 25.4 Production 인프라 준비

- [ ] P0: production DB 생성, migration 전 backup, 접근 권한, audit 설정, PITR/WAL 보관을 확인
- [ ] P0: production object storage bucket 비공개, versioning, lifecycle, signed URL 정책, malware scan 경로 확인
- [ ] P0: production secret은 저장소 파일이 아닌 secret manager/hosting env에 등록하고 접근 권한을 제한
- [ ] P0: backend runtime은 Node.js 22+ production mode, process manager/hosting health restart, structured log 수집을 사용
- [ ] P0: frontend 정적 호스팅에 HTTPS, cache-control, rollback 가능한 versioned artifact 배포 구조 적용
  - 진행 메모(2026-07-08): `public/_headers`와 `docs/frontend-hosting-policy.md`를 추가하고 `npm run release:frontend-artifact`가 build artifact의 `_headers`에서 HSTS, nosniff, `index.html` no-store, hashed asset immutable cache 정책을 확인하도록 보강했다. 실제 hosting platform의 HTTPS redirect/cache header 적용과 rollback artifact 보관 증빙은 production inventory/go-live evidence에서 확인해야 하므로 항목은 배포 증빙 전까지 미완료로 유지한다.
- [ ] P0: CORS, cookie domain, secure cookie, sameSite, API base URL이 production 도메인 기준으로 맞는지 검증
- [ ] P1: production rate limit, request body limit, upload size limit, timeout, slow query threshold 적용
  - 진행 메모(2026-07-08): production inventory strict 검증에 WAF/API gateway 같은 분산 rate limit layer, `API_BODY_LIMIT_BYTES`, `RATE_LIMIT_WINDOW_MS`, `RATE_LIMIT_MAX`, API request timeout policy, `SLOW_QUERY_MS`를 추가했다. 실제 production WAF/gateway/runtime 적용 증빙이 있어야 완료 처리한다.
- [ ] P1: 운영자 break-glass 계정과 일반 관리자 계정을 분리하고 사용 기록을 남김
  - 진행 메모(2026-07-08): `docs/production-environment-inventory-template.md`와 `scripts/verify-production-environment-inventory.mjs`에 일반 관리자 계정 정책, break-glass 계정 reference, time-boxed multi-approval, audit evidence, revoke evidence strict 검증을 추가했다. 실제 계정 생성/권한 회수/사용 기록 증빙은 production inventory 완성본에서 확인해야 하므로 완료 처리는 보류한다.

### 25.5 실데이터 이관 및 Cutover 준비

- [ ] P0: 사용자, 부서, 권한, 거래처, 계좌, 예산, 미결 결제 요청, 첨부 metadata 이관 범위 확정
  - 진행 메모(2026-07-06): data migration evidence 템플릿에 사용자, 부서, 권한, 거래처/계좌, 예산, 미결 결제 요청, 첨부 metadata별 source system, scope, column mapping, validation query 증빙 항목을 추가했다. 실제 이관 범위 승인 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: 이관 전 원천 데이터 freeze window, 변경 금지 시간, 담당자 승인, rollback 조건 확정
  - 진행 메모(2026-07-06): data migration evidence 템플릿이 freeze window start/end, freeze exception approver, change ban communication, rollback condition, rollback owner, cutover abort condition, rerun strategy를 요구하도록 했다. 실제 cutover 승인 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: staging에서 이관 rehearsal을 수행하고 건수, 총액, 상태별 집계, 예산 잔액, 지급 이력 대사 통과
  - 진행 메모(2026-07-06): data migration evidence 템플릿이 staging source extract checksum, load result, row count/status aggregate/payment total/budget balance/vendor payment history/attachment metadata/data-quality 대사를 요구하고 production release gate strict 검증에 연결했다. 실제 staging rehearsal 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: production 이관 직후 mock/local seed/test 데이터가 존재하지 않는지 확인
  - 진행 메모(2026-07-06): data migration evidence 템플릿에 production mock/local seed/test marker check와 mockData, local seed, test email, test account, sample/mock/demo text, sample attachment 점검 증빙을 추가했다. 실제 production 이관 직후 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: 이관 데이터의 개인정보/계좌정보 암호화, 마스킹, 접근 권한을 검증
  - 진행 메모(2026-07-06): data migration evidence 템플릿에 bank account encryption/masking verification, personal data access permission verification, raw account export restriction, migration credential secret-manager reference, migration file retention/deletion evidence를 추가했다. 실제 개인정보/계좌정보 이관 검증 증적은 아직 없으므로 완료 처리는 보류한다.
- [x] P0: 이관 품질 점검 API와 운영 전 대사 기준 문서화
- [x] P1: 이관 오류 목록과 수동 보정 절차, 보정 후 감사 로그 기록 기준 마련
  - 진행 메모(2026-07-08): `docs/cutover-runbook.md`에 failed row quarantine, 수동 보정 approval flow, before/after 대사, AuditLog 정정 기록, break-glass 금지/예외 기준을 추가했다.
- [x] P1: cutover 당일 체크리스트, 담당자 연락망, 단계별 예상 소요 시간 작성
  - 진행 메모(2026-07-08): `docs/cutover-runbook.md`에 cutover commander, data migration owner, DBA, security/finance/operations/support owner 연락망과 T-7/T-2/T-1/T-0/load/reconciliation/business smoke/go-no-go 단계별 예상 시간을 작성했다.

### 25.6 파일럿 및 UAT

- [ ] P0: 요청자, 승인자, 재무팀, 관리자, 외부 감사 역할별 UAT 계정을 실제 권한으로 준비
  - 진행 메모(2026-07-06): role UAT evidence 템플릿이 각 역할별 실제 계정 또는 증적 ID, 권한 검증, 승인자를 요구하도록 했다. 실제 계정 준비/승인 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: pilot 대상 부서와 기간을 정하고 실제 금액 지급 전까지의 전체 업무 흐름을 검증
  - 진행 메모(2026-07-06): role UAT evidence 템플릿이 pilot 대상 부서, 사용자 수, 기간, 실제 금액 지급 전 통제 기준, 제한 금액 또는 테스트 계좌 정책, 은행 송금 dry-run, pilot exit criteria를 요구하도록 했다. 실제 파일럿 실행 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: 결제 요청, 승인, 반려, 보류, 거래처 등록, 첨부, 예산 확인, 보고서, 설정 변경의 UAT 증적을 보관
  - 진행 메모(2026-07-06): role UAT evidence 템플릿에 결제 요청 생성, 증빙 첨부, 예산 확인, 승인/반려/보류, 지급 보류/dry-run, 은행 이체 파일, 거래처 등록, 권한 그룹/사용자 권한/시스템 설정, 외부 연동 테스트, 감사/보고서 read-only, requestId/AuditLog 증빙 항목을 추가했다. 실제 UAT 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: 지급 실행은 실제 은행 송금 전 dry-run 또는 제한 금액/테스트 계좌 정책으로 통제
  - 진행 메모(2026-07-06): role UAT evidence 템플릿이 지급 실행 전 dry-run, 제한 금액 또는 테스트 계좌 정책, 은행 송금 dry-run, 2인 확인/직무 분리 확인을 요구하도록 했다. 실제 dry-run/테스트 계좌 정책 승인 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: UAT 중 발견된 P0/P1 이슈는 go-live 전 해결 또는 책임자 예외 승인 없이는 이월 금지
  - 진행 메모(2026-07-06): role UAT evidence 템플릿이 P0 issue count/resolution evidence, P1 issue count/exception approval, known issue handoff link, go-live exception approver를 요구하도록 했다. 실제 이슈 처리/예외 승인 증적은 아직 없으므로 완료 처리는 보류한다.
- [x] P1: 사용자 교육, 운영 FAQ, 오류 신고 양식, requestId 전달 방법을 파일럿 참여자에게 안내
  - 진행 메모(2026-07-08): `docs/user-training-faq.md`를 역할별 파일럿 교육 자료와 오류 신고 양식으로 추가했고, `docs/admin-manual.md`에서 운영자가 해당 문서를 인수하도록 연결했다.
- [ ] P1: 파일럿 만족도, 업무 처리 시간, 오류 빈도, 문의 유형을 수집해 go-live 전 반영
  - 진행 메모(2026-07-08): `docs/role-uat-evidence-template.md`와 `npm run release:role-uat-evidence`에 `Pilot Feedback Metrics` 섹션을 추가해 파일럿 만족도 점수, 업무 처리 시간 기준/실측, 오류 빈도, 문의 유형, go-live 전 반영 결정, backlog/변경 증빙을 production strict gate에서 요구하도록 했다. 실제 파일럿 수집 결과가 있어야 완료 처리한다.

### 25.7 Go-Live 실행

- [ ] P0: go-live 직전 release version, migration version, frontend artifact, backend artifact, env checksum을 기록
  - 진행 메모(2026-07-06): `docs/production-go-live-evidence-template.md`와 `npm run release:production-go-live-evidence`를 추가해 release version/source ref/git commit, manifest hash, migration review hash, frontend/backend/migration/release input checksum, production env checksum, secret manager version set을 production 승격 전 필수 증빙으로 묶었다. 실제 production 배포 직전 checksum 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: production migration 적용 후 API health, DB health, storage health, auth health, 핵심 smoke test 통과
  - 진행 메모(2026-07-06): production go-live evidence 템플릿이 production DB backup, migration deploy command/result, applied migration version, `/api/health`, `/api/health/db`, `/api/health/storage`, `/api/health/file-security`, `/api/health/jobs`, `/api/health/integrations`, `/api/operations/alerts`, `/api/operations/business-failure-alerts`, `/api/operations/data-quality`, requestId/log evidence를 요구하도록 했다. 실제 production migration/health 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: production frontend 배포 후 로그인, 메뉴 권한, 결제 요청 목록, 첨부 접근, 알림, 보고서 다운로드 확인
  - 진행 메모(2026-07-06): production go-live evidence 템플릿이 production frontend URL, login smoke, menu permission smoke, payment request list, attachment access, notification center, report download, browser console, network API base URL, 결제 요청/첨부/승인/지급 보류/거래처/설정/보고서 업무 smoke와 AuditLog evidence를 요구하도록 했다. 실제 production frontend smoke 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: 운영 시작 전 23장/24장/25장 P0 미완료 항목이 없거나, 예외 승인과 보완 기한이 명시되어 있는지 확인
  - 진행 메모(2026-07-06): production go-live evidence 템플릿이 23/24/25장 open P0 count, approved exception list, exception owner/deadline, go-live readiness command result를 요구하도록 했다. 현재 `release:go-live-readiness`는 54 open P0를 보고하므로 완료 처리는 보류한다.
- [ ] P0: go-live 승인자는 기능 책임자, 보안 책임자, 재무 책임자, 운영 책임자로 분리
  - 진행 메모(2026-07-06): production go-live evidence 템플릿이 기능/보안/재무/운영 책임자의 승인자, 승인 시각, 증적 링크를 별도 행으로 요구하고 production release gate strict 검증에 연결됐다. 실제 승인자 서명/승인 ID는 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: rollback 기준, rollback 담당자, rollback 예상 시간, 사용자 공지 문구를 go-live 전에 확정
  - 진행 메모(2026-07-06): production go-live evidence 템플릿이 rollback trigger criteria, rollback owner/backup owner, estimated time, previous release manifest artifact, user notice message, read-only mode decision path를 요구하도록 했다. 실제 rollback 승인 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P1: go-live 당일 변경 freeze, 장애 대응 회의 채널, 상태 공유 주기를 운영
- [ ] P1: 첫 업무 처리 건은 운영자와 재무 책임자가 함께 관찰하고 감사 로그/알림/DB 반영을 확인

### 25.8 Go-Live 이후 안정화

- [ ] P0: 운영 첫 1주일 동안 로그인 실패, API 5xx, 승인 실패, 지급 실패, 파일 업로드 실패, 보고서 실패를 매일 점검
  - 진행 메모(2026-07-06): `docs/post-go-live-stabilization-evidence-template.md`와 `npm run release:post-go-live-stabilization-evidence`를 추가해 first week stabilization window, daily check owner, 로그인 실패, API 5xx, 승인 실패, 지급 실패, 파일 업로드 실패, 보고서 실패, requestId evidence, `/api/operations/alerts`, `/api/operations/business-failure-alerts`, `/api/operations/data-quality` 점검 결과를 안정화 판정 증빙으로 요구하도록 했다. 실제 운영 첫 주 daily check 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: 첫 지급 실행 후 은행 결과, ERP 상태, 감사 로그, 거래처 지급 이력, 보고서 금액을 대사
  - 진행 메모(2026-07-06): post go-live stabilization evidence 템플릿이 first disbursement ID, 은행 결과, ERP 상태, AuditLog evidence, 거래처 지급 이력 evidence, report totals evidence, 재무 책임자 sign-off를 요구하도록 했다. 실제 첫 production 지급 대사 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: backup과 PITR가 go-live 이후 실제 production 데이터로 성공했는지 첫 주 안에 확인
  - 진행 메모(2026-07-06): post go-live stabilization evidence 템플릿이 go-live 이후 production data 기준 full backup, backup verification, PITR target timestamp, PITR restore rehearsal, object storage restore, report artifact restore 증빙을 요구하도록 했다. 실제 production 데이터 백업/PITR 리허설 결과는 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: 사용자 문의와 장애를 severity 기준으로 분류하고 P0/P1은 당일 대응 원칙 적용
  - 진행 메모(2026-07-06): post go-live stabilization evidence 템플릿이 severity 기준, P0/P1 same-day response owner, requestId 수집 절차, inquiry intake channel, escalation SLA, incident register, remediation owner를 요구하고 production release gate strict 검증에 연결됐다. 실제 사용자 문의/장애 처리 증적은 아직 없으므로 완료 처리는 보류한다.
- [x] P1: 운영 첫 주 hypercare 리포트에 처리 건수, 실패 건수, 평균 처리 시간, 주요 문의, 보완 계획 포함
  - 진행 메모(2026-07-08): `docs/hypercare-runbook.md`와 `docs/post-go-live-stabilization-evidence-template.md`가 처리 건수, 실패 건수, 평균 처리 시간, 주요 문의, remediation plan, owner/deadline, 사용자 커뮤니케이션 요약을 요구한다.
  - 진행 메모(2026-07-06): post go-live stabilization evidence 템플릿이 hypercare report period, processing count, failure count, average processing time, major inquiry summary, remediation plan, owner/deadline, 사용자 커뮤니케이션 요약을 요구하도록 했다.
- [ ] P1: go-live 2주 후 운영 안정화 회고와 남은 P1/P2 backlog 우선순위 재조정
  - 진행 메모(2026-07-06): post go-live stabilization evidence 템플릿이 go-live +2 week review, remaining P1/P2 backlog, backlog priority decision, hotfix/next release plan, operations handoff decision, review sign-off를 요구하도록 했다.
- [ ] P2: 정기 릴리즈 주기, 긴급 hotfix 절차, 운영 개선 요청 intake 프로세스 확정

### 25.9 실사용 가능 최종 판정 기준

- [ ] P0: 실제 production 사용자가 결제 요청을 생성하고 승인자가 처리하며 재무팀이 지급 전 단계까지 안전하게 운영 가능
  - 진행 메모(2026-07-06): `docs/final-acceptance-evidence-template.md`와 `npm run release:final-acceptance-evidence`를 추가해 실제 production 사용자 기준 결제 요청 생성, 증빙 첨부, 승인자 처리, 재무팀 지급 전 단계 처리, 알림 확인, 보고서 생성/다운로드 증빙을 실사용 최종 판정 자료로 요구하도록 했다. 실제 production 사용자 업무 수행 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: 모든 핵심 데이터가 DB/object storage에 저장되고 새로고침, 재로그인, 다른 기기 접속 후에도 유지
  - 진행 메모(2026-07-06): final acceptance evidence 템플릿이 PaymentRequest, ApprovalStep, Attachment metadata, object storage object, Disbursement, ReportRun/report artifact 증거와 새로고침, 재로그인, 다른 기기 또는 다른 브라우저 접속 후 데이터 유지 증빙을 요구하도록 했다. 실제 production DB/object storage 대사 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: 권한 없는 사용자는 UI와 API 양쪽에서 차단되고 감사/보안 이벤트가 기록
  - 진행 메모(2026-07-06): final acceptance evidence 템플릿이 권한 없는 사용자 UI 차단, API 차단, AuditLog, security_events, requestId correlation, 파일 직접 접근 차단, 원문 계좌번호 비노출 증빙을 요구하도록 했다. 실제 production 권한 우회 시도와 이벤트 대사 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: 중복 승인, 중복 지급, 승인 전 지급, 마감 후 변경, 계좌 불일치 지급이 backend에서 차단
  - 진행 메모(2026-07-06): final acceptance evidence 템플릿이 중복 승인, 중복 지급, 승인 전 지급, 마감 후 변경, 계좌 불일치 지급, stale rowVersion 저장, 중복 idempotencyKey replay의 backend 차단 증빙을 요구하도록 했다. 실제 production 또는 승인된 운영형 rehearsal 차단 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: 장애 발생 시 rollback, 복구, 읽기 전용 전환, 사용자 공지 절차를 실제 담당자가 수행 가능
  - 진행 메모(2026-07-06): final acceptance evidence 템플릿이 rollback rehearsal, 복구 절차 수행 담당자, 읽기 전용 전환 판단, 사용자 공지 절차/문구, P0/P1 incident response owner, requestId 기반 추적, Backup/PITR restore owner를 요구하도록 했다. 실제 담당자 수행 증적은 아직 없으므로 완료 처리는 보류한다.
- [ ] P0: 운영 책임자가 배포, 모니터링, 백업, 장애 대응, 사용자 지원을 인수했다는 서명을 남김
  - 진행 메모(2026-07-06): final acceptance evidence 템플릿이 배포, 모니터링, 백업, 장애 대응, 사용자 지원, 권한/보안 운영 인수 증적과 운영 책임자 sign-off를 요구하도록 했다. 실제 운영 책임자 서명/인수 ID는 아직 없으므로 완료 처리는 보류한다.
- [ ] P1: 실사용 시작 후 측정한 KPI와 오류율이 go-live 승인 기준 이내
  - 진행 메모(2026-07-06): final acceptance evidence 템플릿이 KPI measurement window, go-live 승인 기준, actual processing KPI, actual error rate, API 5xx rate, approval/disbursement/file/report failure rate, KPI/error-rate decision을 요구하도록 했다.
- [ ] P2: 향후 개선 backlog가 운영 릴리즈 계획에 편입
  - 진행 메모(2026-07-06): final acceptance evidence 템플릿이 remaining P1/P2 backlog, 운영 릴리즈 계획, hotfix procedure owner, improvement intake process, next review date를 요구하도록 했다.
